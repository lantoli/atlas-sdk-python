# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.paginated_query_shapes import PaginatedQueryShapes
from ..types.query_shape_response import QueryShapeResponse
from ..types.query_stats_details_response import QueryStatsDetailsResponse
from ..types.query_stats_summary_list_response import QueryStatsSummaryListResponse
from .raw_client import AsyncRawQueryShapeInsightsClient, RawQueryShapeInsightsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class QueryShapeInsightsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawQueryShapeInsightsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawQueryShapeInsightsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawQueryShapeInsightsClient
        """
        return self._raw_client

    def get_query_shape_details(
        self,
        group_id: str,
        cluster_name: str,
        query_shape_hash: str,
        *,
        since: typing.Optional[int] = None,
        until: typing.Optional[int] = None,
        process_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryStatsDetailsResponse:
        """
        Returns the metadata and statistics summary for a given query shape hash.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        query_shape_hash : str
            A SHA256 hash of a query shape, output by MongoDB commands like `$queryStats` and `$explain` or slow query logs.

        since : typing.Optional[int]
            Date and time from which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        until : typing.Optional[int]
            Date and time up until which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you specify the **until** parameter, you must specify the **since** parameter.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        process_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Process IDs from which to retrieve query shape statistics. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processIds`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryStatsDetailsResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.query_shape_insights.get_query_shape_details(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            query_shape_hash="queryShapeHash",
        )
        """
        _response = self._raw_client.get_query_shape_details(
            group_id,
            cluster_name,
            query_shape_hash,
            since=since,
            until=until,
            process_ids=process_ids,
            request_options=request_options,
        )
        return _response.data

    def list_query_shape_summaries(
        self,
        group_id: str,
        cluster_name: str,
        *,
        since: typing.Optional[int] = None,
        until: typing.Optional[int] = None,
        process_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        namespaces: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        commands: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        n_summaries: typing.Optional[int] = None,
        series: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        query_shape_hashes: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryStatsSummaryListResponse:
        """
        Returns a list of query shape statistics summaries for a given cluster. Query shape statistics provide performance insights about MongoDB queries, helping users identify problematic query patterns and potential optimizations.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        since : typing.Optional[int]
            Date and time from which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        until : typing.Optional[int]
            Date and time up until which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you specify the **until** parameter, you must specify the **since** parameter.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        process_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Process IDs from which to retrieve query shape statistics. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processId`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.

        namespaces : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Namespaces from which to retrieve query shape statistics. A namespace consists of one database and one collection resource written as `.`: `<database>.<collection>`. To include multiple namespaces, pass the parameter multiple times delimited with an ampersand (`&`) between each namespace. Omit this parameter to return results for all namespaces.

        commands : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Retrieve query shape statistics matching specified MongoDB commands. To include multiple commands, pass the parameter multiple times delimited with an ampersand (`&`) between each command. The currently supported parameters are find, distinct, and aggregate. Omit this parameter to return results for all supported commands.

        n_summaries : typing.Optional[int]
            Maximum number of query statistic summaries to return.

        series : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Query shape statistics data series to retrieve. A series represents a specific metric about query execution. To include multiple series, pass the parameter multiple times delimited with an ampersand (`&`) between each series. Omit this parameter to return results for all available series.

        query_shape_hashes : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            A list of SHA256 hashes of desired query shapes, output by MongoDB commands like `$queryStats` and $explain or slow query logs. To include multiple series, pass the parameter multiple times delimited with an ampersand (`&`) between each series. Omit this parameter to return results for all available series.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryStatsSummaryListResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.query_shape_insights.list_query_shape_summaries(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_query_shape_summaries(
            group_id,
            cluster_name,
            since=since,
            until=until,
            process_ids=process_ids,
            namespaces=namespaces,
            commands=commands,
            n_summaries=n_summaries,
            series=series,
            query_shape_hashes=query_shape_hashes,
            request_options=request_options,
        )
        return _response.data

    def list_cluster_query_shapes(
        self,
        group_id: str,
        cluster_name: str,
        *,
        status: typing.Optional[str] = None,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedQueryShapes:
        """
        Returns a list of query shapes for one cluster. Query shapes may be filtered by their status; at present, this endpoint supports only the REJECTED status.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        status : typing.Optional[str]
            The status of query shapes to retrieve. Only REJECTED status is supported. If omitted, defaults to REJECTED.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedQueryShapes
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.query_shape_insights.list_cluster_query_shapes(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_cluster_query_shapes(
            group_id,
            cluster_name,
            status=status,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_query_shape(
        self,
        group_id: str,
        cluster_name: str,
        query_shape_hash: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryShapeResponse:
        """
        Returns the details for a single query shape. This endpoint only returns query shapes with REJECTED status. If the specified query shape hash does not correspond to a rejected query shape, a 404 Not Found error is returned.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        query_shape_hash : str
            A SHA256 hash of a query shape, output by MongoDB commands like `$queryStats` and `$explain` or slow query logs.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryShapeResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.query_shape_insights.get_cluster_query_shape(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            query_shape_hash="queryShapeHash",
        )
        """
        _response = self._raw_client.get_cluster_query_shape(
            group_id, cluster_name, query_shape_hash, request_options=request_options
        )
        return _response.data

    def update_cluster_query_shape(
        self,
        group_id: str,
        cluster_name: str,
        query_shape_hash: str,
        *,
        status: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryShapeResponse:
        """
        Updates the rejection status of a query shape. Use this endpoint to reject a query shape (preventing it from executing on the cluster) or to unreject a previously rejected query shape (allowing it to execute again). This operation is idempotent: rejecting an already rejected query shape or unrejecting an already unrejected query shape will return 200 OK.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        query_shape_hash : str
            A SHA256 hash of a query shape, output by MongoDB commands like `$queryStats` and `$explain` or slow query logs.

        status : str
            The rejection status of a query shape. Use REJECTED to prevent the query shape from executing on the cluster, or UNREJECTED to allow it to execute.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryShapeResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.query_shape_insights.update_cluster_query_shape(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            query_shape_hash="queryShapeHash",
            status="status",
        )
        """
        _response = self._raw_client.update_cluster_query_shape(
            group_id, cluster_name, query_shape_hash, status=status, request_options=request_options
        )
        return _response.data


class AsyncQueryShapeInsightsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawQueryShapeInsightsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawQueryShapeInsightsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawQueryShapeInsightsClient
        """
        return self._raw_client

    async def get_query_shape_details(
        self,
        group_id: str,
        cluster_name: str,
        query_shape_hash: str,
        *,
        since: typing.Optional[int] = None,
        until: typing.Optional[int] = None,
        process_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryStatsDetailsResponse:
        """
        Returns the metadata and statistics summary for a given query shape hash.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        query_shape_hash : str
            A SHA256 hash of a query shape, output by MongoDB commands like `$queryStats` and `$explain` or slow query logs.

        since : typing.Optional[int]
            Date and time from which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        until : typing.Optional[int]
            Date and time up until which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you specify the **until** parameter, you must specify the **since** parameter.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        process_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Process IDs from which to retrieve query shape statistics. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processIds`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryStatsDetailsResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.query_shape_insights.get_query_shape_details(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                query_shape_hash="queryShapeHash",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_query_shape_details(
            group_id,
            cluster_name,
            query_shape_hash,
            since=since,
            until=until,
            process_ids=process_ids,
            request_options=request_options,
        )
        return _response.data

    async def list_query_shape_summaries(
        self,
        group_id: str,
        cluster_name: str,
        *,
        since: typing.Optional[int] = None,
        until: typing.Optional[int] = None,
        process_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        namespaces: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        commands: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        n_summaries: typing.Optional[int] = None,
        series: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        query_shape_hashes: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryStatsSummaryListResponse:
        """
        Returns a list of query shape statistics summaries for a given cluster. Query shape statistics provide performance insights about MongoDB queries, helping users identify problematic query patterns and potential optimizations.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        since : typing.Optional[int]
            Date and time from which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        until : typing.Optional[int]
            Date and time up until which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

            - If you specify the **until** parameter, you must specify the **since** parameter.
            - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours.

        process_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Process IDs from which to retrieve query shape statistics. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processId`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.

        namespaces : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Namespaces from which to retrieve query shape statistics. A namespace consists of one database and one collection resource written as `.`: `<database>.<collection>`. To include multiple namespaces, pass the parameter multiple times delimited with an ampersand (`&`) between each namespace. Omit this parameter to return results for all namespaces.

        commands : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Retrieve query shape statistics matching specified MongoDB commands. To include multiple commands, pass the parameter multiple times delimited with an ampersand (`&`) between each command. The currently supported parameters are find, distinct, and aggregate. Omit this parameter to return results for all supported commands.

        n_summaries : typing.Optional[int]
            Maximum number of query statistic summaries to return.

        series : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Query shape statistics data series to retrieve. A series represents a specific metric about query execution. To include multiple series, pass the parameter multiple times delimited with an ampersand (`&`) between each series. Omit this parameter to return results for all available series.

        query_shape_hashes : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            A list of SHA256 hashes of desired query shapes, output by MongoDB commands like `$queryStats` and $explain or slow query logs. To include multiple series, pass the parameter multiple times delimited with an ampersand (`&`) between each series. Omit this parameter to return results for all available series.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryStatsSummaryListResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.query_shape_insights.list_query_shape_summaries(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_query_shape_summaries(
            group_id,
            cluster_name,
            since=since,
            until=until,
            process_ids=process_ids,
            namespaces=namespaces,
            commands=commands,
            n_summaries=n_summaries,
            series=series,
            query_shape_hashes=query_shape_hashes,
            request_options=request_options,
        )
        return _response.data

    async def list_cluster_query_shapes(
        self,
        group_id: str,
        cluster_name: str,
        *,
        status: typing.Optional[str] = None,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedQueryShapes:
        """
        Returns a list of query shapes for one cluster. Query shapes may be filtered by their status; at present, this endpoint supports only the REJECTED status.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        status : typing.Optional[str]
            The status of query shapes to retrieve. Only REJECTED status is supported. If omitted, defaults to REJECTED.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedQueryShapes
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.query_shape_insights.list_cluster_query_shapes(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_cluster_query_shapes(
            group_id,
            cluster_name,
            status=status,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_query_shape(
        self,
        group_id: str,
        cluster_name: str,
        query_shape_hash: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryShapeResponse:
        """
        Returns the details for a single query shape. This endpoint only returns query shapes with REJECTED status. If the specified query shape hash does not correspond to a rejected query shape, a 404 Not Found error is returned.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        query_shape_hash : str
            A SHA256 hash of a query shape, output by MongoDB commands like `$queryStats` and `$explain` or slow query logs.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryShapeResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.query_shape_insights.get_cluster_query_shape(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                query_shape_hash="queryShapeHash",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_query_shape(
            group_id, cluster_name, query_shape_hash, request_options=request_options
        )
        return _response.data

    async def update_cluster_query_shape(
        self,
        group_id: str,
        cluster_name: str,
        query_shape_hash: str,
        *,
        status: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> QueryShapeResponse:
        """
        Updates the rejection status of a query shape. Use this endpoint to reject a query shape (preventing it from executing on the cluster) or to unreject a previously rejected query shape (allowing it to execute again). This operation is idempotent: rejecting an already rejected query shape or unrejecting an already unrejected query shape will return 200 OK.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        query_shape_hash : str
            A SHA256 hash of a query shape, output by MongoDB commands like `$queryStats` and `$explain` or slow query logs.

        status : str
            The rejection status of a query shape. Use REJECTED to prevent the query shape from executing on the cluster, or UNREJECTED to allow it to execute.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        QueryShapeResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.query_shape_insights.update_cluster_query_shape(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                query_shape_hash="queryShapeHash",
                status="status",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_cluster_query_shape(
            group_id, cluster_name, query_shape_hash, status=status, request_options=request_options
        )
        return _response.data
