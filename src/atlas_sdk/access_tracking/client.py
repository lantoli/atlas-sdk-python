# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.mongo_db_access_logs_list import MongoDbAccessLogsList
from .raw_client import AsyncRawAccessTrackingClient, RawAccessTrackingClient


class AccessTrackingClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAccessTrackingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAccessTrackingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAccessTrackingClient
        """
        return self._raw_client

    def get_access_history_cluster(
        self,
        group_id: str,
        cluster_name: str,
        *,
        auth_result: typing.Optional[bool] = None,
        end: typing.Optional[int] = None,
        ip_address: typing.Optional[str] = None,
        n_logs: typing.Optional[int] = None,
        start: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MongoDbAccessLogsList:
        """
        Returns the access logs of one cluster identified by the cluster's name. Access logs contain a list of authentication requests made against your cluster. You can't use this feature on tenant-tier clusters (M0, M2, M5). To use this resource, the requesting Service Account or API Key must have the Project Monitoring Admin role, the Project Database Access Admin role, or the Project Cluster Log Viewer role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        auth_result : typing.Optional[bool]
            Flag that indicates whether the response returns the successful authentication attempts only.

        end : typing.Optional[int]
            Date and time when to stop retrieving database history. If you specify **end**, you must also specify **start**. This parameter uses UNIX epoch time in milliseconds.

        ip_address : typing.Optional[str]
            One Internet Protocol address that attempted to authenticate with the database.

        n_logs : typing.Optional[int]
            Maximum number of lines from the log to return.

        start : typing.Optional[int]
            Date and time when MongoDB Cloud begins retrieving database history. If you specify **start**, you must also specify **end**. This parameter uses UNIX epoch time in milliseconds.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MongoDbAccessLogsList
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.access_tracking.get_access_history_cluster(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.get_access_history_cluster(
            group_id,
            cluster_name,
            auth_result=auth_result,
            end=end,
            ip_address=ip_address,
            n_logs=n_logs,
            start=start,
            request_options=request_options,
        )
        return _response.data

    def get_access_history_process(
        self,
        group_id: str,
        hostname: str,
        *,
        auth_result: typing.Optional[bool] = None,
        end: typing.Optional[int] = None,
        ip_address: typing.Optional[str] = None,
        n_logs: typing.Optional[int] = None,
        start: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MongoDbAccessLogsList:
        """
        Returns the access logs of one cluster identified by the cluster's hostname. Access logs contain a list of authentication requests made against your clusters. You can't use this feature on tenant-tier clusters (M0, M2, M5). To use this resource, the requesting Service Account or API Key must have the Project Monitoring Admin role, the Project Database Access Admin role, or the Project Cluster Log Viewer role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        hostname : str
            Fully qualified domain name or IP address of the MongoDB host that stores the log files that you want to download.

        auth_result : typing.Optional[bool]
            Flag that indicates whether the response returns the successful authentication attempts only.

        end : typing.Optional[int]
            Date and time when to stop retrieving database history. If you specify **end**, you must also specify **start**. This parameter uses UNIX epoch time in milliseconds.

        ip_address : typing.Optional[str]
            One Internet Protocol address that attempted to authenticate with the database.

        n_logs : typing.Optional[int]
            Maximum number of lines from the log to return.

        start : typing.Optional[int]
            Date and time when MongoDB Cloud begins retrieving database history. If you specify **start**, you must also specify **end**. This parameter uses UNIX epoch time in milliseconds.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MongoDbAccessLogsList
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.access_tracking.get_access_history_process(
            group_id="32b6e34b3d91647abb20e7b8",
            hostname="hostname",
        )
        """
        _response = self._raw_client.get_access_history_process(
            group_id,
            hostname,
            auth_result=auth_result,
            end=end,
            ip_address=ip_address,
            n_logs=n_logs,
            start=start,
            request_options=request_options,
        )
        return _response.data


class AsyncAccessTrackingClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAccessTrackingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAccessTrackingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAccessTrackingClient
        """
        return self._raw_client

    async def get_access_history_cluster(
        self,
        group_id: str,
        cluster_name: str,
        *,
        auth_result: typing.Optional[bool] = None,
        end: typing.Optional[int] = None,
        ip_address: typing.Optional[str] = None,
        n_logs: typing.Optional[int] = None,
        start: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MongoDbAccessLogsList:
        """
        Returns the access logs of one cluster identified by the cluster's name. Access logs contain a list of authentication requests made against your cluster. You can't use this feature on tenant-tier clusters (M0, M2, M5). To use this resource, the requesting Service Account or API Key must have the Project Monitoring Admin role, the Project Database Access Admin role, or the Project Cluster Log Viewer role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        auth_result : typing.Optional[bool]
            Flag that indicates whether the response returns the successful authentication attempts only.

        end : typing.Optional[int]
            Date and time when to stop retrieving database history. If you specify **end**, you must also specify **start**. This parameter uses UNIX epoch time in milliseconds.

        ip_address : typing.Optional[str]
            One Internet Protocol address that attempted to authenticate with the database.

        n_logs : typing.Optional[int]
            Maximum number of lines from the log to return.

        start : typing.Optional[int]
            Date and time when MongoDB Cloud begins retrieving database history. If you specify **start**, you must also specify **end**. This parameter uses UNIX epoch time in milliseconds.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MongoDbAccessLogsList
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.access_tracking.get_access_history_cluster(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_access_history_cluster(
            group_id,
            cluster_name,
            auth_result=auth_result,
            end=end,
            ip_address=ip_address,
            n_logs=n_logs,
            start=start,
            request_options=request_options,
        )
        return _response.data

    async def get_access_history_process(
        self,
        group_id: str,
        hostname: str,
        *,
        auth_result: typing.Optional[bool] = None,
        end: typing.Optional[int] = None,
        ip_address: typing.Optional[str] = None,
        n_logs: typing.Optional[int] = None,
        start: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MongoDbAccessLogsList:
        """
        Returns the access logs of one cluster identified by the cluster's hostname. Access logs contain a list of authentication requests made against your clusters. You can't use this feature on tenant-tier clusters (M0, M2, M5). To use this resource, the requesting Service Account or API Key must have the Project Monitoring Admin role, the Project Database Access Admin role, or the Project Cluster Log Viewer role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        hostname : str
            Fully qualified domain name or IP address of the MongoDB host that stores the log files that you want to download.

        auth_result : typing.Optional[bool]
            Flag that indicates whether the response returns the successful authentication attempts only.

        end : typing.Optional[int]
            Date and time when to stop retrieving database history. If you specify **end**, you must also specify **start**. This parameter uses UNIX epoch time in milliseconds.

        ip_address : typing.Optional[str]
            One Internet Protocol address that attempted to authenticate with the database.

        n_logs : typing.Optional[int]
            Maximum number of lines from the log to return.

        start : typing.Optional[int]
            Date and time when MongoDB Cloud begins retrieving database history. If you specify **start**, you must also specify **end**. This parameter uses UNIX epoch time in milliseconds.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MongoDbAccessLogsList
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.access_tracking.get_access_history_process(
                group_id="32b6e34b3d91647abb20e7b8",
                hostname="hostname",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_access_history_process(
            group_id,
            hostname,
            auth_result=auth_result,
            end=end,
            ip_address=ip_address,
            n_logs=n_logs,
            start=start,
            request_options=request_options,
        )
        return _response.data
