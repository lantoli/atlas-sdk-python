# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.account_details import AccountDetails
from ..types.azure_connection import AzureConnection
from ..types.base_streams_region import BaseStreamsRegion
from ..types.db_role_to_execute import DbRoleToExecute
from ..types.document import Document
from ..types.link import Link
from ..types.paginated_api_streams_connection import PaginatedApiStreamsConnection
from ..types.paginated_api_streams_private_link import PaginatedApiStreamsPrivateLink
from ..types.paginated_api_streams_stream_processor_with_stats import PaginatedApiStreamsStreamProcessorWithStats
from ..types.paginated_api_streams_tenant import PaginatedApiStreamsTenant
from ..types.paginated_api_streams_vpc_peering_connection import PaginatedApiStreamsVpcPeeringConnection
from ..types.schema_registry_authentication import SchemaRegistryAuthentication
from ..types.stream_config import StreamConfig
from ..types.streams_aws_connection_config import StreamsAwsConnectionConfig
from ..types.streams_connection import StreamsConnection
from ..types.streams_data_process_region import StreamsDataProcessRegion
from ..types.streams_gcp_connection_config import StreamsGcpConnectionConfig
from ..types.streams_kafka_authentication import StreamsKafkaAuthentication
from ..types.streams_kafka_networking import StreamsKafkaNetworking
from ..types.streams_kafka_security import StreamsKafkaSecurity
from ..types.streams_modify_stream_processor_options import StreamsModifyStreamProcessorOptions
from ..types.streams_options import StreamsOptions
from ..types.streams_private_link_connection import StreamsPrivateLinkConnection
from ..types.streams_processor import StreamsProcessor
from ..types.streams_processor_with_stats import StreamsProcessorWithStats
from ..types.streams_public_private_link_networking import StreamsPublicPrivateLinkNetworking
from ..types.streams_sample_connections import StreamsSampleConnections
from ..types.streams_tenant import StreamsTenant
from ..types.tenant_with_sample_connections import TenantWithSampleConnections
from .raw_client import AsyncRawStreamsClient, RawStreamsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class StreamsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawStreamsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawStreamsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawStreamsClient
        """
        return self._raw_client

    def list_stream_workspaces(
        self,
        group_id: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsTenant:
        """
        Returns all stream workspaces for the specified project.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsTenant
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.list_stream_workspaces(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.list_stream_workspaces(
            group_id, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    def create_stream_workspace(
        self,
        group_id_: str,
        *,
        id: typing.Optional[str] = OMIT,
        connections: typing.Optional[typing.Sequence[StreamsConnection]] = OMIT,
        data_process_region: typing.Optional[StreamsDataProcessRegion] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        hostnames: typing.Optional[typing.Sequence[str]] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        sample_connections: typing.Optional[StreamsSampleConnections] = OMIT,
        stream_config: typing.Optional[StreamConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Creates one stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : typing.Optional[str]
            Unique 24-hexadecimal character string that identifies the project.

        connections : typing.Optional[typing.Sequence[StreamsConnection]]
            List of connections configured in the stream workspace.

        data_process_region : typing.Optional[StreamsDataProcessRegion]

        group_id : typing.Optional[str]
            Unique 24-hexadecimal character string that identifies the project.

        hostnames : typing.Optional[typing.Sequence[str]]
            List that contains the hostnames assigned to the stream workspace.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Label that identifies the stream workspace.

        sample_connections : typing.Optional[StreamsSampleConnections]

        stream_config : typing.Optional[StreamConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.create_stream_workspace(
            group_id_="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.create_stream_workspace(
            group_id_,
            id=id,
            connections=connections,
            data_process_region=data_process_region,
            group_id=group_id,
            hostnames=hostnames,
            links=links,
            name=name,
            sample_connections=sample_connections,
            stream_config=stream_config,
            request_options=request_options,
        )
        return _response.data

    def get_stream_workspace(
        self,
        group_id: str,
        tenant_name: str,
        *,
        include_connections: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Returns the details of one stream workspace within the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to return.

        include_connections : typing.Optional[bool]
            Flag to indicate whether connections information should be included in the stream workspace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.get_stream_workspace(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.get_stream_workspace(
            group_id, tenant_name, include_connections=include_connections, request_options=request_options
        )
        return _response.data

    def delete_stream_workspace(
        self, group_id: str, tenant_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Delete one stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.delete_stream_workspace(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.delete_stream_workspace(group_id, tenant_name, request_options=request_options)
        return _response.data

    def update_stream_workspace(
        self,
        group_id: str,
        tenant_name: str,
        *,
        cloud_provider: typing.Optional[str] = OMIT,
        region: typing.Optional[BaseStreamsRegion] = OMIT,
        stream_config: typing.Optional[StreamConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Update one stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to update.

        cloud_provider : typing.Optional[str]
            Human-readable label that identifies the cloud provider.

        region : typing.Optional[BaseStreamsRegion]

        stream_config : typing.Optional[StreamConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.update_stream_workspace(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.update_stream_workspace(
            group_id,
            tenant_name,
            cloud_provider=cloud_provider,
            region=region,
            stream_config=stream_config,
            request_options=request_options,
        )
        return _response.data

    def download_audit_logs(
        self,
        group_id: str,
        tenant_name: str,
        *,
        end_date: typing.Optional[int] = None,
        start_date: typing.Optional[int] = None,
        sp_name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[bytes]:
        """
        Downloads the audit logs for the specified Atlas Streams Processing workspace or stream processor. By default, logs cover periods of 30 days. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role. The API does not support direct calls with the json response schema. You must request a gzip response schema using an accept header of the format: `Accept: application/vnd.atlas.YYYY-MM-DD+gzip`.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        end_date : typing.Optional[int]
            Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        start_date : typing.Optional[int]
            Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        sp_name : typing.Optional[str]
            Name of the stream processor to download logs for. An empty string will download logs for all stream processors in the workspace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.Iterator[bytes]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.download_audit_logs()
        """
        with self._raw_client.download_audit_logs(
            group_id,
            tenant_name,
            end_date=end_date,
            start_date=start_date,
            sp_name=sp_name,
            request_options=request_options,
        ) as r:
            yield from r.data

    def list_stream_connections(
        self,
        group_id: str,
        tenant_name: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsConnection:
        """
        Returns all connections of the stream workspace for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.list_stream_connections(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.list_stream_connections(
            group_id, tenant_name, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    def create_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        *,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        region: typing.Optional[str] = OMIT,
        state: typing.Optional[str] = OMIT,
        type: typing.Optional[str] = OMIT,
        cluster_group_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        db_role_to_execute: typing.Optional[DbRoleToExecute] = OMIT,
        authentication: typing.Optional[StreamsKafkaAuthentication] = OMIT,
        bootstrap_servers: typing.Optional[str] = OMIT,
        config: typing.Optional[typing.Dict[str, str]] = OMIT,
        networking: typing.Optional[StreamsKafkaNetworking] = OMIT,
        security: typing.Optional[StreamsKafkaSecurity] = OMIT,
        headers: typing.Optional[typing.Dict[str, str]] = OMIT,
        url: typing.Optional[str] = OMIT,
        aws: typing.Optional[StreamsAwsConnectionConfig] = OMIT,
        provider: typing.Optional[str] = OMIT,
        schema_registry_authentication: typing.Optional[SchemaRegistryAuthentication] = OMIT,
        schema_registry_urls: typing.Optional[typing.Sequence[str]] = OMIT,
        azure: typing.Optional[AzureConnection] = OMIT,
        public_private_networking: typing.Optional[StreamsPublicPrivateLinkNetworking] = OMIT,
        gcp: typing.Optional[StreamsGcpConnectionConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsConnection:
        """
        Creates one connection for a stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        id : typing.Optional[str]
            Unique identifier of the connection.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Human-readable label that identifies the stream connection. In the case of the Sample type, this is the name of the sample source.

        region : typing.Optional[str]
            The connection's region.

        state : typing.Optional[str]
            The state of the connection.

        type : typing.Optional[str]
            Type of the connection.

        cluster_group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.

        cluster_name : typing.Optional[str]
            Name of the cluster configured for this connection.

        db_role_to_execute : typing.Optional[DbRoleToExecute]

        authentication : typing.Optional[StreamsKafkaAuthentication]

        bootstrap_servers : typing.Optional[str]
            Comma separated list of server addresses.

        config : typing.Optional[typing.Dict[str, str]]
            A map of Kafka key-value pairs for optional configuration. This is a flat object, and keys can have '.' characters.

        networking : typing.Optional[StreamsKafkaNetworking]

        security : typing.Optional[StreamsKafkaSecurity]

        headers : typing.Optional[typing.Dict[str, str]]
            A map of key-value pairs that will be passed as headers for the request.

        url : typing.Optional[str]
            The URL to be used for the request.

        aws : typing.Optional[StreamsAwsConnectionConfig]

        provider : typing.Optional[str]
            The Schema Registry provider.

        schema_registry_authentication : typing.Optional[SchemaRegistryAuthentication]

        schema_registry_urls : typing.Optional[typing.Sequence[str]]
            List of Schema Registry endpoint URLs used by this connection. Each URL must use the http or https scheme and specify a valid host and optional port.

        azure : typing.Optional[AzureConnection]

        public_private_networking : typing.Optional[StreamsPublicPrivateLinkNetworking]

        gcp : typing.Optional[StreamsGcpConnectionConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.create_stream_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.create_stream_connection(
            group_id,
            tenant_name,
            id=id,
            links=links,
            name=name,
            region=region,
            state=state,
            type=type,
            cluster_group_id=cluster_group_id,
            cluster_name=cluster_name,
            db_role_to_execute=db_role_to_execute,
            authentication=authentication,
            bootstrap_servers=bootstrap_servers,
            config=config,
            networking=networking,
            security=security,
            headers=headers,
            url=url,
            aws=aws,
            provider=provider,
            schema_registry_authentication=schema_registry_authentication,
            schema_registry_urls=schema_registry_urls,
            azure=azure,
            public_private_networking=public_private_networking,
            gcp=gcp,
            request_options=request_options,
        )
        return _response.data

    def get_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        connection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsConnection:
        """
        Returns the details of one stream connection within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to return.

        connection_name : str
            Label that identifies the stream connection to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.get_stream_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            connection_name="connectionName",
        )
        """
        _response = self._raw_client.get_stream_connection(
            group_id, tenant_name, connection_name, request_options=request_options
        )
        return _response.data

    def delete_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        connection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Delete one connection of the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        connection_name : str
            Label that identifies the stream connection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.delete_stream_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            connection_name="connectionName",
        )
        """
        _response = self._raw_client.delete_stream_connection(
            group_id, tenant_name, connection_name, request_options=request_options
        )
        return _response.data

    def update_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        connection_name: str,
        *,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        region: typing.Optional[str] = OMIT,
        state: typing.Optional[str] = OMIT,
        type: typing.Optional[str] = OMIT,
        cluster_group_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        db_role_to_execute: typing.Optional[DbRoleToExecute] = OMIT,
        authentication: typing.Optional[StreamsKafkaAuthentication] = OMIT,
        bootstrap_servers: typing.Optional[str] = OMIT,
        config: typing.Optional[typing.Dict[str, str]] = OMIT,
        networking: typing.Optional[StreamsKafkaNetworking] = OMIT,
        security: typing.Optional[StreamsKafkaSecurity] = OMIT,
        headers: typing.Optional[typing.Dict[str, str]] = OMIT,
        url: typing.Optional[str] = OMIT,
        aws: typing.Optional[StreamsAwsConnectionConfig] = OMIT,
        provider: typing.Optional[str] = OMIT,
        schema_registry_authentication: typing.Optional[SchemaRegistryAuthentication] = OMIT,
        schema_registry_urls: typing.Optional[typing.Sequence[str]] = OMIT,
        azure: typing.Optional[AzureConnection] = OMIT,
        public_private_networking: typing.Optional[StreamsPublicPrivateLinkNetworking] = OMIT,
        gcp: typing.Optional[StreamsGcpConnectionConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsConnection:
        """
        Update one connection for the specified stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        connection_name : str
            Label that identifies the stream connection.

        id : typing.Optional[str]
            Unique identifier of the connection.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Human-readable label that identifies the stream connection. In the case of the Sample type, this is the name of the sample source.

        region : typing.Optional[str]
            The connection's region.

        state : typing.Optional[str]
            The state of the connection.

        type : typing.Optional[str]
            Type of the connection.

        cluster_group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.

        cluster_name : typing.Optional[str]
            Name of the cluster configured for this connection.

        db_role_to_execute : typing.Optional[DbRoleToExecute]

        authentication : typing.Optional[StreamsKafkaAuthentication]

        bootstrap_servers : typing.Optional[str]
            Comma separated list of server addresses.

        config : typing.Optional[typing.Dict[str, str]]
            A map of Kafka key-value pairs for optional configuration. This is a flat object, and keys can have '.' characters.

        networking : typing.Optional[StreamsKafkaNetworking]

        security : typing.Optional[StreamsKafkaSecurity]

        headers : typing.Optional[typing.Dict[str, str]]
            A map of key-value pairs that will be passed as headers for the request.

        url : typing.Optional[str]
            The URL to be used for the request.

        aws : typing.Optional[StreamsAwsConnectionConfig]

        provider : typing.Optional[str]
            The Schema Registry provider.

        schema_registry_authentication : typing.Optional[SchemaRegistryAuthentication]

        schema_registry_urls : typing.Optional[typing.Sequence[str]]
            List of Schema Registry endpoint URLs used by this connection. Each URL must use the http or https scheme and specify a valid host and optional port.

        azure : typing.Optional[AzureConnection]

        public_private_networking : typing.Optional[StreamsPublicPrivateLinkNetworking]

        gcp : typing.Optional[StreamsGcpConnectionConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.update_stream_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            connection_name="connectionName",
        )
        """
        _response = self._raw_client.update_stream_connection(
            group_id,
            tenant_name,
            connection_name,
            id=id,
            links=links,
            name=name,
            region=region,
            state=state,
            type=type,
            cluster_group_id=cluster_group_id,
            cluster_name=cluster_name,
            db_role_to_execute=db_role_to_execute,
            authentication=authentication,
            bootstrap_servers=bootstrap_servers,
            config=config,
            networking=networking,
            security=security,
            headers=headers,
            url=url,
            aws=aws,
            provider=provider,
            schema_registry_authentication=schema_registry_authentication,
            schema_registry_urls=schema_registry_urls,
            azure=azure,
            public_private_networking=public_private_networking,
            gcp=gcp,
            request_options=request_options,
        )
        return _response.data

    def create_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        *,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        options: typing.Optional[StreamsOptions] = OMIT,
        pipeline: typing.Optional[typing.Sequence[Document]] = OMIT,
        tier: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsProcessor:
        """
        Create one Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        id : typing.Optional[str]
            Unique 24-hexadecimal character string that identifies the stream processor.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Human-readable name of the stream processor.

        options : typing.Optional[StreamsOptions]

        pipeline : typing.Optional[typing.Sequence[Document]]
            Stream aggregation pipeline you want to apply to your streaming data.

        tier : typing.Optional[str]
            Selected tier for the Stream Workspace. Configures Memory / VCPU allowances.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsProcessor
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.create_stream_processor(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.create_stream_processor(
            group_id,
            tenant_name,
            id=id,
            links=links,
            name=name,
            options=options,
            pipeline=pipeline,
            tier=tier,
            request_options=request_options,
        )
        return _response.data

    def get_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsProcessorWithStats:
        """
        Get one Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsProcessorWithStats
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.get_stream_processor(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            processor_name="processorName",
        )
        """
        _response = self._raw_client.get_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    def delete_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Delete a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.delete_stream_processor(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            processor_name="processorName",
        )
        """
        _response = self._raw_client.delete_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    def update_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        name: typing.Optional[str] = OMIT,
        options: typing.Optional[StreamsModifyStreamProcessorOptions] = OMIT,
        pipeline: typing.Optional[typing.Sequence[Document]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsProcessorWithStats:
        """
        Modify one existing Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        name : typing.Optional[str]
            New name for the stream processor.

        options : typing.Optional[StreamsModifyStreamProcessorOptions]

        pipeline : typing.Optional[typing.Sequence[Document]]
            New pipeline for the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsProcessorWithStats
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.update_stream_processor(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            processor_name="processorName",
        )
        """
        _response = self._raw_client.update_stream_processor(
            group_id,
            tenant_name,
            processor_name,
            name=name,
            options=options,
            pipeline=pipeline,
            request_options=request_options,
        )
        return _response.data

    def start_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Start a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.start_stream_processor(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            processor_name="processorName",
        )
        """
        _response = self._raw_client.start_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    def start_stream_processor_with(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        resume_from_checkpoint: typing.Optional[bool] = OMIT,
        start_at_operation_time: typing.Optional[dt.datetime] = OMIT,
        tier: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Start a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        resume_from_checkpoint : typing.Optional[bool]
            When true or not specified, the stream processor resumes from its last checkpoint. When false, the stream processor starts fresh.

        start_at_operation_time : typing.Optional[dt.datetime]
            The operation time after which the change stream source should begin reporting. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        tier : typing.Optional[str]
            Selected tier for the Stream Workspace. Configures Memory / VCPU allowances.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.start_stream_processor_with(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            processor_name="processorName",
        )
        """
        _response = self._raw_client.start_stream_processor_with(
            group_id,
            tenant_name,
            processor_name,
            resume_from_checkpoint=resume_from_checkpoint,
            start_at_operation_time=start_at_operation_time,
            tier=tier,
            request_options=request_options,
        )
        return _response.data

    def stop_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Stop a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.stop_stream_processor(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
            processor_name="processorName",
        )
        """
        _response = self._raw_client.stop_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    def get_stream_processors(
        self,
        group_id: str,
        tenant_name: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        include_count: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsStreamProcessorWithStats:
        """
        Returns all Stream Processors within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsStreamProcessorWithStats
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.get_stream_processors(
            group_id="32b6e34b3d91647abb20e7b8",
            tenant_name="tenantName",
        )
        """
        _response = self._raw_client.get_stream_processors(
            group_id,
            tenant_name,
            items_per_page=items_per_page,
            page_num=page_num,
            include_count=include_count,
            request_options=request_options,
        )
        return _response.data

    def download_operational_logs(
        self,
        group_id: str,
        tenant_name: str,
        *,
        end_date: typing.Optional[int] = None,
        start_date: typing.Optional[int] = None,
        sp_name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[bytes]:
        """
        Downloads the operational logs for the specified Atlas Streams Processing workspace or stream processor. By default, logs cover periods of 30 days. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role. The API does not support direct calls with the json response schema. You must request a gzip response schema using an accept header of the format: "Accept: application/vnd.atlas.2025-03-12+gzip".

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        end_date : typing.Optional[int]
            Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        start_date : typing.Optional[int]
            Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        sp_name : typing.Optional[str]
            Name of the stream processor to download logs for. An empty string will download logs for all stream processors in the workspace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.Iterator[bytes]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.download_operational_logs()
        """
        with self._raw_client.download_operational_logs(
            group_id,
            tenant_name,
            end_date=end_date,
            start_date=start_date,
            sp_name=sp_name,
            request_options=request_options,
        ) as r:
            yield from r.data

    def get_account_details(
        self,
        group_id: str,
        *,
        cloud_provider: str,
        region_name: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccountDetails:
        """
        Returns the Account ID, and the VPC ID for the group and region specified.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            One of "aws", "azure" or "gcp".

        region_name : str
            The cloud provider specific region name, i.e. "US_EAST_1" for cloud provider "aws".

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccountDetails
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.get_account_details(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
            region_name="regionName",
        )
        """
        _response = self._raw_client.get_account_details(
            group_id, cloud_provider=cloud_provider, region_name=region_name, request_options=request_options
        )
        return _response.data

    def list_active_peering_connections(
        self,
        group_id: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsVpcPeeringConnection:
        """
        Returns a list of active incoming VPC Peering Connections.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsVpcPeeringConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.list_active_peering_connections(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.list_active_peering_connections(
            group_id, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    def list_private_link_connections(
        self,
        group_id: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsPrivateLink:
        """
        Returns all Private Link connections for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role, Project Data Access roles, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsPrivateLink
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.list_private_link_connections(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.list_private_link_connections(
            group_id, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    def create_private_link_connection(
        self,
        group_id: str,
        *,
        provider: str,
        id: typing.Optional[str] = OMIT,
        arn: typing.Optional[str] = OMIT,
        azure_resource_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        dns_domain: typing.Optional[str] = OMIT,
        dns_sub_domain: typing.Optional[typing.Sequence[str]] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        gcp_connection_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        gcp_service_attachment_uris: typing.Optional[typing.Sequence[str]] = OMIT,
        interface_endpoint_id: typing.Optional[str] = OMIT,
        interface_endpoint_name: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        provider_account_id: typing.Optional[str] = OMIT,
        region: typing.Optional[str] = OMIT,
        service_endpoint_id: typing.Optional[str] = OMIT,
        state: typing.Optional[str] = OMIT,
        vendor: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsPrivateLinkConnection:
        """
        Creates one Private Link in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        provider : str
            Provider where the Kafka cluster is deployed. Valid values are AWS and AZURE.

        id : typing.Optional[str]
            The ID of the Private Link connection.

        arn : typing.Optional[str]
            Amazon Resource Name (ARN). Required for AWS Provider and MSK vendor.

        azure_resource_ids : typing.Optional[typing.Sequence[str]]
            Azure Resource IDs of each availability zone for the Azure Confluent cluster.

        dns_domain : typing.Optional[str]
            The domain hostname. Required for the following provider and vendor combinations:
            - AWS provider with CONFLUENT vendor.
            - AZURE provider with EVENTHUB or CONFLUENT vendor.

        dns_sub_domain : typing.Optional[typing.Sequence[str]]
            Sub-Domain name of Confluent cluster. These are typically your availability zones. Required for AWS Provider and CONFLUENT vendor, if your AWS CONFLUENT cluster doesn't use subdomains, you must set this to the empty array [].

        error_message : typing.Optional[str]
            Error message if the state is FAILED.

        gcp_connection_ids : typing.Optional[typing.Sequence[str]]
            List of GCP Private Service Connect connection IDs.

        gcp_service_attachment_uris : typing.Optional[typing.Sequence[str]]
            Service Attachment URIs of each availability zone for the GCP Confluent cluster.

        interface_endpoint_id : typing.Optional[str]
            Interface endpoint ID that is created from the service endpoint ID provided.

        interface_endpoint_name : typing.Optional[str]
            Interface endpoint name that is created from the service endpoint ID provided.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        provider_account_id : typing.Optional[str]
            Account ID from the cloud provider.

        region : typing.Optional[str]
            The region of the Provider’s cluster. See [AZURE](https://www.mongodb.com/docs/atlas/reference/microsoft-azure/#stream-processing-instances) and [AWS](https://www.mongodb.com/docs/atlas/reference/amazon-aws/#stream-processing-instances) supported regions.

        service_endpoint_id : typing.Optional[str]
            For AZURE EVENTHUB, this is the [namespace endpoint ID](https://learn.microsoft.com/en-us/rest/api/eventhub/namespaces/get). For AWS CONFLUENT cluster, this is the [VPC Endpoint service name](https://docs.confluent.io/cloud/current/networking/private-links/aws-privatelink.html).

        state : typing.Optional[str]
            State the connection is in.

        vendor : typing.Optional[str]
            Vendor that manages the cloud service. The list of supported vendor values is:
            - AWS
            -- `MSK` for AWS MSK Kafka clusters
            -- `CONFLUENT` for Confluent Kafka clusters on AWS
            -- `KINESIS` for AWS Kinesis Data Streams

            - Azure
            -- `EVENTHUB` for Azure EventHub.
            -- `CONFLUENT` for Confluent Kafka clusters on Azure
            -- `AZURE_BLOB_STORAGE` for Azure Blob Storage

            - GCP
            -- `CONFLUENT` for Confluent Kafka clusters on GCP
            -- `PUBSUB` for Google Cloud Pub/Sub

            **NOTE** Omitting the vendor field will default to using the GENERIC vendor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsPrivateLinkConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.create_private_link_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            provider="provider",
        )
        """
        _response = self._raw_client.create_private_link_connection(
            group_id,
            provider=provider,
            id=id,
            arn=arn,
            azure_resource_ids=azure_resource_ids,
            dns_domain=dns_domain,
            dns_sub_domain=dns_sub_domain,
            error_message=error_message,
            gcp_connection_ids=gcp_connection_ids,
            gcp_service_attachment_uris=gcp_service_attachment_uris,
            interface_endpoint_id=interface_endpoint_id,
            interface_endpoint_name=interface_endpoint_name,
            links=links,
            provider_account_id=provider_account_id,
            region=region,
            service_endpoint_id=service_endpoint_id,
            state=state,
            vendor=vendor,
            request_options=request_options,
        )
        return _response.data

    def get_private_link_connection(
        self, group_id: str, connection_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> StreamsPrivateLinkConnection:
        """
        Returns the details of one Private Link connection within the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        connection_id : str
            Unique ID that identifies the Private Link connection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsPrivateLinkConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.get_private_link_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            connection_id="connectionId",
        )
        """
        _response = self._raw_client.get_private_link_connection(
            group_id, connection_id, request_options=request_options
        )
        return _response.data

    def delete_private_link_connection(
        self, group_id: str, connection_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Deletes one Private Link in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        connection_id : str
            Unique ID that identifies the Private Link connection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.delete_private_link_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            connection_id="connectionId",
        )
        """
        _response = self._raw_client.delete_private_link_connection(
            group_id, connection_id, request_options=request_options
        )
        return _response.data

    def list_vpc_peering_connections(
        self,
        group_id: str,
        *,
        requester_account_id: str,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsVpcPeeringConnection:
        """
        Returns a list of incoming VPC Peering Connections.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        requester_account_id : str
            The Account ID of the VPC Peering connection/s.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsVpcPeeringConnection
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.list_vpc_peering_connections(
            group_id="32b6e34b3d91647abb20e7b8",
            requester_account_id="requesterAccountId",
        )
        """
        _response = self._raw_client.list_vpc_peering_connections(
            group_id,
            requester_account_id=requester_account_id,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    def delete_vpc_peering_connection(
        self, group_id: str, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Deletes an incoming VPC Peering connection.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : str
            The VPC Peering Connection id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.delete_vpc_peering_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            id="id",
        )
        """
        _response = self._raw_client.delete_vpc_peering_connection(group_id, id, request_options=request_options)
        return _response.data

    def accept_vpc_peering_connection(
        self,
        group_id: str,
        id: str,
        *,
        requester_account_id: typing.Optional[str] = OMIT,
        requester_vpc_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Requests the acceptance of an incoming VPC Peering connection.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : str
            The VPC Peering Connection id.

        requester_account_id : typing.Optional[str]
            The AWS requester account ID.

        requester_vpc_id : typing.Optional[str]
            The AWS requester VPC ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.accept_vpc_peering_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            id="id",
        )
        """
        _response = self._raw_client.accept_vpc_peering_connection(
            group_id,
            id,
            requester_account_id=requester_account_id,
            requester_vpc_id=requester_vpc_id,
            request_options=request_options,
        )
        return _response.data

    def reject_vpc_peering_connection(
        self, group_id: str, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Requests the rejection of an incoming VPC Peering connection.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : str
            The VPC Peering Connection id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.reject_vpc_peering_connection(
            group_id="32b6e34b3d91647abb20e7b8",
            id="id",
        )
        """
        _response = self._raw_client.reject_vpc_peering_connection(group_id, id, request_options=request_options)
        return _response.data

    def with_stream_sample_connections(
        self,
        group_id: str,
        *,
        request: TenantWithSampleConnections,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Creates one stream workspace in the specified project with sample connections. To use this resource the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request : TenantWithSampleConnections

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.streams.with_stream_sample_connections(
            group_id="32b6e34b3d91647abb20e7b8",
            request={"key": "value"},
        )
        """
        _response = self._raw_client.with_stream_sample_connections(
            group_id, request=request, request_options=request_options
        )
        return _response.data


class AsyncStreamsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawStreamsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawStreamsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawStreamsClient
        """
        return self._raw_client

    async def list_stream_workspaces(
        self,
        group_id: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsTenant:
        """
        Returns all stream workspaces for the specified project.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsTenant
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.list_stream_workspaces(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_stream_workspaces(
            group_id, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    async def create_stream_workspace(
        self,
        group_id_: str,
        *,
        id: typing.Optional[str] = OMIT,
        connections: typing.Optional[typing.Sequence[StreamsConnection]] = OMIT,
        data_process_region: typing.Optional[StreamsDataProcessRegion] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        hostnames: typing.Optional[typing.Sequence[str]] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        sample_connections: typing.Optional[StreamsSampleConnections] = OMIT,
        stream_config: typing.Optional[StreamConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Creates one stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : typing.Optional[str]
            Unique 24-hexadecimal character string that identifies the project.

        connections : typing.Optional[typing.Sequence[StreamsConnection]]
            List of connections configured in the stream workspace.

        data_process_region : typing.Optional[StreamsDataProcessRegion]

        group_id : typing.Optional[str]
            Unique 24-hexadecimal character string that identifies the project.

        hostnames : typing.Optional[typing.Sequence[str]]
            List that contains the hostnames assigned to the stream workspace.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Label that identifies the stream workspace.

        sample_connections : typing.Optional[StreamsSampleConnections]

        stream_config : typing.Optional[StreamConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.create_stream_workspace(
                group_id_="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_stream_workspace(
            group_id_,
            id=id,
            connections=connections,
            data_process_region=data_process_region,
            group_id=group_id,
            hostnames=hostnames,
            links=links,
            name=name,
            sample_connections=sample_connections,
            stream_config=stream_config,
            request_options=request_options,
        )
        return _response.data

    async def get_stream_workspace(
        self,
        group_id: str,
        tenant_name: str,
        *,
        include_connections: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Returns the details of one stream workspace within the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to return.

        include_connections : typing.Optional[bool]
            Flag to indicate whether connections information should be included in the stream workspace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.get_stream_workspace(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_stream_workspace(
            group_id, tenant_name, include_connections=include_connections, request_options=request_options
        )
        return _response.data

    async def delete_stream_workspace(
        self, group_id: str, tenant_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Delete one stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.delete_stream_workspace(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_stream_workspace(
            group_id, tenant_name, request_options=request_options
        )
        return _response.data

    async def update_stream_workspace(
        self,
        group_id: str,
        tenant_name: str,
        *,
        cloud_provider: typing.Optional[str] = OMIT,
        region: typing.Optional[BaseStreamsRegion] = OMIT,
        stream_config: typing.Optional[StreamConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Update one stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to update.

        cloud_provider : typing.Optional[str]
            Human-readable label that identifies the cloud provider.

        region : typing.Optional[BaseStreamsRegion]

        stream_config : typing.Optional[StreamConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.update_stream_workspace(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_stream_workspace(
            group_id,
            tenant_name,
            cloud_provider=cloud_provider,
            region=region,
            stream_config=stream_config,
            request_options=request_options,
        )
        return _response.data

    async def download_audit_logs(
        self,
        group_id: str,
        tenant_name: str,
        *,
        end_date: typing.Optional[int] = None,
        start_date: typing.Optional[int] = None,
        sp_name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[bytes]:
        """
        Downloads the audit logs for the specified Atlas Streams Processing workspace or stream processor. By default, logs cover periods of 30 days. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role. The API does not support direct calls with the json response schema. You must request a gzip response schema using an accept header of the format: `Accept: application/vnd.atlas.YYYY-MM-DD+gzip`.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        end_date : typing.Optional[int]
            Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        start_date : typing.Optional[int]
            Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        sp_name : typing.Optional[str]
            Name of the stream processor to download logs for. An empty string will download logs for all stream processors in the workspace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.AsyncIterator[bytes]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.download_audit_logs()


        asyncio.run(main())
        """
        async with self._raw_client.download_audit_logs(
            group_id,
            tenant_name,
            end_date=end_date,
            start_date=start_date,
            sp_name=sp_name,
            request_options=request_options,
        ) as r:
            async for _chunk in r.data:
                yield _chunk

    async def list_stream_connections(
        self,
        group_id: str,
        tenant_name: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsConnection:
        """
        Returns all connections of the stream workspace for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.list_stream_connections(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_stream_connections(
            group_id, tenant_name, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    async def create_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        *,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        region: typing.Optional[str] = OMIT,
        state: typing.Optional[str] = OMIT,
        type: typing.Optional[str] = OMIT,
        cluster_group_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        db_role_to_execute: typing.Optional[DbRoleToExecute] = OMIT,
        authentication: typing.Optional[StreamsKafkaAuthentication] = OMIT,
        bootstrap_servers: typing.Optional[str] = OMIT,
        config: typing.Optional[typing.Dict[str, str]] = OMIT,
        networking: typing.Optional[StreamsKafkaNetworking] = OMIT,
        security: typing.Optional[StreamsKafkaSecurity] = OMIT,
        headers: typing.Optional[typing.Dict[str, str]] = OMIT,
        url: typing.Optional[str] = OMIT,
        aws: typing.Optional[StreamsAwsConnectionConfig] = OMIT,
        provider: typing.Optional[str] = OMIT,
        schema_registry_authentication: typing.Optional[SchemaRegistryAuthentication] = OMIT,
        schema_registry_urls: typing.Optional[typing.Sequence[str]] = OMIT,
        azure: typing.Optional[AzureConnection] = OMIT,
        public_private_networking: typing.Optional[StreamsPublicPrivateLinkNetworking] = OMIT,
        gcp: typing.Optional[StreamsGcpConnectionConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsConnection:
        """
        Creates one connection for a stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        id : typing.Optional[str]
            Unique identifier of the connection.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Human-readable label that identifies the stream connection. In the case of the Sample type, this is the name of the sample source.

        region : typing.Optional[str]
            The connection's region.

        state : typing.Optional[str]
            The state of the connection.

        type : typing.Optional[str]
            Type of the connection.

        cluster_group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.

        cluster_name : typing.Optional[str]
            Name of the cluster configured for this connection.

        db_role_to_execute : typing.Optional[DbRoleToExecute]

        authentication : typing.Optional[StreamsKafkaAuthentication]

        bootstrap_servers : typing.Optional[str]
            Comma separated list of server addresses.

        config : typing.Optional[typing.Dict[str, str]]
            A map of Kafka key-value pairs for optional configuration. This is a flat object, and keys can have '.' characters.

        networking : typing.Optional[StreamsKafkaNetworking]

        security : typing.Optional[StreamsKafkaSecurity]

        headers : typing.Optional[typing.Dict[str, str]]
            A map of key-value pairs that will be passed as headers for the request.

        url : typing.Optional[str]
            The URL to be used for the request.

        aws : typing.Optional[StreamsAwsConnectionConfig]

        provider : typing.Optional[str]
            The Schema Registry provider.

        schema_registry_authentication : typing.Optional[SchemaRegistryAuthentication]

        schema_registry_urls : typing.Optional[typing.Sequence[str]]
            List of Schema Registry endpoint URLs used by this connection. Each URL must use the http or https scheme and specify a valid host and optional port.

        azure : typing.Optional[AzureConnection]

        public_private_networking : typing.Optional[StreamsPublicPrivateLinkNetworking]

        gcp : typing.Optional[StreamsGcpConnectionConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.create_stream_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_stream_connection(
            group_id,
            tenant_name,
            id=id,
            links=links,
            name=name,
            region=region,
            state=state,
            type=type,
            cluster_group_id=cluster_group_id,
            cluster_name=cluster_name,
            db_role_to_execute=db_role_to_execute,
            authentication=authentication,
            bootstrap_servers=bootstrap_servers,
            config=config,
            networking=networking,
            security=security,
            headers=headers,
            url=url,
            aws=aws,
            provider=provider,
            schema_registry_authentication=schema_registry_authentication,
            schema_registry_urls=schema_registry_urls,
            azure=azure,
            public_private_networking=public_private_networking,
            gcp=gcp,
            request_options=request_options,
        )
        return _response.data

    async def get_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        connection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsConnection:
        """
        Returns the details of one stream connection within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace to return.

        connection_name : str
            Label that identifies the stream connection to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.get_stream_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                connection_name="connectionName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_stream_connection(
            group_id, tenant_name, connection_name, request_options=request_options
        )
        return _response.data

    async def delete_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        connection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Delete one connection of the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        connection_name : str
            Label that identifies the stream connection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.delete_stream_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                connection_name="connectionName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_stream_connection(
            group_id, tenant_name, connection_name, request_options=request_options
        )
        return _response.data

    async def update_stream_connection(
        self,
        group_id: str,
        tenant_name: str,
        connection_name: str,
        *,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        region: typing.Optional[str] = OMIT,
        state: typing.Optional[str] = OMIT,
        type: typing.Optional[str] = OMIT,
        cluster_group_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        db_role_to_execute: typing.Optional[DbRoleToExecute] = OMIT,
        authentication: typing.Optional[StreamsKafkaAuthentication] = OMIT,
        bootstrap_servers: typing.Optional[str] = OMIT,
        config: typing.Optional[typing.Dict[str, str]] = OMIT,
        networking: typing.Optional[StreamsKafkaNetworking] = OMIT,
        security: typing.Optional[StreamsKafkaSecurity] = OMIT,
        headers: typing.Optional[typing.Dict[str, str]] = OMIT,
        url: typing.Optional[str] = OMIT,
        aws: typing.Optional[StreamsAwsConnectionConfig] = OMIT,
        provider: typing.Optional[str] = OMIT,
        schema_registry_authentication: typing.Optional[SchemaRegistryAuthentication] = OMIT,
        schema_registry_urls: typing.Optional[typing.Sequence[str]] = OMIT,
        azure: typing.Optional[AzureConnection] = OMIT,
        public_private_networking: typing.Optional[StreamsPublicPrivateLinkNetworking] = OMIT,
        gcp: typing.Optional[StreamsGcpConnectionConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsConnection:
        """
        Update one connection for the specified stream workspace in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        connection_name : str
            Label that identifies the stream connection.

        id : typing.Optional[str]
            Unique identifier of the connection.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Human-readable label that identifies the stream connection. In the case of the Sample type, this is the name of the sample source.

        region : typing.Optional[str]
            The connection's region.

        state : typing.Optional[str]
            The state of the connection.

        type : typing.Optional[str]
            Type of the connection.

        cluster_group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.

        cluster_name : typing.Optional[str]
            Name of the cluster configured for this connection.

        db_role_to_execute : typing.Optional[DbRoleToExecute]

        authentication : typing.Optional[StreamsKafkaAuthentication]

        bootstrap_servers : typing.Optional[str]
            Comma separated list of server addresses.

        config : typing.Optional[typing.Dict[str, str]]
            A map of Kafka key-value pairs for optional configuration. This is a flat object, and keys can have '.' characters.

        networking : typing.Optional[StreamsKafkaNetworking]

        security : typing.Optional[StreamsKafkaSecurity]

        headers : typing.Optional[typing.Dict[str, str]]
            A map of key-value pairs that will be passed as headers for the request.

        url : typing.Optional[str]
            The URL to be used for the request.

        aws : typing.Optional[StreamsAwsConnectionConfig]

        provider : typing.Optional[str]
            The Schema Registry provider.

        schema_registry_authentication : typing.Optional[SchemaRegistryAuthentication]

        schema_registry_urls : typing.Optional[typing.Sequence[str]]
            List of Schema Registry endpoint URLs used by this connection. Each URL must use the http or https scheme and specify a valid host and optional port.

        azure : typing.Optional[AzureConnection]

        public_private_networking : typing.Optional[StreamsPublicPrivateLinkNetworking]

        gcp : typing.Optional[StreamsGcpConnectionConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.update_stream_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                connection_name="connectionName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_stream_connection(
            group_id,
            tenant_name,
            connection_name,
            id=id,
            links=links,
            name=name,
            region=region,
            state=state,
            type=type,
            cluster_group_id=cluster_group_id,
            cluster_name=cluster_name,
            db_role_to_execute=db_role_to_execute,
            authentication=authentication,
            bootstrap_servers=bootstrap_servers,
            config=config,
            networking=networking,
            security=security,
            headers=headers,
            url=url,
            aws=aws,
            provider=provider,
            schema_registry_authentication=schema_registry_authentication,
            schema_registry_urls=schema_registry_urls,
            azure=azure,
            public_private_networking=public_private_networking,
            gcp=gcp,
            request_options=request_options,
        )
        return _response.data

    async def create_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        *,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        name: typing.Optional[str] = OMIT,
        options: typing.Optional[StreamsOptions] = OMIT,
        pipeline: typing.Optional[typing.Sequence[Document]] = OMIT,
        tier: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsProcessor:
        """
        Create one Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        id : typing.Optional[str]
            Unique 24-hexadecimal character string that identifies the stream processor.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        name : typing.Optional[str]
            Human-readable name of the stream processor.

        options : typing.Optional[StreamsOptions]

        pipeline : typing.Optional[typing.Sequence[Document]]
            Stream aggregation pipeline you want to apply to your streaming data.

        tier : typing.Optional[str]
            Selected tier for the Stream Workspace. Configures Memory / VCPU allowances.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsProcessor
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.create_stream_processor(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_stream_processor(
            group_id,
            tenant_name,
            id=id,
            links=links,
            name=name,
            options=options,
            pipeline=pipeline,
            tier=tier,
            request_options=request_options,
        )
        return _response.data

    async def get_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsProcessorWithStats:
        """
        Get one Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsProcessorWithStats
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.get_stream_processor(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                processor_name="processorName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    async def delete_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Delete a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.delete_stream_processor(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                processor_name="processorName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    async def update_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        name: typing.Optional[str] = OMIT,
        options: typing.Optional[StreamsModifyStreamProcessorOptions] = OMIT,
        pipeline: typing.Optional[typing.Sequence[Document]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsProcessorWithStats:
        """
        Modify one existing Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        name : typing.Optional[str]
            New name for the stream processor.

        options : typing.Optional[StreamsModifyStreamProcessorOptions]

        pipeline : typing.Optional[typing.Sequence[Document]]
            New pipeline for the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsProcessorWithStats
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.update_stream_processor(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                processor_name="processorName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_stream_processor(
            group_id,
            tenant_name,
            processor_name,
            name=name,
            options=options,
            pipeline=pipeline,
            request_options=request_options,
        )
        return _response.data

    async def start_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Start a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.start_stream_processor(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                processor_name="processorName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.start_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    async def start_stream_processor_with(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        resume_from_checkpoint: typing.Optional[bool] = OMIT,
        start_at_operation_time: typing.Optional[dt.datetime] = OMIT,
        tier: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Start a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        resume_from_checkpoint : typing.Optional[bool]
            When true or not specified, the stream processor resumes from its last checkpoint. When false, the stream processor starts fresh.

        start_at_operation_time : typing.Optional[dt.datetime]
            The operation time after which the change stream source should begin reporting. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        tier : typing.Optional[str]
            Selected tier for the Stream Workspace. Configures Memory / VCPU allowances.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.start_stream_processor_with(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                processor_name="processorName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.start_stream_processor_with(
            group_id,
            tenant_name,
            processor_name,
            resume_from_checkpoint=resume_from_checkpoint,
            start_at_operation_time=start_at_operation_time,
            tier=tier,
            request_options=request_options,
        )
        return _response.data

    async def stop_stream_processor(
        self,
        group_id: str,
        tenant_name: str,
        processor_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Stop a Stream Processor within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        processor_name : str
            Label that identifies the stream processor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.stop_stream_processor(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
                processor_name="processorName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.stop_stream_processor(
            group_id, tenant_name, processor_name, request_options=request_options
        )
        return _response.data

    async def get_stream_processors(
        self,
        group_id: str,
        tenant_name: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        include_count: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsStreamProcessorWithStats:
        """
        Returns all Stream Processors within the specified stream workspace. To use this resource, the requesting Service Account or API Key must have the Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsStreamProcessorWithStats
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.get_stream_processors(
                group_id="32b6e34b3d91647abb20e7b8",
                tenant_name="tenantName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_stream_processors(
            group_id,
            tenant_name,
            items_per_page=items_per_page,
            page_num=page_num,
            include_count=include_count,
            request_options=request_options,
        )
        return _response.data

    async def download_operational_logs(
        self,
        group_id: str,
        tenant_name: str,
        *,
        end_date: typing.Optional[int] = None,
        start_date: typing.Optional[int] = None,
        sp_name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[bytes]:
        """
        Downloads the operational logs for the specified Atlas Streams Processing workspace or stream processor. By default, logs cover periods of 30 days. To use this resource, the requesting Service Account or API Key must have the Project Data Access roles, Project Owner role or Project Stream Processing Owner role. The API does not support direct calls with the json response schema. You must request a gzip response schema using an accept header of the format: "Accept: application/vnd.atlas.2025-03-12+gzip".

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        tenant_name : str
            Label that identifies the stream workspace.

        end_date : typing.Optional[int]
            Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        start_date : typing.Optional[int]
            Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch.

        sp_name : typing.Optional[str]
            Name of the stream processor to download logs for. An empty string will download logs for all stream processors in the workspace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.AsyncIterator[bytes]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.download_operational_logs()


        asyncio.run(main())
        """
        async with self._raw_client.download_operational_logs(
            group_id,
            tenant_name,
            end_date=end_date,
            start_date=start_date,
            sp_name=sp_name,
            request_options=request_options,
        ) as r:
            async for _chunk in r.data:
                yield _chunk

    async def get_account_details(
        self,
        group_id: str,
        *,
        cloud_provider: str,
        region_name: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccountDetails:
        """
        Returns the Account ID, and the VPC ID for the group and region specified.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            One of "aws", "azure" or "gcp".

        region_name : str
            The cloud provider specific region name, i.e. "US_EAST_1" for cloud provider "aws".

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccountDetails
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.get_account_details(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
                region_name="regionName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_account_details(
            group_id, cloud_provider=cloud_provider, region_name=region_name, request_options=request_options
        )
        return _response.data

    async def list_active_peering_connections(
        self,
        group_id: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsVpcPeeringConnection:
        """
        Returns a list of active incoming VPC Peering Connections.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsVpcPeeringConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.list_active_peering_connections(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_active_peering_connections(
            group_id, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    async def list_private_link_connections(
        self,
        group_id: str,
        *,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsPrivateLink:
        """
        Returns all Private Link connections for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role, Project Data Access roles, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsPrivateLink
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.list_private_link_connections(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_private_link_connections(
            group_id, items_per_page=items_per_page, page_num=page_num, request_options=request_options
        )
        return _response.data

    async def create_private_link_connection(
        self,
        group_id: str,
        *,
        provider: str,
        id: typing.Optional[str] = OMIT,
        arn: typing.Optional[str] = OMIT,
        azure_resource_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        dns_domain: typing.Optional[str] = OMIT,
        dns_sub_domain: typing.Optional[typing.Sequence[str]] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        gcp_connection_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        gcp_service_attachment_uris: typing.Optional[typing.Sequence[str]] = OMIT,
        interface_endpoint_id: typing.Optional[str] = OMIT,
        interface_endpoint_name: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        provider_account_id: typing.Optional[str] = OMIT,
        region: typing.Optional[str] = OMIT,
        service_endpoint_id: typing.Optional[str] = OMIT,
        state: typing.Optional[str] = OMIT,
        vendor: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsPrivateLinkConnection:
        """
        Creates one Private Link in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        provider : str
            Provider where the Kafka cluster is deployed. Valid values are AWS and AZURE.

        id : typing.Optional[str]
            The ID of the Private Link connection.

        arn : typing.Optional[str]
            Amazon Resource Name (ARN). Required for AWS Provider and MSK vendor.

        azure_resource_ids : typing.Optional[typing.Sequence[str]]
            Azure Resource IDs of each availability zone for the Azure Confluent cluster.

        dns_domain : typing.Optional[str]
            The domain hostname. Required for the following provider and vendor combinations:
            - AWS provider with CONFLUENT vendor.
            - AZURE provider with EVENTHUB or CONFLUENT vendor.

        dns_sub_domain : typing.Optional[typing.Sequence[str]]
            Sub-Domain name of Confluent cluster. These are typically your availability zones. Required for AWS Provider and CONFLUENT vendor, if your AWS CONFLUENT cluster doesn't use subdomains, you must set this to the empty array [].

        error_message : typing.Optional[str]
            Error message if the state is FAILED.

        gcp_connection_ids : typing.Optional[typing.Sequence[str]]
            List of GCP Private Service Connect connection IDs.

        gcp_service_attachment_uris : typing.Optional[typing.Sequence[str]]
            Service Attachment URIs of each availability zone for the GCP Confluent cluster.

        interface_endpoint_id : typing.Optional[str]
            Interface endpoint ID that is created from the service endpoint ID provided.

        interface_endpoint_name : typing.Optional[str]
            Interface endpoint name that is created from the service endpoint ID provided.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        provider_account_id : typing.Optional[str]
            Account ID from the cloud provider.

        region : typing.Optional[str]
            The region of the Provider’s cluster. See [AZURE](https://www.mongodb.com/docs/atlas/reference/microsoft-azure/#stream-processing-instances) and [AWS](https://www.mongodb.com/docs/atlas/reference/amazon-aws/#stream-processing-instances) supported regions.

        service_endpoint_id : typing.Optional[str]
            For AZURE EVENTHUB, this is the [namespace endpoint ID](https://learn.microsoft.com/en-us/rest/api/eventhub/namespaces/get). For AWS CONFLUENT cluster, this is the [VPC Endpoint service name](https://docs.confluent.io/cloud/current/networking/private-links/aws-privatelink.html).

        state : typing.Optional[str]
            State the connection is in.

        vendor : typing.Optional[str]
            Vendor that manages the cloud service. The list of supported vendor values is:
            - AWS
            -- `MSK` for AWS MSK Kafka clusters
            -- `CONFLUENT` for Confluent Kafka clusters on AWS
            -- `KINESIS` for AWS Kinesis Data Streams

            - Azure
            -- `EVENTHUB` for Azure EventHub.
            -- `CONFLUENT` for Confluent Kafka clusters on Azure
            -- `AZURE_BLOB_STORAGE` for Azure Blob Storage

            - GCP
            -- `CONFLUENT` for Confluent Kafka clusters on GCP
            -- `PUBSUB` for Google Cloud Pub/Sub

            **NOTE** Omitting the vendor field will default to using the GENERIC vendor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsPrivateLinkConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.create_private_link_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                provider="provider",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_private_link_connection(
            group_id,
            provider=provider,
            id=id,
            arn=arn,
            azure_resource_ids=azure_resource_ids,
            dns_domain=dns_domain,
            dns_sub_domain=dns_sub_domain,
            error_message=error_message,
            gcp_connection_ids=gcp_connection_ids,
            gcp_service_attachment_uris=gcp_service_attachment_uris,
            interface_endpoint_id=interface_endpoint_id,
            interface_endpoint_name=interface_endpoint_name,
            links=links,
            provider_account_id=provider_account_id,
            region=region,
            service_endpoint_id=service_endpoint_id,
            state=state,
            vendor=vendor,
            request_options=request_options,
        )
        return _response.data

    async def get_private_link_connection(
        self, group_id: str, connection_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> StreamsPrivateLinkConnection:
        """
        Returns the details of one Private Link connection within the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        connection_id : str
            Unique ID that identifies the Private Link connection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsPrivateLinkConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.get_private_link_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                connection_id="connectionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_private_link_connection(
            group_id, connection_id, request_options=request_options
        )
        return _response.data

    async def delete_private_link_connection(
        self, group_id: str, connection_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Deletes one Private Link in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        connection_id : str
            Unique ID that identifies the Private Link connection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.delete_private_link_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                connection_id="connectionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_private_link_connection(
            group_id, connection_id, request_options=request_options
        )
        return _response.data

    async def list_vpc_peering_connections(
        self,
        group_id: str,
        *,
        requester_account_id: str,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiStreamsVpcPeeringConnection:
        """
        Returns a list of incoming VPC Peering Connections.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        requester_account_id : str
            The Account ID of the VPC Peering connection/s.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiStreamsVpcPeeringConnection
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.list_vpc_peering_connections(
                group_id="32b6e34b3d91647abb20e7b8",
                requester_account_id="requesterAccountId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_vpc_peering_connections(
            group_id,
            requester_account_id=requester_account_id,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    async def delete_vpc_peering_connection(
        self, group_id: str, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Deletes an incoming VPC Peering connection.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : str
            The VPC Peering Connection id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.delete_vpc_peering_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_vpc_peering_connection(group_id, id, request_options=request_options)
        return _response.data

    async def accept_vpc_peering_connection(
        self,
        group_id: str,
        id: str,
        *,
        requester_account_id: typing.Optional[str] = OMIT,
        requester_vpc_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Requests the acceptance of an incoming VPC Peering connection.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : str
            The VPC Peering Connection id.

        requester_account_id : typing.Optional[str]
            The AWS requester account ID.

        requester_vpc_id : typing.Optional[str]
            The AWS requester VPC ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.accept_vpc_peering_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.accept_vpc_peering_connection(
            group_id,
            id,
            requester_account_id=requester_account_id,
            requester_vpc_id=requester_vpc_id,
            request_options=request_options,
        )
        return _response.data

    async def reject_vpc_peering_connection(
        self, group_id: str, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Requests the rejection of an incoming VPC Peering connection.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        id : str
            The VPC Peering Connection id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.reject_vpc_peering_connection(
                group_id="32b6e34b3d91647abb20e7b8",
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.reject_vpc_peering_connection(group_id, id, request_options=request_options)
        return _response.data

    async def with_stream_sample_connections(
        self,
        group_id: str,
        *,
        request: TenantWithSampleConnections,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamsTenant:
        """
        Creates one stream workspace in the specified project with sample connections. To use this resource the requesting Service Account or API Key must have the Project Data Access Admin role, Project Owner role or Project Stream Processing Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request : TenantWithSampleConnections

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamsTenant
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.streams.with_stream_sample_connections(
                group_id="32b6e34b3d91647abb20e7b8",
                request={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.with_stream_sample_connections(
            group_id, request=request, request_options=request_options
        )
        return _response.data
