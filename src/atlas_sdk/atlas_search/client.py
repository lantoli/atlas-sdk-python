# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.api_atlas_fts_analyzers import ApiAtlasFtsAnalyzers
from ..types.api_atlas_fts_mappings import ApiAtlasFtsMappings
from ..types.api_search_deployment_response import ApiSearchDeploymentResponse
from ..types.api_search_deployment_spec import ApiSearchDeploymentSpec
from ..types.base_search_index_create_request_definition import BaseSearchIndexCreateRequestDefinition
from ..types.basic_db_object import BasicDbObject
from ..types.cluster_search_index import ClusterSearchIndex
from ..types.search_index_response import SearchIndexResponse
from ..types.search_index_update_request_definition import SearchIndexUpdateRequestDefinition
from ..types.search_synonym_mapping_definition import SearchSynonymMappingDefinition
from .raw_client import AsyncRawAtlasSearchClient, RawAtlasSearchClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AtlasSearchClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAtlasSearchClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAtlasSearchClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAtlasSearchClient
        """
        return self._raw_client

    def create_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection_name: str,
        database: str,
        name: str,
        index_id: typing.Optional[str] = OMIT,
        num_partitions: typing.Optional[int] = OMIT,
        status: typing.Optional[str] = OMIT,
        stored_source: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        type: typing.Optional[str] = OMIT,
        analyzer: typing.Optional[str] = OMIT,
        analyzers: typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]] = OMIT,
        mappings: typing.Optional[ApiAtlasFtsMappings] = OMIT,
        search_analyzer: typing.Optional[str] = OMIT,
        synonyms: typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]] = OMIT,
        fields: typing.Optional[typing.Sequence[BasicDbObject]] = OMIT,
        nested_root: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClusterSearchIndex:
        """
        Creates one Atlas Search index on the specified collection. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. Only clusters running MongoDB v4.2 or later can use Atlas Search. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection on which to create an Atlas Search index.

        collection_name : str
            Human-readable label that identifies the collection that contains one or more Atlas Search indexes.

        database : str
            Human-readable label that identifies the database that contains the collection with one or more Atlas Search indexes.

        name : str
            Human-readable label that identifies this index. Within each namespace, names of all indexes in the namespace must be unique.

        index_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies this Atlas Search index.

        num_partitions : typing.Optional[int]
            Number of index partitions. Allowed values are [1, 2, 4].

        status : typing.Optional[str]
            Condition of the search index when you made this request.

            - `IN_PROGRESS`: Atlas is building or re-building the index after an edit.
            - `STEADY`: You can use this search index.
            - `FAILED`: Atlas could not build the index.
            - `MIGRATING`: Atlas is upgrading the underlying cluster tier and migrating indexes.
            - `PAUSED`: The cluster is paused.

        stored_source : typing.Optional[typing.Dict[str, typing.Any]]
            Flag that indicates whether to store all fields (true) on Atlas Search. By default, Atlas doesn't store (false) the fields on Atlas Search.  Alternatively, you can specify an object that only contains the list of fields to store (include) or not store (exclude) on Atlas Search. Note that storing all fields (true) is not allowed for vector search indexes. To learn more, see documentation.

        type : typing.Optional[str]
            Type of the index. Default type is search.

        analyzer : typing.Optional[str]
            Specific pre-defined method chosen to convert database field text into searchable words. This conversion reduces the text of fields into the smallest units of text. These units are called a **term** or **token**. This process, known as tokenization, involves a variety of changes made to the text in fields:

            - extracting words
            - removing punctuation
            - removing accents
            - changing to lowercase
            - removing common words
            - reducing words to their root form (stemming)
            - changing words to their base form (lemmatization)
             MongoDB Cloud uses the selected process to build the Atlas Search index.

        analyzers : typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]]
            List of user-defined methods to convert database field text into searchable words.

        mappings : typing.Optional[ApiAtlasFtsMappings]

        search_analyzer : typing.Optional[str]
            Method applied to identify words when searching this index.

        synonyms : typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]]
            Rule sets that map words to their synonyms in this index.

        fields : typing.Optional[typing.Sequence[BasicDbObject]]
            Settings that configure the fields, one per object, to index. You must define at least one "vector" type field. You can optionally define "filter" type fields also.

        nested_root : typing.Optional[str]
            Top-level path to the array that contains vector fields. When provided, vector fields under this path are treated as nested.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClusterSearchIndex
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.create_cluster_fts_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            collection_name="collectionName",
            database="database",
            name="name",
        )
        """
        _response = self._raw_client.create_cluster_fts_index(
            group_id,
            cluster_name,
            collection_name=collection_name,
            database=database,
            name=name,
            index_id=index_id,
            num_partitions=num_partitions,
            status=status,
            stored_source=stored_source,
            type=type,
            analyzer=analyzer,
            analyzers=analyzers,
            mappings=mappings,
            search_analyzer=search_analyzer,
            synonyms=synonyms,
            fields=fields,
            nested_root=nested_root,
            request_options=request_options,
        )
        return _response.data

    def list_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ClusterSearchIndex]:
        """
        Returns all Atlas Search indexes on the specified collection. Atlas Search indexes contain the indexed fields and the analyzers used to create the indexes. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        database_name : str
            Human-readable label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ClusterSearchIndex]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.list_cluster_fts_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            database_name="databaseName",
            collection_name="collectionName",
        )
        """
        _response = self._raw_client.list_cluster_fts_index(
            group_id, cluster_name, database_name, collection_name, request_options=request_options
        )
        return _response.data

    def get_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClusterSearchIndex:
        """
        Returns one Atlas Search index in the specified project. You identify this index using its unique ID. Atlas Search index contains the indexed fields and the analyzers used to create the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Application Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Application Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Application Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClusterSearchIndex
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.get_cluster_fts_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            index_id="indexId",
        )
        """
        _response = self._raw_client.get_cluster_fts_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    def delete_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one Atlas Search index that you identified with its unique ID. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the database and collection with one or more Application Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search index. Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.delete_cluster_fts_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            index_id="indexId",
        )
        """
        _response = self._raw_client.delete_cluster_fts_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    def update_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id_: str,
        *,
        collection_name: str,
        database: str,
        name: str,
        index_id: typing.Optional[str] = OMIT,
        num_partitions: typing.Optional[int] = OMIT,
        status: typing.Optional[str] = OMIT,
        stored_source: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        type: typing.Optional[str] = OMIT,
        analyzer: typing.Optional[str] = OMIT,
        analyzers: typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]] = OMIT,
        mappings: typing.Optional[ApiAtlasFtsMappings] = OMIT,
        search_analyzer: typing.Optional[str] = OMIT,
        synonyms: typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]] = OMIT,
        fields: typing.Optional[typing.Sequence[BasicDbObject]] = OMIT,
        nested_root: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClusterSearchIndex:
        """
        Updates one Atlas Search index that you identified with its unique ID. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection whose Atlas Search index to update.

        index_id_ : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        collection_name : str
            Human-readable label that identifies the collection that contains one or more Atlas Search indexes.

        database : str
            Human-readable label that identifies the database that contains the collection with one or more Atlas Search indexes.

        name : str
            Human-readable label that identifies this index. Within each namespace, names of all indexes in the namespace must be unique.

        index_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies this Atlas Search index.

        num_partitions : typing.Optional[int]
            Number of index partitions. Allowed values are [1, 2, 4].

        status : typing.Optional[str]
            Condition of the search index when you made this request.

            - `IN_PROGRESS`: Atlas is building or re-building the index after an edit.
            - `STEADY`: You can use this search index.
            - `FAILED`: Atlas could not build the index.
            - `MIGRATING`: Atlas is upgrading the underlying cluster tier and migrating indexes.
            - `PAUSED`: The cluster is paused.

        stored_source : typing.Optional[typing.Dict[str, typing.Any]]
            Flag that indicates whether to store all fields (true) on Atlas Search. By default, Atlas doesn't store (false) the fields on Atlas Search.  Alternatively, you can specify an object that only contains the list of fields to store (include) or not store (exclude) on Atlas Search. Note that storing all fields (true) is not allowed for vector search indexes. To learn more, see documentation.

        type : typing.Optional[str]
            Type of the index. Default type is search.

        analyzer : typing.Optional[str]
            Specific pre-defined method chosen to convert database field text into searchable words. This conversion reduces the text of fields into the smallest units of text. These units are called a **term** or **token**. This process, known as tokenization, involves a variety of changes made to the text in fields:

            - extracting words
            - removing punctuation
            - removing accents
            - changing to lowercase
            - removing common words
            - reducing words to their root form (stemming)
            - changing words to their base form (lemmatization)
             MongoDB Cloud uses the selected process to build the Atlas Search index.

        analyzers : typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]]
            List of user-defined methods to convert database field text into searchable words.

        mappings : typing.Optional[ApiAtlasFtsMappings]

        search_analyzer : typing.Optional[str]
            Method applied to identify words when searching this index.

        synonyms : typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]]
            Rule sets that map words to their synonyms in this index.

        fields : typing.Optional[typing.Sequence[BasicDbObject]]
            Settings that configure the fields, one per object, to index. You must define at least one "vector" type field. You can optionally define "filter" type fields also.

        nested_root : typing.Optional[str]
            Top-level path to the array that contains vector fields. When provided, vector fields under this path are treated as nested.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClusterSearchIndex
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.update_cluster_fts_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            index_id_="indexId",
            collection_name="collectionName",
            database="database",
            name="name",
        )
        """
        _response = self._raw_client.update_cluster_fts_index(
            group_id,
            cluster_name,
            index_id_,
            collection_name=collection_name,
            database=database,
            name=name,
            index_id=index_id,
            num_partitions=num_partitions,
            status=status,
            stored_source=stored_source,
            type=type,
            analyzer=analyzer,
            analyzers=analyzers,
            mappings=mappings,
            search_analyzer=search_analyzer,
            synonyms=synonyms,
            fields=fields,
            nested_root=nested_root,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_search_deployment(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiSearchDeploymentResponse:
        """
        Returns the Search Nodes for the specified cluster. Deprecated versions: v2-{2024-05-30}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to return the Search Nodes for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiSearchDeploymentResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.get_cluster_search_deployment(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.get_cluster_search_deployment(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    def create_cluster_search_deployment(
        self,
        group_id: str,
        cluster_name: str,
        *,
        specs: typing.Sequence[ApiSearchDeploymentSpec],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiSearchDeploymentResponse:
        """
        Creates Search Nodes for the specified cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to create Search Nodes for.

        specs : typing.Sequence[ApiSearchDeploymentSpec]
            List of settings that configure the Search Nodes for your cluster.

            **NOTE**: We accept a single configuration for all nodes currently.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiSearchDeploymentResponse
            Created

        Examples
        --------
        from atlas_sdk import ApiSearchDeploymentSpec, AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.create_cluster_search_deployment(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            specs=[
                ApiSearchDeploymentSpec(
                    instance_size="instanceSize",
                    node_count=2,
                )
            ],
        )
        """
        _response = self._raw_client.create_cluster_search_deployment(
            group_id, cluster_name, specs=specs, request_options=request_options
        )
        return _response.data

    def delete_cluster_search_deployment(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Deletes the Search Nodes for the specified cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.delete_cluster_search_deployment(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.delete_cluster_search_deployment(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    def update_cluster_search_deployment(
        self,
        group_id: str,
        cluster_name: str,
        *,
        specs: typing.Sequence[ApiSearchDeploymentSpec],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiSearchDeploymentResponse:
        """
        Updates the Search Nodes for the specified cluster. Deprecated versions: v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to update the Search Nodes for.

        specs : typing.Sequence[ApiSearchDeploymentSpec]
            List of settings that configure the Search Nodes for your cluster.

            **NOTE**: We accept a single configuration for all nodes currently.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiSearchDeploymentResponse
            OK

        Examples
        --------
        from atlas_sdk import ApiSearchDeploymentSpec, AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.update_cluster_search_deployment(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            specs=[
                ApiSearchDeploymentSpec(
                    instance_size="instanceSize",
                    node_count=2,
                )
            ],
        )
        """
        _response = self._raw_client.update_cluster_search_deployment(
            group_id, cluster_name, specs=specs, request_options=request_options
        )
        return _response.data

    def list_cluster_search_indexes(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[SearchIndexResponse]:
        """
        Returns all Atlas Search indexes on the specified cluster. Atlas Search indexes contain the indexed fields and the analyzers used to create the indexes. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[SearchIndexResponse]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.list_cluster_search_indexes(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_cluster_search_indexes(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    def create_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection_name: str,
        database: str,
        name: str,
        type: typing.Optional[str] = OMIT,
        definition: typing.Optional[BaseSearchIndexCreateRequestDefinition] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Creates one Atlas Search index on the specified collection. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. Only clusters running MongoDB v4.2 or later can use Atlas Search. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection on which to create an Atlas Search index.

        collection_name : str
            Label that identifies the collection to create an Atlas Search index in.

        database : str
            Label that identifies the database that contains the collection to create an Atlas Search index in.

        name : str
            Label that identifies this index. Within each namespace, names of all indexes in the namespace must be unique.

        type : typing.Optional[str]
            Type of the index. The default type is search.

        definition : typing.Optional[BaseSearchIndexCreateRequestDefinition]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            Created

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.create_cluster_search_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            collection_name="collectionName",
            database="database",
            name="name",
        )
        """
        _response = self._raw_client.create_cluster_search_index(
            group_id,
            cluster_name,
            collection_name=collection_name,
            database=database,
            name=name,
            type=type,
            definition=definition,
            request_options=request_options,
        )
        return _response.data

    def list_search_index(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[SearchIndexResponse]:
        """
        Returns all Atlas Search indexes on the specified collection. Atlas Search indexes contain the indexed fields and the analyzers used to create the indexes. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[SearchIndexResponse]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.list_search_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            database_name="databaseName",
            collection_name="collectionName",
        )
        """
        _response = self._raw_client.list_search_index(
            group_id, cluster_name, database_name, collection_name, request_options=request_options
        )
        return _response.data

    def get_index_by_name(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        index_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Returns one Atlas Search index in the specified project. You identify this index using its database, collection and name. Atlas Search index contains the indexed fields and the analyzers used to create the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        index_name : str
            Name of the Atlas Search index to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.get_index_by_name(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            database_name="databaseName",
            collection_name="collectionName",
            index_name="indexName",
        )
        """
        _response = self._raw_client.get_index_by_name(
            group_id, cluster_name, database_name, collection_name, index_name, request_options=request_options
        )
        return _response.data

    def delete_index_by_name(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        index_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one Atlas Search index that you identified with its database, collection, and name. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. This deletion is eventually consistent.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the database and collection with one or more Application Search indexes.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        index_name : str
            Name of the Atlas Search index to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.delete_index_by_name(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            database_name="databaseName",
            collection_name="collectionName",
            index_name="indexName",
        )
        """
        _response = self._raw_client.delete_index_by_name(
            group_id, cluster_name, database_name, collection_name, index_name, request_options=request_options
        )
        return _response.data

    def update_index_by_name(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        index_name: str,
        *,
        definition: SearchIndexUpdateRequestDefinition,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Updates one Atlas Search index that you identified with its database, collection name, and index name. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection whose Atlas Search index you want to update.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        index_name : str
            Name of the Atlas Search index to update.

        definition : SearchIndexUpdateRequestDefinition
            The index definition to update the search index to.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient, SearchIndexUpdateRequestDefinition

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.update_index_by_name(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            database_name="databaseName",
            collection_name="collectionName",
            index_name="indexName",
            definition=SearchIndexUpdateRequestDefinition(),
        )
        """
        _response = self._raw_client.update_index_by_name(
            group_id,
            cluster_name,
            database_name,
            collection_name,
            index_name,
            definition=definition,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Returns one Atlas Search index in the specified project. You identify this index using its unique ID. Atlas Search index contains the indexed fields and the analyzers used to create the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Application Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Application Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Application Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.get_cluster_search_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            index_id="indexId",
        )
        """
        _response = self._raw_client.get_cluster_search_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    def delete_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one Atlas Search index that you identified with its unique ID. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. This deletion is eventually consistent.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the database and collection with one or more Application Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search index. Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.delete_cluster_search_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            index_id="indexId",
        )
        """
        _response = self._raw_client.delete_cluster_search_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    def update_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        definition: SearchIndexUpdateRequestDefinition,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Updates one Atlas Search index that you identified with its unique ID. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection whose Atlas Search index you want to update.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        definition : SearchIndexUpdateRequestDefinition
            The index definition to update the search index to.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient, SearchIndexUpdateRequestDefinition

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.atlas_search.update_cluster_search_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            index_id="indexId",
            definition=SearchIndexUpdateRequestDefinition(),
        )
        """
        _response = self._raw_client.update_cluster_search_index(
            group_id, cluster_name, index_id, definition=definition, request_options=request_options
        )
        return _response.data


class AsyncAtlasSearchClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAtlasSearchClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAtlasSearchClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAtlasSearchClient
        """
        return self._raw_client

    async def create_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection_name: str,
        database: str,
        name: str,
        index_id: typing.Optional[str] = OMIT,
        num_partitions: typing.Optional[int] = OMIT,
        status: typing.Optional[str] = OMIT,
        stored_source: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        type: typing.Optional[str] = OMIT,
        analyzer: typing.Optional[str] = OMIT,
        analyzers: typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]] = OMIT,
        mappings: typing.Optional[ApiAtlasFtsMappings] = OMIT,
        search_analyzer: typing.Optional[str] = OMIT,
        synonyms: typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]] = OMIT,
        fields: typing.Optional[typing.Sequence[BasicDbObject]] = OMIT,
        nested_root: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClusterSearchIndex:
        """
        Creates one Atlas Search index on the specified collection. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. Only clusters running MongoDB v4.2 or later can use Atlas Search. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection on which to create an Atlas Search index.

        collection_name : str
            Human-readable label that identifies the collection that contains one or more Atlas Search indexes.

        database : str
            Human-readable label that identifies the database that contains the collection with one or more Atlas Search indexes.

        name : str
            Human-readable label that identifies this index. Within each namespace, names of all indexes in the namespace must be unique.

        index_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies this Atlas Search index.

        num_partitions : typing.Optional[int]
            Number of index partitions. Allowed values are [1, 2, 4].

        status : typing.Optional[str]
            Condition of the search index when you made this request.

            - `IN_PROGRESS`: Atlas is building or re-building the index after an edit.
            - `STEADY`: You can use this search index.
            - `FAILED`: Atlas could not build the index.
            - `MIGRATING`: Atlas is upgrading the underlying cluster tier and migrating indexes.
            - `PAUSED`: The cluster is paused.

        stored_source : typing.Optional[typing.Dict[str, typing.Any]]
            Flag that indicates whether to store all fields (true) on Atlas Search. By default, Atlas doesn't store (false) the fields on Atlas Search.  Alternatively, you can specify an object that only contains the list of fields to store (include) or not store (exclude) on Atlas Search. Note that storing all fields (true) is not allowed for vector search indexes. To learn more, see documentation.

        type : typing.Optional[str]
            Type of the index. Default type is search.

        analyzer : typing.Optional[str]
            Specific pre-defined method chosen to convert database field text into searchable words. This conversion reduces the text of fields into the smallest units of text. These units are called a **term** or **token**. This process, known as tokenization, involves a variety of changes made to the text in fields:

            - extracting words
            - removing punctuation
            - removing accents
            - changing to lowercase
            - removing common words
            - reducing words to their root form (stemming)
            - changing words to their base form (lemmatization)
             MongoDB Cloud uses the selected process to build the Atlas Search index.

        analyzers : typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]]
            List of user-defined methods to convert database field text into searchable words.

        mappings : typing.Optional[ApiAtlasFtsMappings]

        search_analyzer : typing.Optional[str]
            Method applied to identify words when searching this index.

        synonyms : typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]]
            Rule sets that map words to their synonyms in this index.

        fields : typing.Optional[typing.Sequence[BasicDbObject]]
            Settings that configure the fields, one per object, to index. You must define at least one "vector" type field. You can optionally define "filter" type fields also.

        nested_root : typing.Optional[str]
            Top-level path to the array that contains vector fields. When provided, vector fields under this path are treated as nested.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClusterSearchIndex
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.create_cluster_fts_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                collection_name="collectionName",
                database="database",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_cluster_fts_index(
            group_id,
            cluster_name,
            collection_name=collection_name,
            database=database,
            name=name,
            index_id=index_id,
            num_partitions=num_partitions,
            status=status,
            stored_source=stored_source,
            type=type,
            analyzer=analyzer,
            analyzers=analyzers,
            mappings=mappings,
            search_analyzer=search_analyzer,
            synonyms=synonyms,
            fields=fields,
            nested_root=nested_root,
            request_options=request_options,
        )
        return _response.data

    async def list_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ClusterSearchIndex]:
        """
        Returns all Atlas Search indexes on the specified collection. Atlas Search indexes contain the indexed fields and the analyzers used to create the indexes. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        database_name : str
            Human-readable label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ClusterSearchIndex]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.list_cluster_fts_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                database_name="databaseName",
                collection_name="collectionName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_cluster_fts_index(
            group_id, cluster_name, database_name, collection_name, request_options=request_options
        )
        return _response.data

    async def get_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClusterSearchIndex:
        """
        Returns one Atlas Search index in the specified project. You identify this index using its unique ID. Atlas Search index contains the indexed fields and the analyzers used to create the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Application Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Application Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Application Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClusterSearchIndex
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.get_cluster_fts_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                index_id="indexId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_fts_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    async def delete_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one Atlas Search index that you identified with its unique ID. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the database and collection with one or more Application Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search index. Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.delete_cluster_fts_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                index_id="indexId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_cluster_fts_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    async def update_cluster_fts_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id_: str,
        *,
        collection_name: str,
        database: str,
        name: str,
        index_id: typing.Optional[str] = OMIT,
        num_partitions: typing.Optional[int] = OMIT,
        status: typing.Optional[str] = OMIT,
        stored_source: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        type: typing.Optional[str] = OMIT,
        analyzer: typing.Optional[str] = OMIT,
        analyzers: typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]] = OMIT,
        mappings: typing.Optional[ApiAtlasFtsMappings] = OMIT,
        search_analyzer: typing.Optional[str] = OMIT,
        synonyms: typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]] = OMIT,
        fields: typing.Optional[typing.Sequence[BasicDbObject]] = OMIT,
        nested_root: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClusterSearchIndex:
        """
        Updates one Atlas Search index that you identified with its unique ID. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection whose Atlas Search index to update.

        index_id_ : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        collection_name : str
            Human-readable label that identifies the collection that contains one or more Atlas Search indexes.

        database : str
            Human-readable label that identifies the database that contains the collection with one or more Atlas Search indexes.

        name : str
            Human-readable label that identifies this index. Within each namespace, names of all indexes in the namespace must be unique.

        index_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies this Atlas Search index.

        num_partitions : typing.Optional[int]
            Number of index partitions. Allowed values are [1, 2, 4].

        status : typing.Optional[str]
            Condition of the search index when you made this request.

            - `IN_PROGRESS`: Atlas is building or re-building the index after an edit.
            - `STEADY`: You can use this search index.
            - `FAILED`: Atlas could not build the index.
            - `MIGRATING`: Atlas is upgrading the underlying cluster tier and migrating indexes.
            - `PAUSED`: The cluster is paused.

        stored_source : typing.Optional[typing.Dict[str, typing.Any]]
            Flag that indicates whether to store all fields (true) on Atlas Search. By default, Atlas doesn't store (false) the fields on Atlas Search.  Alternatively, you can specify an object that only contains the list of fields to store (include) or not store (exclude) on Atlas Search. Note that storing all fields (true) is not allowed for vector search indexes. To learn more, see documentation.

        type : typing.Optional[str]
            Type of the index. Default type is search.

        analyzer : typing.Optional[str]
            Specific pre-defined method chosen to convert database field text into searchable words. This conversion reduces the text of fields into the smallest units of text. These units are called a **term** or **token**. This process, known as tokenization, involves a variety of changes made to the text in fields:

            - extracting words
            - removing punctuation
            - removing accents
            - changing to lowercase
            - removing common words
            - reducing words to their root form (stemming)
            - changing words to their base form (lemmatization)
             MongoDB Cloud uses the selected process to build the Atlas Search index.

        analyzers : typing.Optional[typing.Sequence[ApiAtlasFtsAnalyzers]]
            List of user-defined methods to convert database field text into searchable words.

        mappings : typing.Optional[ApiAtlasFtsMappings]

        search_analyzer : typing.Optional[str]
            Method applied to identify words when searching this index.

        synonyms : typing.Optional[typing.Sequence[SearchSynonymMappingDefinition]]
            Rule sets that map words to their synonyms in this index.

        fields : typing.Optional[typing.Sequence[BasicDbObject]]
            Settings that configure the fields, one per object, to index. You must define at least one "vector" type field. You can optionally define "filter" type fields also.

        nested_root : typing.Optional[str]
            Top-level path to the array that contains vector fields. When provided, vector fields under this path are treated as nested.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClusterSearchIndex
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.update_cluster_fts_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                index_id_="indexId",
                collection_name="collectionName",
                database="database",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_cluster_fts_index(
            group_id,
            cluster_name,
            index_id_,
            collection_name=collection_name,
            database=database,
            name=name,
            index_id=index_id,
            num_partitions=num_partitions,
            status=status,
            stored_source=stored_source,
            type=type,
            analyzer=analyzer,
            analyzers=analyzers,
            mappings=mappings,
            search_analyzer=search_analyzer,
            synonyms=synonyms,
            fields=fields,
            nested_root=nested_root,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_search_deployment(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiSearchDeploymentResponse:
        """
        Returns the Search Nodes for the specified cluster. Deprecated versions: v2-{2024-05-30}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to return the Search Nodes for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiSearchDeploymentResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.get_cluster_search_deployment(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_search_deployment(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def create_cluster_search_deployment(
        self,
        group_id: str,
        cluster_name: str,
        *,
        specs: typing.Sequence[ApiSearchDeploymentSpec],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiSearchDeploymentResponse:
        """
        Creates Search Nodes for the specified cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to create Search Nodes for.

        specs : typing.Sequence[ApiSearchDeploymentSpec]
            List of settings that configure the Search Nodes for your cluster.

            **NOTE**: We accept a single configuration for all nodes currently.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiSearchDeploymentResponse
            Created

        Examples
        --------
        import asyncio

        from atlas_sdk import ApiSearchDeploymentSpec, AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.create_cluster_search_deployment(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                specs=[
                    ApiSearchDeploymentSpec(
                        instance_size="instanceSize",
                        node_count=2,
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_cluster_search_deployment(
            group_id, cluster_name, specs=specs, request_options=request_options
        )
        return _response.data

    async def delete_cluster_search_deployment(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Deletes the Search Nodes for the specified cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.delete_cluster_search_deployment(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_cluster_search_deployment(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def update_cluster_search_deployment(
        self,
        group_id: str,
        cluster_name: str,
        *,
        specs: typing.Sequence[ApiSearchDeploymentSpec],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiSearchDeploymentResponse:
        """
        Updates the Search Nodes for the specified cluster. Deprecated versions: v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Label that identifies the cluster to update the Search Nodes for.

        specs : typing.Sequence[ApiSearchDeploymentSpec]
            List of settings that configure the Search Nodes for your cluster.

            **NOTE**: We accept a single configuration for all nodes currently.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiSearchDeploymentResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import ApiSearchDeploymentSpec, AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.update_cluster_search_deployment(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                specs=[
                    ApiSearchDeploymentSpec(
                        instance_size="instanceSize",
                        node_count=2,
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_cluster_search_deployment(
            group_id, cluster_name, specs=specs, request_options=request_options
        )
        return _response.data

    async def list_cluster_search_indexes(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[SearchIndexResponse]:
        """
        Returns all Atlas Search indexes on the specified cluster. Atlas Search indexes contain the indexed fields and the analyzers used to create the indexes. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[SearchIndexResponse]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.list_cluster_search_indexes(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_cluster_search_indexes(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def create_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection_name: str,
        database: str,
        name: str,
        type: typing.Optional[str] = OMIT,
        definition: typing.Optional[BaseSearchIndexCreateRequestDefinition] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Creates one Atlas Search index on the specified collection. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. Only clusters running MongoDB v4.2 or later can use Atlas Search. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection on which to create an Atlas Search index.

        collection_name : str
            Label that identifies the collection to create an Atlas Search index in.

        database : str
            Label that identifies the database that contains the collection to create an Atlas Search index in.

        name : str
            Label that identifies this index. Within each namespace, names of all indexes in the namespace must be unique.

        type : typing.Optional[str]
            Type of the index. The default type is search.

        definition : typing.Optional[BaseSearchIndexCreateRequestDefinition]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            Created

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.create_cluster_search_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                collection_name="collectionName",
                database="database",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_cluster_search_index(
            group_id,
            cluster_name,
            collection_name=collection_name,
            database=database,
            name=name,
            type=type,
            definition=definition,
            request_options=request_options,
        )
        return _response.data

    async def list_search_index(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[SearchIndexResponse]:
        """
        Returns all Atlas Search indexes on the specified collection. Atlas Search indexes contain the indexed fields and the analyzers used to create the indexes. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[SearchIndexResponse]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.list_search_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                database_name="databaseName",
                collection_name="collectionName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_search_index(
            group_id, cluster_name, database_name, collection_name, request_options=request_options
        )
        return _response.data

    async def get_index_by_name(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        index_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Returns one Atlas Search index in the specified project. You identify this index using its database, collection and name. Atlas Search index contains the indexed fields and the analyzers used to create the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        index_name : str
            Name of the Atlas Search index to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.get_index_by_name(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                database_name="databaseName",
                collection_name="collectionName",
                index_name="indexName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_index_by_name(
            group_id, cluster_name, database_name, collection_name, index_name, request_options=request_options
        )
        return _response.data

    async def delete_index_by_name(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        index_name: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one Atlas Search index that you identified with its database, collection, and name. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. This deletion is eventually consistent.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the database and collection with one or more Application Search indexes.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        index_name : str
            Name of the Atlas Search index to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.delete_index_by_name(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                database_name="databaseName",
                collection_name="collectionName",
                index_name="indexName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_index_by_name(
            group_id, cluster_name, database_name, collection_name, index_name, request_options=request_options
        )
        return _response.data

    async def update_index_by_name(
        self,
        group_id: str,
        cluster_name: str,
        database_name: str,
        collection_name: str,
        index_name: str,
        *,
        definition: SearchIndexUpdateRequestDefinition,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Updates one Atlas Search index that you identified with its database, collection name, and index name. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection whose Atlas Search index you want to update.

        database_name : str
            Label that identifies the database that contains the collection with one or more Atlas Search indexes.

        collection_name : str
            Name of the collection that contains one or more Atlas Search indexes.

        index_name : str
            Name of the Atlas Search index to update.

        definition : SearchIndexUpdateRequestDefinition
            The index definition to update the search index to.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, SearchIndexUpdateRequestDefinition

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.update_index_by_name(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                database_name="databaseName",
                collection_name="collectionName",
                index_name="indexName",
                definition=SearchIndexUpdateRequestDefinition(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_index_by_name(
            group_id,
            cluster_name,
            database_name,
            collection_name,
            index_name,
            definition=definition,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Returns one Atlas Search index in the specified project. You identify this index using its unique ID. Atlas Search index contains the indexed fields and the analyzers used to create the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Write role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection with one or more Atlas Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Application Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Application Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Application Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.get_cluster_search_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                index_id="indexId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_search_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    async def delete_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one Atlas Search index that you identified with its unique ID. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. This deletion is eventually consistent.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the database and collection with one or more Application Search indexes.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search index. Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.delete_cluster_search_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                index_id="indexId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_cluster_search_index(
            group_id, cluster_name, index_id, request_options=request_options
        )
        return _response.data

    async def update_cluster_search_index(
        self,
        group_id: str,
        cluster_name: str,
        index_id: str,
        *,
        definition: SearchIndexUpdateRequestDefinition,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchIndexResponse:
        """
        Updates one Atlas Search index that you identified with its unique ID. Atlas Search indexes define the fields on which to create the index and the analyzers to use when creating the index. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Name of the cluster that contains the collection whose Atlas Search index you want to update.

        index_id : str
            Unique 24-hexadecimal digit string that identifies the Atlas Search [index](https://dochub.mongodb.org/core/index-definitions-fts). Use the [Get All Atlas Search Indexes for a Collection API](https://docs.atlas.mongodb.com/reference/api/fts-indexes-get-all/) endpoint to find the IDs of all Atlas Search indexes.

        definition : SearchIndexUpdateRequestDefinition
            The index definition to update the search index to.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchIndexResponse
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, SearchIndexUpdateRequestDefinition

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atlas_search.update_cluster_search_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                index_id="indexId",
                definition=SearchIndexUpdateRequestDefinition(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_cluster_search_index(
            group_id, cluster_name, index_id, definition=definition, request_options=request_options
        )
        return _response.data
