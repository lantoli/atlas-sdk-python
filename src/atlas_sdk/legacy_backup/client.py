# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.api_atlas_checkpoint import ApiAtlasCheckpoint
from ..types.api_atlas_snapshot_schedule import ApiAtlasSnapshotSchedule
from ..types.api_bson_timestamp import ApiBsonTimestamp
from ..types.backup_restore_job import BackupRestoreJob
from ..types.backup_restore_job_delivery import BackupRestoreJobDelivery
from ..types.backup_snapshot import BackupSnapshot
from ..types.backup_snapshot_part import BackupSnapshotPart
from ..types.link import Link
from ..types.paginated_api_atlas_checkpoint import PaginatedApiAtlasCheckpoint
from ..types.paginated_restore_job import PaginatedRestoreJob
from ..types.paginated_snapshot import PaginatedSnapshot
from ..types.restore_job_file_hash import RestoreJobFileHash
from .raw_client import AsyncRawLegacyBackupClient, RawLegacyBackupClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class LegacyBackupClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawLegacyBackupClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawLegacyBackupClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawLegacyBackupClient
        """
        return self._raw_client

    def list_cluster_backup_checkpoints(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiAtlasCheckpoint:
        """
        Returns all legacy backup checkpoints for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the checkpoints that you want to return.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiAtlasCheckpoint
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.list_cluster_backup_checkpoints(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_cluster_backup_checkpoints(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_backup_checkpoint(
        self,
        group_id: str,
        cluster_name: str,
        checkpoint_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiAtlasCheckpoint:
        """
        Returns one legacy backup checkpoint for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the checkpoints that you want to return.

        checkpoint_id : str
            Unique 24-hexadecimal digit string that identifies the checkpoint.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiAtlasCheckpoint
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.get_cluster_backup_checkpoint(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            checkpoint_id="checkpointId",
        )
        """
        _response = self._raw_client.get_cluster_backup_checkpoint(
            group_id, cluster_name, checkpoint_id, request_options=request_options
        )
        return _response.data

    def list_cluster_restore_jobs(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        batch_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedRestoreJob:
        """
        Returns all legacy backup restore jobs for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation. If you use the `BATCH-ID` query parameter, you can retrieve all restore jobs in the specified batch. When creating a restore job for a sharded cluster, MongoDB Cloud creates a separate job for each shard, plus another for the config server. Each of those jobs are part of a batch. However, a batch can't include a restore job for a replica set.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        batch_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the batch of restore jobs to return. Timestamp in ISO 8601 date and time format in UTC when creating a restore job for a sharded cluster, Application creates a separate job for each shard, plus another for the config host. Each of these jobs comprise one batch. A restore job for a replica set can't be part of a batch.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedRestoreJob
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.list_cluster_restore_jobs(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_cluster_restore_jobs(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            batch_id=batch_id,
            request_options=request_options,
        )
        return _response.data

    def create_cluster_restore_job(
        self,
        group_id_: str,
        cluster_name_: str,
        *,
        delivery: BackupRestoreJobDelivery,
        batch_id: typing.Optional[str] = OMIT,
        checkpoint_id: typing.Optional[str] = OMIT,
        cluster_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        created: typing.Optional[dt.datetime] = OMIT,
        deployment_job_id: typing.Optional[str] = OMIT,
        encryption_enabled: typing.Optional[bool] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        hashes: typing.Optional[typing.Sequence[RestoreJobFileHash]] = OMIT,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        master_key_uuid: typing.Optional[str] = OMIT,
        oplog_inc: typing.Optional[int] = OMIT,
        oplog_ts: typing.Optional[str] = OMIT,
        point_in_time_utc_millis: typing.Optional[int] = OMIT,
        snapshot_id: typing.Optional[str] = OMIT,
        status_name: typing.Optional[str] = OMIT,
        timestamp: typing.Optional[ApiBsonTimestamp] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedRestoreJob:
        """
        Restores one legacy backup for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation. This endpoint doesn't support creating checkpoint restore jobs for sharded clusters, or creating restore jobs for queryable backup snapshots. If you create an automated restore job by specifying `delivery.methodName` of `AUTOMATED_RESTORE` in your request body, MongoDB Cloud removes all existing data on the target cluster prior to the restore.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name_ : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        delivery : BackupRestoreJobDelivery

        batch_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the batch to which this restore job belongs. This parameter exists only for a sharded cluster restore.

        checkpoint_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the sharded cluster checkpoint. The checkpoint represents the point in time back to which you want to restore you data. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`. Use this parameter with sharded clusters only.

            - If you set `checkpointId`, you can't set `oplogInc`, `oplogTs`, `snapshotId`, or `pointInTimeUTCMillis`.
            - If you provide this parameter, this endpoint restores all data up to this checkpoint to the database you specify in the `delivery` object.

        cluster_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the cluster with the snapshot you want to return. This parameter returns for restore clusters.

        cluster_name : typing.Optional[str]
            Human-readable label that identifies the cluster containing the snapshots you want to retrieve.

        created : typing.Optional[dt.datetime]
            Date and time when someone requested this restore job. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        deployment_job_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the an imported deployment job. This parameter exists when restoring from an imported snapshot/cluster shot.

        encryption_enabled : typing.Optional[bool]
            Flag that indicates whether someone encrypted the data in the restored snapshot.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that owns the snapshots.

        hashes : typing.Optional[typing.Sequence[RestoreJobFileHash]]
            List that contains documents mapping each restore file to a hashed checksum. This parameter applies after you download the corresponding `delivery.url`. If `"methodName" : "HTTP"`, this list contains one object that represents the hash of the `.tar.gz` file.

        id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the restore job.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        master_key_uuid : typing.Optional[str]
            Universally Unique Identifier (UUID) that identifies the Key Management Interoperability (KMIP) master key used to encrypt the snapshot data. This parameter applies only when `"encryptionEnabled" : "true"`.

        oplog_inc : typing.Optional[int]
            Thirty-two-bit incrementing ordinal that represents operations within a given second. When paired with `oplogTs`, this represents the point in time to which MongoDB Cloud restores your data. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`.

            - If you set `oplogInc`, you must set `oplogTs`, and can't set `checkpointId`, `snapshotId`, or `pointInTimeUTCMillis`.
            - If you provide this parameter, this endpoint restores all data up to and including this Oplog timestamp to the database you specified in the `delivery` object.

        oplog_ts : typing.Optional[str]
            Date and time from which you want to restore this snapshot. This parameter expresses its value in ISO 8601 format in UTC. This represents the first part of an Oplog timestamp. When paired with `oplogInc`, they represent the last database operation to which you want to restore your data. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`. Run a query against `local.oplog.rs` on your replica set to find the desired timestamp.

            - If you set `oplogTs`, you must set `oplogInc`, and you can't set `checkpointId`, `snapshotId`, or `pointInTimeUTCMillis`.
            - If you provide this parameter, this endpoint restores all data up to and including this Oplog timestamp to the database you specified in the `delivery` object.

        point_in_time_utc_millis : typing.Optional[int]
            Timestamp from which you want to restore this snapshot. This parameter expresses its value in the number of milliseconds elapsed since the UNIX epoch. This timestamp must fall within the last 24 hours of the current time. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`.

            - If you provide this parameter, this endpoint restores all data up to this point in time to the database you specified in the `delivery` object.
            - If you set `pointInTimeUTCMillis`, you can't set `oplogInc`, `oplogTs`, `snapshotId`, or `checkpointId`.

        snapshot_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the snapshot to restore. If you set `snapshotId`, you can't set `oplogInc`, `oplogTs`, `pointInTimeUTCMillis`, or `checkpointId`.

        status_name : typing.Optional[str]
            Human-readable label that identifies the status of the downloadable file at the time of the request.

        timestamp : typing.Optional[ApiBsonTimestamp]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedRestoreJob
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient, BackupRestoreJobDelivery

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.create_cluster_restore_job(
            group_id_="32b6e34b3d91647abb20e7b8",
            cluster_name_="clusterName",
            delivery=BackupRestoreJobDelivery(
                method_name="methodName",
            ),
        )
        """
        _response = self._raw_client.create_cluster_restore_job(
            group_id_,
            cluster_name_,
            delivery=delivery,
            batch_id=batch_id,
            checkpoint_id=checkpoint_id,
            cluster_id=cluster_id,
            cluster_name=cluster_name,
            created=created,
            deployment_job_id=deployment_job_id,
            encryption_enabled=encryption_enabled,
            group_id=group_id,
            hashes=hashes,
            id=id,
            links=links,
            master_key_uuid=master_key_uuid,
            oplog_inc=oplog_inc,
            oplog_ts=oplog_ts,
            point_in_time_utc_millis=point_in_time_utc_millis,
            snapshot_id=snapshot_id,
            status_name=status_name,
            timestamp=timestamp,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_restore_job(
        self, group_id: str, cluster_name: str, job_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> BackupRestoreJob:
        """
        Returns one legacy backup restore job for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        job_id : str
            Unique 24-hexadecimal digit string that identifies the restore job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupRestoreJob
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.get_cluster_restore_job(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            job_id="jobId",
        )
        """
        _response = self._raw_client.get_cluster_restore_job(
            group_id, cluster_name, job_id, request_options=request_options
        )
        return _response.data

    def get_cluster_snapshot_schedule(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiAtlasSnapshotSchedule:
        """
        Returns the snapshot schedule for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiAtlasSnapshotSchedule
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.get_cluster_snapshot_schedule(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.get_cluster_snapshot_schedule(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    def update_cluster_snapshot_schedule(
        self,
        group_id_: str,
        cluster_name: str,
        *,
        cluster_checkpoint_interval_min: int,
        cluster_id: str,
        daily_snapshot_retention_days: int,
        monthly_snapshot_retention_months: int,
        point_in_time_window_hours: int,
        snapshot_interval_hours: int,
        snapshot_retention_days: int,
        weekly_snapshot_retention_weeks: int,
        group_id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiAtlasSnapshotSchedule:
        """
        Updates the snapshot schedule for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        cluster_checkpoint_interval_min : int
            Quantity of time expressed in minutes between successive cluster checkpoints. This parameter applies only to sharded clusters. This number determines the granularity of continuous cloud backups for sharded clusters.

        cluster_id : str
            Unique 24-hexadecimal digit string that identifies the cluster with the snapshot you want to return.

        daily_snapshot_retention_days : int
            Quantity of time to keep daily snapshots. MongoDB Cloud expresses this value in days. Set this value to `0` to disable daily snapshot retention.

        monthly_snapshot_retention_months : int
            Number of months that MongoDB Cloud must keep monthly snapshots. Set this value to `0` to disable monthly snapshot retention.

        point_in_time_window_hours : int
            Number of hours before the current time from which MongoDB Cloud can create a Continuous Cloud Backup snapshot.

        snapshot_interval_hours : int
            Number of hours that must elapse before taking another snapshot.

        snapshot_retention_days : int
            Number of days that MongoDB Cloud must keep recent snapshots.

        weekly_snapshot_retention_weeks : int
            Number of weeks that MongoDB Cloud must keep weekly snapshots. Set this value to `0` to disable weekly snapshot retention.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the cluster.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiAtlasSnapshotSchedule
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.update_cluster_snapshot_schedule(
            group_id_="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            cluster_checkpoint_interval_min=1,
            cluster_id="32b6e34b3d91647abb20e7b8",
            daily_snapshot_retention_days=1,
            monthly_snapshot_retention_months=1,
            point_in_time_window_hours=1,
            snapshot_interval_hours=1,
            snapshot_retention_days=1,
            weekly_snapshot_retention_weeks=1,
        )
        """
        _response = self._raw_client.update_cluster_snapshot_schedule(
            group_id_,
            cluster_name,
            cluster_checkpoint_interval_min=cluster_checkpoint_interval_min,
            cluster_id=cluster_id,
            daily_snapshot_retention_days=daily_snapshot_retention_days,
            monthly_snapshot_retention_months=monthly_snapshot_retention_months,
            point_in_time_window_hours=point_in_time_window_hours,
            snapshot_interval_hours=snapshot_interval_hours,
            snapshot_retention_days=snapshot_retention_days,
            weekly_snapshot_retention_weeks=weekly_snapshot_retention_weeks,
            group_id=group_id,
            links=links,
            request_options=request_options,
        )
        return _response.data

    def list_cluster_snapshots(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        completed: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSnapshot:
        """
        Returns all legacy backup snapshots for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        completed : typing.Optional[str]
            Human-readable label that specifies whether to return only completed, incomplete, or all snapshots. By default, MongoDB Cloud only returns completed snapshots.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSnapshot
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.list_cluster_snapshots(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_cluster_snapshots(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            completed=completed,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_snapshot(
        self,
        group_id: str,
        cluster_name: str,
        snapshot_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupSnapshot:
        """
        Returns one legacy backup snapshot for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        snapshot_id : str
            Unique 24-hexadecimal digit string that identifies the desired snapshot.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupSnapshot
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.get_cluster_snapshot(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            snapshot_id="snapshotId",
        )
        """
        _response = self._raw_client.get_cluster_snapshot(
            group_id, cluster_name, snapshot_id, request_options=request_options
        )
        return _response.data

    def delete_cluster_snapshot(
        self,
        group_id: str,
        cluster_name: str,
        snapshot_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one legacy backup snapshot for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        snapshot_id : str
            Unique 24-hexadecimal digit string that identifies the desired snapshot.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.delete_cluster_snapshot(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            snapshot_id="snapshotId",
        )
        """
        _response = self._raw_client.delete_cluster_snapshot(
            group_id, cluster_name, snapshot_id, request_options=request_options
        )
        return _response.data

    def update_cluster_snapshot(
        self,
        group_id_: str,
        cluster_name_: str,
        snapshot_id: str,
        *,
        cluster_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        complete: typing.Optional[bool] = OMIT,
        created: typing.Optional[ApiBsonTimestamp] = OMIT,
        do_not_delete: typing.Optional[bool] = OMIT,
        expires: typing.Optional[dt.datetime] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        incremental: typing.Optional[bool] = OMIT,
        last_oplog_applied_timestamp: typing.Optional[ApiBsonTimestamp] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        parts: typing.Optional[typing.Sequence[BackupSnapshotPart]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupSnapshot:
        """
        Changes the expiration date for one legacy backup snapshot for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name_ : str
            Human-readable label that identifies the cluster.

        snapshot_id : str
            Unique 24-hexadecimal digit string that identifies the desired snapshot.

        cluster_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the cluster with the snapshots you want to return.

        cluster_name : typing.Optional[str]
            Human-readable label that identifies the cluster.

        complete : typing.Optional[bool]
            Flag that indicates whether the snapshot exists. This flag returns `false` while MongoDB Cloud creates the snapshot.

        created : typing.Optional[ApiBsonTimestamp]

        do_not_delete : typing.Optional[bool]
            Flag that indicates whether someone can delete this snapshot. You can't set `"doNotDelete" : true` and set a timestamp for **expires** in the same request.

        expires : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud deletes the snapshot. If `"doNotDelete" : true`, MongoDB Cloud removes any value set for this parameter. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that owns the snapshots.

        id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the snapshot.

        incremental : typing.Optional[bool]
            Flag indicating if this is an incremental or a full snapshot.

        last_oplog_applied_timestamp : typing.Optional[ApiBsonTimestamp]

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        parts : typing.Optional[typing.Sequence[BackupSnapshotPart]]
            Metadata that describes the complete snapshot.

            - For a replica set, this array contains a single document.
            - For a sharded cluster, this array contains one document for each shard plus one document for the config host.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupSnapshot
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.legacy_backup.update_cluster_snapshot(
            group_id_="32b6e34b3d91647abb20e7b8",
            cluster_name_="clusterName",
            snapshot_id="snapshotId",
        )
        """
        _response = self._raw_client.update_cluster_snapshot(
            group_id_,
            cluster_name_,
            snapshot_id,
            cluster_id=cluster_id,
            cluster_name=cluster_name,
            complete=complete,
            created=created,
            do_not_delete=do_not_delete,
            expires=expires,
            group_id=group_id,
            id=id,
            incremental=incremental,
            last_oplog_applied_timestamp=last_oplog_applied_timestamp,
            links=links,
            parts=parts,
            request_options=request_options,
        )
        return _response.data


class AsyncLegacyBackupClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawLegacyBackupClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawLegacyBackupClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawLegacyBackupClient
        """
        return self._raw_client

    async def list_cluster_backup_checkpoints(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiAtlasCheckpoint:
        """
        Returns all legacy backup checkpoints for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the checkpoints that you want to return.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiAtlasCheckpoint
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.list_cluster_backup_checkpoints(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_cluster_backup_checkpoints(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_backup_checkpoint(
        self,
        group_id: str,
        cluster_name: str,
        checkpoint_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiAtlasCheckpoint:
        """
        Returns one legacy backup checkpoint for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the checkpoints that you want to return.

        checkpoint_id : str
            Unique 24-hexadecimal digit string that identifies the checkpoint.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiAtlasCheckpoint
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.get_cluster_backup_checkpoint(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                checkpoint_id="checkpointId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_backup_checkpoint(
            group_id, cluster_name, checkpoint_id, request_options=request_options
        )
        return _response.data

    async def list_cluster_restore_jobs(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        batch_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedRestoreJob:
        """
        Returns all legacy backup restore jobs for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation. If you use the `BATCH-ID` query parameter, you can retrieve all restore jobs in the specified batch. When creating a restore job for a sharded cluster, MongoDB Cloud creates a separate job for each shard, plus another for the config server. Each of those jobs are part of a batch. However, a batch can't include a restore job for a replica set.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        batch_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the batch of restore jobs to return. Timestamp in ISO 8601 date and time format in UTC when creating a restore job for a sharded cluster, Application creates a separate job for each shard, plus another for the config host. Each of these jobs comprise one batch. A restore job for a replica set can't be part of a batch.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedRestoreJob
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.list_cluster_restore_jobs(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_cluster_restore_jobs(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            batch_id=batch_id,
            request_options=request_options,
        )
        return _response.data

    async def create_cluster_restore_job(
        self,
        group_id_: str,
        cluster_name_: str,
        *,
        delivery: BackupRestoreJobDelivery,
        batch_id: typing.Optional[str] = OMIT,
        checkpoint_id: typing.Optional[str] = OMIT,
        cluster_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        created: typing.Optional[dt.datetime] = OMIT,
        deployment_job_id: typing.Optional[str] = OMIT,
        encryption_enabled: typing.Optional[bool] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        hashes: typing.Optional[typing.Sequence[RestoreJobFileHash]] = OMIT,
        id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        master_key_uuid: typing.Optional[str] = OMIT,
        oplog_inc: typing.Optional[int] = OMIT,
        oplog_ts: typing.Optional[str] = OMIT,
        point_in_time_utc_millis: typing.Optional[int] = OMIT,
        snapshot_id: typing.Optional[str] = OMIT,
        status_name: typing.Optional[str] = OMIT,
        timestamp: typing.Optional[ApiBsonTimestamp] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedRestoreJob:
        """
        Restores one legacy backup for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation. This endpoint doesn't support creating checkpoint restore jobs for sharded clusters, or creating restore jobs for queryable backup snapshots. If you create an automated restore job by specifying `delivery.methodName` of `AUTOMATED_RESTORE` in your request body, MongoDB Cloud removes all existing data on the target cluster prior to the restore.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name_ : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        delivery : BackupRestoreJobDelivery

        batch_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the batch to which this restore job belongs. This parameter exists only for a sharded cluster restore.

        checkpoint_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the sharded cluster checkpoint. The checkpoint represents the point in time back to which you want to restore you data. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`. Use this parameter with sharded clusters only.

            - If you set `checkpointId`, you can't set `oplogInc`, `oplogTs`, `snapshotId`, or `pointInTimeUTCMillis`.
            - If you provide this parameter, this endpoint restores all data up to this checkpoint to the database you specify in the `delivery` object.

        cluster_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the cluster with the snapshot you want to return. This parameter returns for restore clusters.

        cluster_name : typing.Optional[str]
            Human-readable label that identifies the cluster containing the snapshots you want to retrieve.

        created : typing.Optional[dt.datetime]
            Date and time when someone requested this restore job. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        deployment_job_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the an imported deployment job. This parameter exists when restoring from an imported snapshot/cluster shot.

        encryption_enabled : typing.Optional[bool]
            Flag that indicates whether someone encrypted the data in the restored snapshot.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that owns the snapshots.

        hashes : typing.Optional[typing.Sequence[RestoreJobFileHash]]
            List that contains documents mapping each restore file to a hashed checksum. This parameter applies after you download the corresponding `delivery.url`. If `"methodName" : "HTTP"`, this list contains one object that represents the hash of the `.tar.gz` file.

        id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the restore job.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        master_key_uuid : typing.Optional[str]
            Universally Unique Identifier (UUID) that identifies the Key Management Interoperability (KMIP) master key used to encrypt the snapshot data. This parameter applies only when `"encryptionEnabled" : "true"`.

        oplog_inc : typing.Optional[int]
            Thirty-two-bit incrementing ordinal that represents operations within a given second. When paired with `oplogTs`, this represents the point in time to which MongoDB Cloud restores your data. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`.

            - If you set `oplogInc`, you must set `oplogTs`, and can't set `checkpointId`, `snapshotId`, or `pointInTimeUTCMillis`.
            - If you provide this parameter, this endpoint restores all data up to and including this Oplog timestamp to the database you specified in the `delivery` object.

        oplog_ts : typing.Optional[str]
            Date and time from which you want to restore this snapshot. This parameter expresses its value in ISO 8601 format in UTC. This represents the first part of an Oplog timestamp. When paired with `oplogInc`, they represent the last database operation to which you want to restore your data. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`. Run a query against `local.oplog.rs` on your replica set to find the desired timestamp.

            - If you set `oplogTs`, you must set `oplogInc`, and you can't set `checkpointId`, `snapshotId`, or `pointInTimeUTCMillis`.
            - If you provide this parameter, this endpoint restores all data up to and including this Oplog timestamp to the database you specified in the `delivery` object.

        point_in_time_utc_millis : typing.Optional[int]
            Timestamp from which you want to restore this snapshot. This parameter expresses its value in the number of milliseconds elapsed since the UNIX epoch. This timestamp must fall within the last 24 hours of the current time. This parameter applies when `"delivery.methodName" : "AUTOMATED_RESTORE"`.

            - If you provide this parameter, this endpoint restores all data up to this point in time to the database you specified in the `delivery` object.
            - If you set `pointInTimeUTCMillis`, you can't set `oplogInc`, `oplogTs`, `snapshotId`, or `checkpointId`.

        snapshot_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the snapshot to restore. If you set `snapshotId`, you can't set `oplogInc`, `oplogTs`, `pointInTimeUTCMillis`, or `checkpointId`.

        status_name : typing.Optional[str]
            Human-readable label that identifies the status of the downloadable file at the time of the request.

        timestamp : typing.Optional[ApiBsonTimestamp]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedRestoreJob
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, BackupRestoreJobDelivery

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.create_cluster_restore_job(
                group_id_="32b6e34b3d91647abb20e7b8",
                cluster_name_="clusterName",
                delivery=BackupRestoreJobDelivery(
                    method_name="methodName",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_cluster_restore_job(
            group_id_,
            cluster_name_,
            delivery=delivery,
            batch_id=batch_id,
            checkpoint_id=checkpoint_id,
            cluster_id=cluster_id,
            cluster_name=cluster_name,
            created=created,
            deployment_job_id=deployment_job_id,
            encryption_enabled=encryption_enabled,
            group_id=group_id,
            hashes=hashes,
            id=id,
            links=links,
            master_key_uuid=master_key_uuid,
            oplog_inc=oplog_inc,
            oplog_ts=oplog_ts,
            point_in_time_utc_millis=point_in_time_utc_millis,
            snapshot_id=snapshot_id,
            status_name=status_name,
            timestamp=timestamp,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_restore_job(
        self, group_id: str, cluster_name: str, job_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> BackupRestoreJob:
        """
        Returns one legacy backup restore job for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        job_id : str
            Unique 24-hexadecimal digit string that identifies the restore job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupRestoreJob
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.get_cluster_restore_job(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                job_id="jobId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_restore_job(
            group_id, cluster_name, job_id, request_options=request_options
        )
        return _response.data

    async def get_cluster_snapshot_schedule(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiAtlasSnapshotSchedule:
        """
        Returns the snapshot schedule for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiAtlasSnapshotSchedule
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.get_cluster_snapshot_schedule(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_snapshot_schedule(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def update_cluster_snapshot_schedule(
        self,
        group_id_: str,
        cluster_name: str,
        *,
        cluster_checkpoint_interval_min: int,
        cluster_id: str,
        daily_snapshot_retention_days: int,
        monthly_snapshot_retention_months: int,
        point_in_time_window_hours: int,
        snapshot_interval_hours: int,
        snapshot_retention_days: int,
        weekly_snapshot_retention_weeks: int,
        group_id: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiAtlasSnapshotSchedule:
        """
        Updates the snapshot schedule for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

         Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster with the snapshot you want to return.

        cluster_checkpoint_interval_min : int
            Quantity of time expressed in minutes between successive cluster checkpoints. This parameter applies only to sharded clusters. This number determines the granularity of continuous cloud backups for sharded clusters.

        cluster_id : str
            Unique 24-hexadecimal digit string that identifies the cluster with the snapshot you want to return.

        daily_snapshot_retention_days : int
            Quantity of time to keep daily snapshots. MongoDB Cloud expresses this value in days. Set this value to `0` to disable daily snapshot retention.

        monthly_snapshot_retention_months : int
            Number of months that MongoDB Cloud must keep monthly snapshots. Set this value to `0` to disable monthly snapshot retention.

        point_in_time_window_hours : int
            Number of hours before the current time from which MongoDB Cloud can create a Continuous Cloud Backup snapshot.

        snapshot_interval_hours : int
            Number of hours that must elapse before taking another snapshot.

        snapshot_retention_days : int
            Number of days that MongoDB Cloud must keep recent snapshots.

        weekly_snapshot_retention_weeks : int
            Number of weeks that MongoDB Cloud must keep weekly snapshots. Set this value to `0` to disable weekly snapshot retention.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the cluster.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiAtlasSnapshotSchedule
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.update_cluster_snapshot_schedule(
                group_id_="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                cluster_checkpoint_interval_min=1,
                cluster_id="32b6e34b3d91647abb20e7b8",
                daily_snapshot_retention_days=1,
                monthly_snapshot_retention_months=1,
                point_in_time_window_hours=1,
                snapshot_interval_hours=1,
                snapshot_retention_days=1,
                weekly_snapshot_retention_weeks=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_cluster_snapshot_schedule(
            group_id_,
            cluster_name,
            cluster_checkpoint_interval_min=cluster_checkpoint_interval_min,
            cluster_id=cluster_id,
            daily_snapshot_retention_days=daily_snapshot_retention_days,
            monthly_snapshot_retention_months=monthly_snapshot_retention_months,
            point_in_time_window_hours=point_in_time_window_hours,
            snapshot_interval_hours=snapshot_interval_hours,
            snapshot_retention_days=snapshot_retention_days,
            weekly_snapshot_retention_weeks=weekly_snapshot_retention_weeks,
            group_id=group_id,
            links=links,
            request_options=request_options,
        )
        return _response.data

    async def list_cluster_snapshots(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        completed: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSnapshot:
        """
        Returns all legacy backup snapshots for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        completed : typing.Optional[str]
            Human-readable label that specifies whether to return only completed, incomplete, or all snapshots. By default, MongoDB Cloud only returns completed snapshots.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSnapshot
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.list_cluster_snapshots(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_cluster_snapshots(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            completed=completed,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_snapshot(
        self,
        group_id: str,
        cluster_name: str,
        snapshot_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupSnapshot:
        """
        Returns one legacy backup snapshot for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        snapshot_id : str
            Unique 24-hexadecimal digit string that identifies the desired snapshot.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupSnapshot
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.get_cluster_snapshot(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                snapshot_id="snapshotId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_snapshot(
            group_id, cluster_name, snapshot_id, request_options=request_options
        )
        return _response.data

    async def delete_cluster_snapshot(
        self,
        group_id: str,
        cluster_name: str,
        snapshot_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one legacy backup snapshot for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster.

        snapshot_id : str
            Unique 24-hexadecimal digit string that identifies the desired snapshot.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.delete_cluster_snapshot(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                snapshot_id="snapshotId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_cluster_snapshot(
            group_id, cluster_name, snapshot_id, request_options=request_options
        )
        return _response.data

    async def update_cluster_snapshot(
        self,
        group_id_: str,
        cluster_name_: str,
        snapshot_id: str,
        *,
        cluster_id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        complete: typing.Optional[bool] = OMIT,
        created: typing.Optional[ApiBsonTimestamp] = OMIT,
        do_not_delete: typing.Optional[bool] = OMIT,
        expires: typing.Optional[dt.datetime] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        incremental: typing.Optional[bool] = OMIT,
        last_oplog_applied_timestamp: typing.Optional[ApiBsonTimestamp] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        parts: typing.Optional[typing.Sequence[BackupSnapshotPart]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupSnapshot:
        """
        Changes the expiration date for one legacy backup snapshot for one cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Effective 23 March 2020, all new clusters can use only Cloud Backups. When you upgrade to 4.2, your backup system upgrades to cloud backup if it is currently set to legacy backup. After this upgrade, all your existing legacy backup snapshots remain available. They expire over time in accordance with your retention policy. Your backup policy resets to the default schedule. If you had a custom backup policy in place with legacy backups, you must re-create it with the procedure outlined in the Cloud Backup documentation.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name_ : str
            Human-readable label that identifies the cluster.

        snapshot_id : str
            Unique 24-hexadecimal digit string that identifies the desired snapshot.

        cluster_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the cluster with the snapshots you want to return.

        cluster_name : typing.Optional[str]
            Human-readable label that identifies the cluster.

        complete : typing.Optional[bool]
            Flag that indicates whether the snapshot exists. This flag returns `false` while MongoDB Cloud creates the snapshot.

        created : typing.Optional[ApiBsonTimestamp]

        do_not_delete : typing.Optional[bool]
            Flag that indicates whether someone can delete this snapshot. You can't set `"doNotDelete" : true` and set a timestamp for **expires** in the same request.

        expires : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud deletes the snapshot. If `"doNotDelete" : true`, MongoDB Cloud removes any value set for this parameter. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that owns the snapshots.

        id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the snapshot.

        incremental : typing.Optional[bool]
            Flag indicating if this is an incremental or a full snapshot.

        last_oplog_applied_timestamp : typing.Optional[ApiBsonTimestamp]

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        parts : typing.Optional[typing.Sequence[BackupSnapshotPart]]
            Metadata that describes the complete snapshot.

            - For a replica set, this array contains a single document.
            - For a sharded cluster, this array contains one document for each shard plus one document for the config host.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupSnapshot
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.legacy_backup.update_cluster_snapshot(
                group_id_="32b6e34b3d91647abb20e7b8",
                cluster_name_="clusterName",
                snapshot_id="snapshotId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_cluster_snapshot(
            group_id_,
            cluster_name_,
            snapshot_id,
            cluster_id=cluster_id,
            cluster_name=cluster_name,
            complete=complete,
            created=created,
            do_not_delete=do_not_delete,
            expires=expires,
            group_id=group_id,
            id=id,
            incremental=incremental,
            last_oplog_applied_timestamp=last_oplog_applied_timestamp,
            links=links,
            parts=parts,
            request_options=request_options,
        )
        return _response.data
