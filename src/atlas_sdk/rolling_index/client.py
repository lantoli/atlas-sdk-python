# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.collation import Collation
from ..types.index_options import IndexOptions
from .raw_client import AsyncRawRollingIndexClient, RawRollingIndexClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RollingIndexClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawRollingIndexClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawRollingIndexClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawRollingIndexClient
        """
        return self._raw_client

    def create_rolling_index(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection: str,
        db: str,
        keys: typing.Sequence[typing.Dict[str, str]],
        collation: typing.Optional[Collation] = OMIT,
        options: typing.Optional[IndexOptions] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Creates an index on the cluster identified by its name in a rolling manner. Creating the index in this way allows index builds on one replica set member as a standalone at a time, starting with the secondary members. Creating indexes in this way requires at least one replica set election. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role or the Project Index Manager role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster on which MongoDB Cloud creates an index.

        collection : str
            Human-readable label of the collection for which MongoDB Cloud creates an index.

        db : str
            Human-readable label of the database that holds the collection on which MongoDB Cloud creates an index.

        keys : typing.Sequence[typing.Dict[str, str]]
            List that contains one or more objects that describe the parameters that you want to index.

        collation : typing.Optional[Collation]

        options : typing.Optional[IndexOptions]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient, Collation, IndexOptions

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.rolling_index.create_rolling_index(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            collation=Collation(
                alternate="non-ignorable",
                backwards=False,
                case_first="lower",
                case_level=False,
                locale="af",
                max_variable="punct",
                normalization=False,
                numeric_ordering=False,
                strength=3,
            ),
            collection="accounts",
            db="sample_airbnb",
            keys=[{"property_type": "1"}],
            options=IndexOptions(
                name="PartialIndexTest",
                partial_filter_expression={"limit": {"$gt": 900}},
            ),
        )
        """
        _response = self._raw_client.create_rolling_index(
            group_id,
            cluster_name,
            collection=collection,
            db=db,
            keys=keys,
            collation=collation,
            options=options,
            request_options=request_options,
        )
        return _response.data


class AsyncRollingIndexClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawRollingIndexClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawRollingIndexClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawRollingIndexClient
        """
        return self._raw_client

    async def create_rolling_index(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection: str,
        db: str,
        keys: typing.Sequence[typing.Dict[str, str]],
        collation: typing.Optional[Collation] = OMIT,
        options: typing.Optional[IndexOptions] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Creates an index on the cluster identified by its name in a rolling manner. Creating the index in this way allows index builds on one replica set member as a standalone at a time, starting with the secondary members. Creating indexes in this way requires at least one replica set election. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role or the Project Index Manager role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster on which MongoDB Cloud creates an index.

        collection : str
            Human-readable label of the collection for which MongoDB Cloud creates an index.

        db : str
            Human-readable label of the database that holds the collection on which MongoDB Cloud creates an index.

        keys : typing.Sequence[typing.Dict[str, str]]
            List that contains one or more objects that describe the parameters that you want to index.

        collation : typing.Optional[Collation]

        options : typing.Optional[IndexOptions]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, Collation, IndexOptions

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.rolling_index.create_rolling_index(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                collation=Collation(
                    alternate="non-ignorable",
                    backwards=False,
                    case_first="lower",
                    case_level=False,
                    locale="af",
                    max_variable="punct",
                    normalization=False,
                    numeric_ordering=False,
                    strength=3,
                ),
                collection="accounts",
                db="sample_airbnb",
                keys=[{"property_type": "1"}],
                options=IndexOptions(
                    name="PartialIndexTest",
                    partial_filter_expression={"limit": {"$gt": 900}},
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_rolling_index(
            group_id,
            cluster_name,
            collection=collection,
            db=db,
            keys=keys,
            collation=collation,
            options=options,
            request_options=request_options,
        )
        return _response.data
