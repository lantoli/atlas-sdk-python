# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class IndexOptions(UniversalBaseModel):
    """
    One or more settings that determine how the MongoDB Cloud creates this MongoDB index.
    """

    f_2dsphere_index_version: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="2dsphereIndexVersion"),
        pydantic.Field(
            alias="2dsphereIndexVersion",
            description="Index version number applied to the 2dsphere index. MongoDB 3.2 and later use version 3. Use this option to override the default version number. This option applies to the **2dsphere** index type only.",
        ),
    ] = None
    background: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Flag that indicates whether MongoDB should build the index in the background. This applies to MongoDB databases running feature compatibility version 4.0 or earlier. MongoDB databases running FCV 4.2 or later build indexes using an optimized build process. This process holds the exclusive lock only at the beginning and end of the build process. The rest of the build process yields to interleaving read and write operations. MongoDB databases running FCV 4.2 or later ignore this option. This option applies to all index types.
    """

    bits: typing.Optional[int] = pydantic.Field(default=None)
    """
    Number of precision applied to the stored geohash value of the location data. This option applies to the **2d** index type only.
    """

    bucket_size: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="bucketSize"),
        pydantic.Field(
            alias="bucketSize",
            description="Number of units within which to group the location values. You could group in the same bucket those location values within the specified number of units to each other. This option applies to the geoHaystack index type only.\n\nMongoDB 5.0 removed geoHaystack Indexes and the `geoSearch` command.",
        ),
    ] = None
    columnstore_projection: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, int]],
        FieldMetadata(alias="columnstoreProjection"),
        pydantic.Field(
            alias="columnstoreProjection",
            description="The `columnstoreProjection` document allows to include or exclude sub-schemas schema. One cannot combine inclusion and exclusion statements. Accordingly, the <value> can be either of the following:\n1 or true to include the field and recursively all fields it is a prefix of in the index\n0 or false to exclude the field and recursively all fields it is a prefix of from the index.",
        ),
    ] = None
    default_language: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human language that determines the list of stop words and the rules for the stemmer and tokenizer. This option accepts the supported languages using its name in lowercase English or the ISO 639-2 code. If you set this parameter to `"none"`, then the text search uses simple tokenization with no list of stop words and no stemming. This option applies to the **text** index type only.
    """

    expire_after_seconds: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="expireAfterSeconds"),
        pydantic.Field(
            alias="expireAfterSeconds",
            description="Number of seconds that MongoDB retains documents in a Time To Live (TTL) index.",
        ),
    ] = None
    hidden: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Flag that determines whether the index is hidden from the query planner. A hidden index is not evaluated as part of the query plan selection.
    """

    language_override: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable label that identifies the document parameter that contains the override language for the document. This option applies to the **text** index type only.
    """

    max: typing.Optional[int] = pydantic.Field(default=None)
    """
    Upper inclusive boundary to limit the longitude and latitude values. This option applies to the 2d index type only.
    """

    min: typing.Optional[int] = pydantic.Field(default=None)
    """
    Lower inclusive boundary to limit the longitude and latitude values. This option applies to the 2d index type only.
    """

    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable label that identifies this index. This option applies to all index types.
    """

    partial_filter_expression: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, typing.Any]],
        FieldMetadata(alias="partialFilterExpression"),
        pydantic.Field(
            alias="partialFilterExpression",
            description='Rules that limit the documents that the index references to a filter expression. All MongoDB index types accept a `partialFilterExpression` option. `partialFilterExpression` can include following expressions:\n\n- equality (`"parameter" : "value"` or using the `$eq` operator)\n- `"$exists": true`\n, maximum: `$gt`, `$gte`, `$lt`, `$lte` comparisons\n- `$type`\n- `$and` (top-level only)\n This option applies to all index types.',
        ),
    ] = None
    sparse: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Flag that indicates whether the index references documents that only have the specified parameter. These indexes use less space but behave differently in some situations like when sorting. The following index types default to sparse and ignore this option: `2dsphere`, `2d`, `geoHaystack`, `text`.
    
    Compound indexes that includes one or more indexes with `2dsphere` keys alongside other key types, only the `2dsphere` index parameters determine which documents the index references. If you run MongoDB 3.2 or later, use partial indexes. This option applies to all index types.
    """

    storage_engine: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, typing.Any]],
        FieldMetadata(alias="storageEngine"),
        pydantic.Field(
            alias="storageEngine",
            description='Storage engine set for the specific index. This value can be set only at creation. This option uses the following format: `"storageEngine" : { "<storage-engine-name>" : "<options>" }` MongoDB validates storage engine configuration options when creating indexes. To support replica sets with members with different storage engines, MongoDB logs these options to the oplog during replication. This option applies to all index types.',
        ),
    ] = None
    text_index_version: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="textIndexVersion"),
        pydantic.Field(
            alias="textIndexVersion",
            description="Version applied to this text index. MongoDB 3.2 and later use version `3`. Use this option to override the default version number. This option applies to the **text** index type only.",
        ),
    ] = None
    weights: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Relative importance to place upon provided index parameters. This object expresses this as key/value pairs of index parameter and weight to apply to that parameter. You can specify weights for some or all the indexed parameters. The weight must be an integer between 1 and 99,999. MongoDB 5.0 and later can apply **weights** to **text** indexes only.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
