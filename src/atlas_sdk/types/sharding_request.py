# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .shard_entry import ShardEntry


class ShardingRequest(UniversalBaseModel):
    """
    Document that configures sharding on the destination cluster when migrating from a replica set source to a sharded cluster destination on MongoDB 6.0 or higher. If you don't wish to shard any collections on the destination cluster, leave this empty.
    """

    create_supporting_indexes: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="createSupportingIndexes"),
        pydantic.Field(
            alias="createSupportingIndexes",
            description="Flag that lets the migration create supporting indexes for the shard keys, if none exists, as the destination cluster also needs compatible indexes for the specified shard keys.",
        ),
    ]
    sharding_entries: typing_extensions.Annotated[
        typing.List[ShardEntry],
        FieldMetadata(alias="shardingEntries"),
        pydantic.Field(
            alias="shardingEntries",
            description="List of shard configurations to shard destination collections. Atlas shards only those collections that you include in the sharding entries array.",
        ),
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
