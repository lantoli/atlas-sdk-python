# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .shard_keys import ShardKeys


class ShardEntry(UniversalBaseModel):
    """
    Sharding configuration for a collection to be sharded on the destination cluster.
    """

    collection: str = pydantic.Field()
    """
    Human-readable label that identifies the collection to be sharded on the destination cluster.
    """

    database: str = pydantic.Field()
    """
    Human-readable label that identifies the database that contains the collection to be sharded on the destination cluster.
    """

    shard_collection: typing_extensions.Annotated[
        ShardKeys, FieldMetadata(alias="shardCollection"), pydantic.Field(alias="shardCollection")
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
