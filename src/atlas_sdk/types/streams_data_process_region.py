# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .base_streams_region import BaseStreamsRegion
from .link import Link


class StreamsDataProcessRegion(UniversalBaseModel):
    """
    Information about the cloud provider region in which MongoDB Cloud processes the stream.
    """

    cloud_provider: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="cloudProvider"),
        pydantic.Field(alias="cloudProvider", description="Human-readable label that identifies the cloud provider."),
    ]
    links: typing.Optional[typing.List[Link]] = pydantic.Field(default=None)
    """
    List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.
    """

    region: BaseStreamsRegion

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
