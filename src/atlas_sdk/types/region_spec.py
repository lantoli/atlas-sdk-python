# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class RegionSpec(UniversalBaseModel):
    """
    Physical location where MongoDB Cloud provisions cluster nodes.
    """

    analytics_nodes: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="analyticsNodes"),
        pydantic.Field(
            alias="analyticsNodes",
            description="Number of analytics nodes in the region. Analytics nodes handle analytic data such as reporting queries from MongoDB Connector for Business Intelligence on MongoDB Cloud. Analytics nodes are read-only, and can never become the primary. Use `replicationSpecs[n].{region}.analyticsNodes` instead.",
        ),
    ] = None
    electable_nodes: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="electableNodes"),
        pydantic.Field(
            alias="electableNodes",
            description="Number of electable nodes to deploy in the specified region. Electable nodes can become the primary and can facilitate local reads. Use `replicationSpecs[n].{region}.electableNodes` instead.",
        ),
    ] = None
    priority: typing.Optional[int] = pydantic.Field(default=None)
    """
    Number that indicates the election priority of the region. To identify the Preferred Region of the cluster, set this parameter to `7`. The primary node runs in the **Preferred Region**. To identify a read-only region, set this parameter to `0`.
    """

    read_only_nodes: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="readOnlyNodes"),
        pydantic.Field(
            alias="readOnlyNodes",
            description="Number of read-only nodes in the region. Read-only nodes can never become the primary member, but can facilitate local reads. Use `replicationSpecs[n].{region}.readOnlyNodes` instead.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
