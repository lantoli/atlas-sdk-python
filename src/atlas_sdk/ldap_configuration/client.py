# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.db_user_tlsx509settings import DbUserTlsx509Settings
from ..types.ldap_security_settings import LdapSecuritySettings
from ..types.ldap_verify_connectivity_job_request import LdapVerifyConnectivityJobRequest
from ..types.link import Link
from ..types.user_security import UserSecurity
from .raw_client import AsyncRawLdapConfigurationClient, RawLdapConfigurationClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class LdapConfigurationClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawLdapConfigurationClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawLdapConfigurationClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawLdapConfigurationClient
        """
        return self._raw_client

    def get_user_security(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> UserSecurity:
        """
        Returns the current LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSecurity
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.ldap_configuration.get_user_security(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.get_user_security(group_id, request_options=request_options)
        return _response.data

    def update_user_security(
        self,
        group_id: str,
        *,
        customer_x509: typing.Optional[DbUserTlsx509Settings] = OMIT,
        ldap: typing.Optional[LdapSecuritySettings] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UserSecurity:
        """
        Edits the LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Updating this configuration triggers a rolling restart of the database.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        customer_x509 : typing.Optional[DbUserTlsx509Settings]

        ldap : typing.Optional[LdapSecuritySettings]

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSecurity
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.ldap_configuration.update_user_security(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.update_user_security(
            group_id, customer_x509=customer_x509, ldap=ldap, links=links, request_options=request_options
        )
        return _response.data

    def delete_ldap_user_mapping(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> UserSecurity:
        """
        Removes the current LDAP Distinguished Name mapping captured in the ``userToDNMapping`` document from the LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSecurity
            Accepted

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.ldap_configuration.delete_ldap_user_mapping(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.delete_ldap_user_mapping(group_id, request_options=request_options)
        return _response.data

    def verify_user_security_ldap(
        self,
        group_id: str,
        *,
        bind_password: str,
        bind_username: str,
        hostname: str,
        port: int,
        authz_query_template: typing.Optional[str] = OMIT,
        ca_certificate: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> LdapVerifyConnectivityJobRequest:
        """
        Verifies the LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        bind_password : str
            Password that MongoDB Cloud uses to authenticate the `bindUsername`.

        bind_username : str
            Full Distinguished Name (DN) of the Lightweight Directory Access Protocol (LDAP) user that MongoDB Cloud uses to connect to the LDAP host. LDAP distinguished names must be formatted according to RFC 2253.

        hostname : str
            Human-readable label that identifies the hostname or Internet Protocol (IP) address of the Lightweight Directory Access Protocol (LDAP) host. This host must have access to the internet or have a Virtual Private Cloud (VPC) peering connection to your cluster.

        port : int
            IANA port to which the Lightweight Directory Access Protocol (LDAP) host listens for client connections.

        authz_query_template : typing.Optional[str]
            Lightweight Directory Access Protocol (LDAP) query template that MongoDB Cloud applies to create an LDAP query to return the LDAP groups associated with the authenticated MongoDB user. MongoDB Cloud uses this parameter only for user authorization.

            Use the `{USER}` placeholder in the Uniform Resource Locator (URL) to substitute the authenticated username. The query relates to the host specified with the hostname. Format this query per [RFC 4515](https://datatracker.ietf.org/doc/html/rfc4515) and [RFC 4516](https://datatracker.ietf.org/doc/html/rfc4516).

        ca_certificate : typing.Optional[str]
            Certificate Authority (CA) certificate that MongoDB Cloud uses to verify the identity of the Lightweight Directory Access Protocol (LDAP) host. MongoDB Cloud allows self-signed certificates. To delete an assigned value, pass an empty string: `"caCertificate": ""`.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LdapVerifyConnectivityJobRequest
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.ldap_configuration.verify_user_security_ldap(
            group_id="32b6e34b3d91647abb20e7b8",
            bind_password="bindPassword",
            bind_username="CN=BindUser,CN=Users,DC=myldapserver,DC=mycompany,DC=com",
            hostname="hostname",
            port=1,
        )
        """
        _response = self._raw_client.verify_user_security_ldap(
            group_id,
            bind_password=bind_password,
            bind_username=bind_username,
            hostname=hostname,
            port=port,
            authz_query_template=authz_query_template,
            ca_certificate=ca_certificate,
            links=links,
            request_options=request_options,
        )
        return _response.data

    def get_user_security_verify(
        self, group_id: str, request_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> LdapVerifyConnectivityJobRequest:
        """
        Returns the status of one request to verify one LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_id : str
            Unique string that identifies the request to verify an Lightweight Directory Access Protocol (LDAP) configuration.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LdapVerifyConnectivityJobRequest
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.ldap_configuration.get_user_security_verify(
            group_id="32b6e34b3d91647abb20e7b8",
            request_id="requestId",
        )
        """
        _response = self._raw_client.get_user_security_verify(group_id, request_id, request_options=request_options)
        return _response.data


class AsyncLdapConfigurationClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawLdapConfigurationClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawLdapConfigurationClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawLdapConfigurationClient
        """
        return self._raw_client

    async def get_user_security(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> UserSecurity:
        """
        Returns the current LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSecurity
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ldap_configuration.get_user_security(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_user_security(group_id, request_options=request_options)
        return _response.data

    async def update_user_security(
        self,
        group_id: str,
        *,
        customer_x509: typing.Optional[DbUserTlsx509Settings] = OMIT,
        ldap: typing.Optional[LdapSecuritySettings] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UserSecurity:
        """
        Edits the LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Updating this configuration triggers a rolling restart of the database.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        customer_x509 : typing.Optional[DbUserTlsx509Settings]

        ldap : typing.Optional[LdapSecuritySettings]

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSecurity
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ldap_configuration.update_user_security(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_user_security(
            group_id, customer_x509=customer_x509, ldap=ldap, links=links, request_options=request_options
        )
        return _response.data

    async def delete_ldap_user_mapping(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> UserSecurity:
        """
        Removes the current LDAP Distinguished Name mapping captured in the ``userToDNMapping`` document from the LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSecurity
            Accepted

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ldap_configuration.delete_ldap_user_mapping(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_ldap_user_mapping(group_id, request_options=request_options)
        return _response.data

    async def verify_user_security_ldap(
        self,
        group_id: str,
        *,
        bind_password: str,
        bind_username: str,
        hostname: str,
        port: int,
        authz_query_template: typing.Optional[str] = OMIT,
        ca_certificate: typing.Optional[str] = OMIT,
        links: typing.Optional[typing.Sequence[Link]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> LdapVerifyConnectivityJobRequest:
        """
        Verifies the LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        bind_password : str
            Password that MongoDB Cloud uses to authenticate the `bindUsername`.

        bind_username : str
            Full Distinguished Name (DN) of the Lightweight Directory Access Protocol (LDAP) user that MongoDB Cloud uses to connect to the LDAP host. LDAP distinguished names must be formatted according to RFC 2253.

        hostname : str
            Human-readable label that identifies the hostname or Internet Protocol (IP) address of the Lightweight Directory Access Protocol (LDAP) host. This host must have access to the internet or have a Virtual Private Cloud (VPC) peering connection to your cluster.

        port : int
            IANA port to which the Lightweight Directory Access Protocol (LDAP) host listens for client connections.

        authz_query_template : typing.Optional[str]
            Lightweight Directory Access Protocol (LDAP) query template that MongoDB Cloud applies to create an LDAP query to return the LDAP groups associated with the authenticated MongoDB user. MongoDB Cloud uses this parameter only for user authorization.

            Use the `{USER}` placeholder in the Uniform Resource Locator (URL) to substitute the authenticated username. The query relates to the host specified with the hostname. Format this query per [RFC 4515](https://datatracker.ietf.org/doc/html/rfc4515) and [RFC 4516](https://datatracker.ietf.org/doc/html/rfc4516).

        ca_certificate : typing.Optional[str]
            Certificate Authority (CA) certificate that MongoDB Cloud uses to verify the identity of the Lightweight Directory Access Protocol (LDAP) host. MongoDB Cloud allows self-signed certificates. To delete an assigned value, pass an empty string: `"caCertificate": ""`.

        links : typing.Optional[typing.Sequence[Link]]
            List of one or more Uniform Resource Locators (URLs) that point to API sub-resources, related API resources, or both. RFC 5988 outlines these relationships.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LdapVerifyConnectivityJobRequest
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ldap_configuration.verify_user_security_ldap(
                group_id="32b6e34b3d91647abb20e7b8",
                bind_password="bindPassword",
                bind_username="CN=BindUser,CN=Users,DC=myldapserver,DC=mycompany,DC=com",
                hostname="hostname",
                port=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.verify_user_security_ldap(
            group_id,
            bind_password=bind_password,
            bind_username=bind_username,
            hostname=hostname,
            port=port,
            authz_query_template=authz_query_template,
            ca_certificate=ca_certificate,
            links=links,
            request_options=request_options,
        )
        return _response.data

    async def get_user_security_verify(
        self, group_id: str, request_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> LdapVerifyConnectivityJobRequest:
        """
        Returns the status of one request to verify one LDAP configuration for the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_id : str
            Unique string that identifies the request to verify an Lightweight Directory Access Protocol (LDAP) configuration.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LdapVerifyConnectivityJobRequest
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ldap_configuration.get_user_security_verify(
                group_id="32b6e34b3d91647abb20e7b8",
                request_id="requestId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_user_security_verify(
            group_id, request_id, request_options=request_options
        )
        return _response.data
