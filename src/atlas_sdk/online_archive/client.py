# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.backup_online_archive import BackupOnlineArchive
from ..types.create_data_process_region import CreateDataProcessRegion
from ..types.criteria import Criteria
from ..types.data_expiration_rule import DataExpirationRule
from ..types.data_process_region import DataProcessRegion
from ..types.online_archive_schedule import OnlineArchiveSchedule
from ..types.paginated_online_archive import PaginatedOnlineArchive
from ..types.partition_field import PartitionField
from .raw_client import AsyncRawOnlineArchiveClient, RawOnlineArchiveClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class OnlineArchiveClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawOnlineArchiveClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawOnlineArchiveClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawOnlineArchiveClient
        """
        return self._raw_client

    def list_online_archives(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedOnlineArchive:
        """
        Returns details of all online archives. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection for which you want to return the online archives.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedOnlineArchive
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.online_archive.list_online_archives(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_online_archives(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    def create_online_archive(
        self,
        group_id: str,
        cluster_name: str,
        *,
        coll_name: str,
        criteria: Criteria,
        db_name: str,
        collection_type: typing.Optional[str] = OMIT,
        data_expiration_rule: typing.Optional[DataExpirationRule] = OMIT,
        data_process_region: typing.Optional[CreateDataProcessRegion] = OMIT,
        partition_fields: typing.Optional[typing.Sequence[PartitionField]] = OMIT,
        paused: typing.Optional[bool] = OMIT,
        schedule: typing.Optional[OnlineArchiveSchedule] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupOnlineArchive:
        """
        Creates one online archive. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection for which you want to create one online archive.

        coll_name : str
            Human-readable label that identifies the collection for which you created the online archive.

        criteria : Criteria

        db_name : str
            Human-readable label of the database that contains the collection that contains the online archive.

        collection_type : typing.Optional[str]
            Classification of MongoDB database collection that you want to return.

            If you set this parameter to `TIMESERIES`, set `"criteria.type" : "date"` and `"criteria.dateFormat" : "ISODATE"`.

        data_expiration_rule : typing.Optional[DataExpirationRule]

        data_process_region : typing.Optional[CreateDataProcessRegion]

        partition_fields : typing.Optional[typing.Sequence[PartitionField]]
            List that contains document parameters to use to logically divide data within a collection. Partitions provide a coarse level of filtering of the underlying collection data. To divide your data, specify parameters that you frequently query. If you specified `criteria.type`: `DATE` in the Create One Online Archive endpoint, then you can specify up to three parameters by which to query. One of these parameters must be the `DATE` value, which is required in this case. If you specified `criteria.type`: `CUSTOM` in the Create One Online Archive endpoint, then you can specify up to two parameters by which to query. Queries that don't use `criteria.type`: `DATE` or `criteria.type`: `CUSTOM` parameters cause MongoDB to scan a full collection of all archived documents. This takes more time and increases your costs.

        paused : typing.Optional[bool]
            Flag that indicates whether this online archive exists in the paused state. A request to resume fails if the collection has another active online archive. To pause an active online archive or resume a paused online archive, you must include this parameter. To pause an active archive, set this to **true**. To resume a paused archive, set this to **false**.

        schedule : typing.Optional[OnlineArchiveSchedule]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupOnlineArchive
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient, Criteria

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.online_archive.create_online_archive(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            coll_name="collName",
            criteria=Criteria(),
            db_name="dbName",
        )
        """
        _response = self._raw_client.create_online_archive(
            group_id,
            cluster_name,
            coll_name=coll_name,
            criteria=criteria,
            db_name=db_name,
            collection_type=collection_type,
            data_expiration_rule=data_expiration_rule,
            data_process_region=data_process_region,
            partition_fields=partition_fields,
            paused=paused,
            schedule=schedule,
            request_options=request_options,
        )
        return _response.data

    def get_online_archive(
        self,
        group_id: str,
        cluster_name: str,
        archive_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupOnlineArchive:
        """
        Returns one online archive for one cluster. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the specified collection from which Application created the online archive.

        archive_id : str
            Unique 24-hexadecimal digit string that identifies the online archive to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupOnlineArchive
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.online_archive.get_online_archive(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            archive_id="archiveId",
        )
        """
        _response = self._raw_client.get_online_archive(
            group_id, cluster_name, archive_id, request_options=request_options
        )
        return _response.data

    def delete_online_archive(
        self,
        group_id: str,
        cluster_name: str,
        archive_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one online archive. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection from which you want to remove an online archive.

        archive_id : str
            Unique 24-hexadecimal digit string that identifies the online archive to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.online_archive.delete_online_archive(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            archive_id="archiveId",
        )
        """
        _response = self._raw_client.delete_online_archive(
            group_id, cluster_name, archive_id, request_options=request_options
        )
        return _response.data

    def update_online_archive(
        self,
        group_id_: str,
        cluster_name_: str,
        archive_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        coll_name: typing.Optional[str] = OMIT,
        collection_type: typing.Optional[str] = OMIT,
        criteria: typing.Optional[Criteria] = OMIT,
        data_expiration_rule: typing.Optional[DataExpirationRule] = OMIT,
        data_process_region: typing.Optional[DataProcessRegion] = OMIT,
        data_set_name: typing.Optional[str] = OMIT,
        db_name: typing.Optional[str] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        partition_fields: typing.Optional[typing.Sequence[PartitionField]] = OMIT,
        paused: typing.Optional[bool] = OMIT,
        schedule: typing.Optional[OnlineArchiveSchedule] = OMIT,
        state: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupOnlineArchive:
        """
        Updates, pauses, or resumes one online archive. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name_ : str
            Human-readable label that identifies the cluster that contains the specified collection from which Application created the online archive.

        archive_id : str
            Unique 24-hexadecimal digit string that identifies the online archive to update.

        id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the online archive.

        cluster_name : typing.Optional[str]
            Human-readable label that identifies the cluster that contains the collection for which you want to create an online archive.

        coll_name : typing.Optional[str]
            Human-readable label that identifies the collection for which you created the online archive.

        collection_type : typing.Optional[str]
            Classification of MongoDB database collection that you want to return.

            If you set this parameter to `TIMESERIES`, set `"criteria.type" : "date"` and `"criteria.dateFormat" : "ISODATE"`.

        criteria : typing.Optional[Criteria]

        data_expiration_rule : typing.Optional[DataExpirationRule]

        data_process_region : typing.Optional[DataProcessRegion]

        data_set_name : typing.Optional[str]
            Human-readable label that identifies the dataset that Atlas generates for this online archive.

        db_name : typing.Optional[str]
            Human-readable label of the database that contains the collection that contains the online archive.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the specified cluster. The specified cluster contains the collection for which to create the online archive.

        partition_fields : typing.Optional[typing.Sequence[PartitionField]]
            List that contains document parameters to use to logically divide data within a collection. Partitions provide a coarse level of filtering of the underlying collection data. To divide your data, specify parameters that you frequently query. If you specified `criteria.type`: `DATE` in the Create One Online Archive endpoint, then you can specify up to three parameters by which to query. One of these parameters must be the `DATE` value, which is required in this case. If you specified `criteria.type`: `CUSTOM` in the Create One Online Archive endpoint, then you can specify up to two parameters by which to query. Queries that don't use `criteria.type`: `DATE` or `criteria.type`: `CUSTOM` parameters cause MongoDB to scan a full collection of all archived documents. This takes more time and increases your costs.

        paused : typing.Optional[bool]
            Flag that indicates whether this online archive exists in the paused state. A request to resume fails if the collection has another active online archive. To pause an active online archive or resume a paused online archive, you must include this parameter. To pause an active archive, set this to **true**. To resume a paused archive, set this to **false**.

        schedule : typing.Optional[OnlineArchiveSchedule]

        state : typing.Optional[str]
            Phase of the process to create this online archive when you made this request.

            | State       | Indication |
            |-------------|------------|
            | `PENDING`   | MongoDB Cloud has queued documents for archive. Archiving hasn't started. |
            | `ARCHIVING` | MongoDB Cloud started archiving documents that meet the archival criteria. |
            | `IDLE`      | MongoDB Cloud waits to start the next archival job. |
            | `PAUSING`   | Someone chose to stop archiving. MongoDB Cloud finishes the running archival job then changes the state to `PAUSED` when that job completes. |
            | `PAUSED`    | MongoDB Cloud has stopped archiving. Archived documents can be queried. The specified archiving operation on the active cluster cannot archive additional documents. You can resume archiving for paused archives at any time. |
            | `ORPHANED`  | Someone has deleted the collection associated with an active or paused archive. MongoDB Cloud doesn't delete the archived data. You must manually delete the online archives associated with the deleted collection. |
            | `DELETED`   | Someone has deleted the archive was deleted. When someone deletes an online archive, MongoDB Cloud removes all associated archived documents from the cloud object storage. |

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupOnlineArchive
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.online_archive.update_online_archive(
            group_id_="32b6e34b3d91647abb20e7b8",
            cluster_name_="clusterName",
            archive_id="archiveId",
        )
        """
        _response = self._raw_client.update_online_archive(
            group_id_,
            cluster_name_,
            archive_id,
            id=id,
            cluster_name=cluster_name,
            coll_name=coll_name,
            collection_type=collection_type,
            criteria=criteria,
            data_expiration_rule=data_expiration_rule,
            data_process_region=data_process_region,
            data_set_name=data_set_name,
            db_name=db_name,
            group_id=group_id,
            partition_fields=partition_fields,
            paused=paused,
            schedule=schedule,
            state=state,
            request_options=request_options,
        )
        return _response.data

    def download_query_logs(
        self,
        group_id: str,
        cluster_name: str,
        *,
        start_date: typing.Optional[int] = None,
        end_date: typing.Optional[int] = None,
        archive_only: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[bytes]:
        """
        Downloads query logs for the specified online archive. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Only or higher role. The API does not support direct calls with the json response schema. You must request a gzip response schema using an accept header of the format: `Accept: application/vnd.atlas.YYYY-MM-DD+gzip`.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection for which you want to return the query logs from one online archive.

        start_date : typing.Optional[int]
            Date and time that specifies the starting point for the range of log messages to return. This resource expresses this value in the number of seconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

        end_date : typing.Optional[int]
            Date and time that specifies the end point for the range of log messages to return. This resource expresses this value in the number of seconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

        archive_only : typing.Optional[bool]
            Flag that indicates whether to download logs for queries against your online archive only or both your online archive and cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.Iterator[bytes]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.online_archive.download_query_logs()
        """
        with self._raw_client.download_query_logs(
            group_id,
            cluster_name,
            start_date=start_date,
            end_date=end_date,
            archive_only=archive_only,
            request_options=request_options,
        ) as r:
            yield from r.data


class AsyncOnlineArchiveClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawOnlineArchiveClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawOnlineArchiveClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawOnlineArchiveClient
        """
        return self._raw_client

    async def list_online_archives(
        self,
        group_id: str,
        cluster_name: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedOnlineArchive:
        """
        Returns details of all online archives. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection for which you want to return the online archives.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedOnlineArchive
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.online_archive.list_online_archives(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_online_archives(
            group_id,
            cluster_name,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    async def create_online_archive(
        self,
        group_id: str,
        cluster_name: str,
        *,
        coll_name: str,
        criteria: Criteria,
        db_name: str,
        collection_type: typing.Optional[str] = OMIT,
        data_expiration_rule: typing.Optional[DataExpirationRule] = OMIT,
        data_process_region: typing.Optional[CreateDataProcessRegion] = OMIT,
        partition_fields: typing.Optional[typing.Sequence[PartitionField]] = OMIT,
        paused: typing.Optional[bool] = OMIT,
        schedule: typing.Optional[OnlineArchiveSchedule] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupOnlineArchive:
        """
        Creates one online archive. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection for which you want to create one online archive.

        coll_name : str
            Human-readable label that identifies the collection for which you created the online archive.

        criteria : Criteria

        db_name : str
            Human-readable label of the database that contains the collection that contains the online archive.

        collection_type : typing.Optional[str]
            Classification of MongoDB database collection that you want to return.

            If you set this parameter to `TIMESERIES`, set `"criteria.type" : "date"` and `"criteria.dateFormat" : "ISODATE"`.

        data_expiration_rule : typing.Optional[DataExpirationRule]

        data_process_region : typing.Optional[CreateDataProcessRegion]

        partition_fields : typing.Optional[typing.Sequence[PartitionField]]
            List that contains document parameters to use to logically divide data within a collection. Partitions provide a coarse level of filtering of the underlying collection data. To divide your data, specify parameters that you frequently query. If you specified `criteria.type`: `DATE` in the Create One Online Archive endpoint, then you can specify up to three parameters by which to query. One of these parameters must be the `DATE` value, which is required in this case. If you specified `criteria.type`: `CUSTOM` in the Create One Online Archive endpoint, then you can specify up to two parameters by which to query. Queries that don't use `criteria.type`: `DATE` or `criteria.type`: `CUSTOM` parameters cause MongoDB to scan a full collection of all archived documents. This takes more time and increases your costs.

        paused : typing.Optional[bool]
            Flag that indicates whether this online archive exists in the paused state. A request to resume fails if the collection has another active online archive. To pause an active online archive or resume a paused online archive, you must include this parameter. To pause an active archive, set this to **true**. To resume a paused archive, set this to **false**.

        schedule : typing.Optional[OnlineArchiveSchedule]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupOnlineArchive
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, Criteria

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.online_archive.create_online_archive(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                coll_name="collName",
                criteria=Criteria(),
                db_name="dbName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_online_archive(
            group_id,
            cluster_name,
            coll_name=coll_name,
            criteria=criteria,
            db_name=db_name,
            collection_type=collection_type,
            data_expiration_rule=data_expiration_rule,
            data_process_region=data_process_region,
            partition_fields=partition_fields,
            paused=paused,
            schedule=schedule,
            request_options=request_options,
        )
        return _response.data

    async def get_online_archive(
        self,
        group_id: str,
        cluster_name: str,
        archive_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupOnlineArchive:
        """
        Returns one online archive for one cluster. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the specified collection from which Application created the online archive.

        archive_id : str
            Unique 24-hexadecimal digit string that identifies the online archive to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupOnlineArchive
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.online_archive.get_online_archive(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                archive_id="archiveId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_online_archive(
            group_id, cluster_name, archive_id, request_options=request_options
        )
        return _response.data

    async def delete_online_archive(
        self,
        group_id: str,
        cluster_name: str,
        archive_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one online archive. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection from which you want to remove an online archive.

        archive_id : str
            Unique 24-hexadecimal digit string that identifies the online archive to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.online_archive.delete_online_archive(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                archive_id="archiveId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_online_archive(
            group_id, cluster_name, archive_id, request_options=request_options
        )
        return _response.data

    async def update_online_archive(
        self,
        group_id_: str,
        cluster_name_: str,
        archive_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        cluster_name: typing.Optional[str] = OMIT,
        coll_name: typing.Optional[str] = OMIT,
        collection_type: typing.Optional[str] = OMIT,
        criteria: typing.Optional[Criteria] = OMIT,
        data_expiration_rule: typing.Optional[DataExpirationRule] = OMIT,
        data_process_region: typing.Optional[DataProcessRegion] = OMIT,
        data_set_name: typing.Optional[str] = OMIT,
        db_name: typing.Optional[str] = OMIT,
        group_id: typing.Optional[str] = OMIT,
        partition_fields: typing.Optional[typing.Sequence[PartitionField]] = OMIT,
        paused: typing.Optional[bool] = OMIT,
        schedule: typing.Optional[OnlineArchiveSchedule] = OMIT,
        state: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BackupOnlineArchive:
        """
        Updates, pauses, or resumes one online archive. This archive stores data from one cluster within one project. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role.

        Parameters
        ----------
        group_id_ : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name_ : str
            Human-readable label that identifies the cluster that contains the specified collection from which Application created the online archive.

        archive_id : str
            Unique 24-hexadecimal digit string that identifies the online archive to update.

        id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the online archive.

        cluster_name : typing.Optional[str]
            Human-readable label that identifies the cluster that contains the collection for which you want to create an online archive.

        coll_name : typing.Optional[str]
            Human-readable label that identifies the collection for which you created the online archive.

        collection_type : typing.Optional[str]
            Classification of MongoDB database collection that you want to return.

            If you set this parameter to `TIMESERIES`, set `"criteria.type" : "date"` and `"criteria.dateFormat" : "ISODATE"`.

        criteria : typing.Optional[Criteria]

        data_expiration_rule : typing.Optional[DataExpirationRule]

        data_process_region : typing.Optional[DataProcessRegion]

        data_set_name : typing.Optional[str]
            Human-readable label that identifies the dataset that Atlas generates for this online archive.

        db_name : typing.Optional[str]
            Human-readable label of the database that contains the collection that contains the online archive.

        group_id : typing.Optional[str]
            Unique 24-hexadecimal digit string that identifies the project that contains the specified cluster. The specified cluster contains the collection for which to create the online archive.

        partition_fields : typing.Optional[typing.Sequence[PartitionField]]
            List that contains document parameters to use to logically divide data within a collection. Partitions provide a coarse level of filtering of the underlying collection data. To divide your data, specify parameters that you frequently query. If you specified `criteria.type`: `DATE` in the Create One Online Archive endpoint, then you can specify up to three parameters by which to query. One of these parameters must be the `DATE` value, which is required in this case. If you specified `criteria.type`: `CUSTOM` in the Create One Online Archive endpoint, then you can specify up to two parameters by which to query. Queries that don't use `criteria.type`: `DATE` or `criteria.type`: `CUSTOM` parameters cause MongoDB to scan a full collection of all archived documents. This takes more time and increases your costs.

        paused : typing.Optional[bool]
            Flag that indicates whether this online archive exists in the paused state. A request to resume fails if the collection has another active online archive. To pause an active online archive or resume a paused online archive, you must include this parameter. To pause an active archive, set this to **true**. To resume a paused archive, set this to **false**.

        schedule : typing.Optional[OnlineArchiveSchedule]

        state : typing.Optional[str]
            Phase of the process to create this online archive when you made this request.

            | State       | Indication |
            |-------------|------------|
            | `PENDING`   | MongoDB Cloud has queued documents for archive. Archiving hasn't started. |
            | `ARCHIVING` | MongoDB Cloud started archiving documents that meet the archival criteria. |
            | `IDLE`      | MongoDB Cloud waits to start the next archival job. |
            | `PAUSING`   | Someone chose to stop archiving. MongoDB Cloud finishes the running archival job then changes the state to `PAUSED` when that job completes. |
            | `PAUSED`    | MongoDB Cloud has stopped archiving. Archived documents can be queried. The specified archiving operation on the active cluster cannot archive additional documents. You can resume archiving for paused archives at any time. |
            | `ORPHANED`  | Someone has deleted the collection associated with an active or paused archive. MongoDB Cloud doesn't delete the archived data. You must manually delete the online archives associated with the deleted collection. |
            | `DELETED`   | Someone has deleted the archive was deleted. When someone deletes an online archive, MongoDB Cloud removes all associated archived documents from the cloud object storage. |

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BackupOnlineArchive
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.online_archive.update_online_archive(
                group_id_="32b6e34b3d91647abb20e7b8",
                cluster_name_="clusterName",
                archive_id="archiveId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_online_archive(
            group_id_,
            cluster_name_,
            archive_id,
            id=id,
            cluster_name=cluster_name,
            coll_name=coll_name,
            collection_type=collection_type,
            criteria=criteria,
            data_expiration_rule=data_expiration_rule,
            data_process_region=data_process_region,
            data_set_name=data_set_name,
            db_name=db_name,
            group_id=group_id,
            partition_fields=partition_fields,
            paused=paused,
            schedule=schedule,
            state=state,
            request_options=request_options,
        )
        return _response.data

    async def download_query_logs(
        self,
        group_id: str,
        cluster_name: str,
        *,
        start_date: typing.Optional[int] = None,
        end_date: typing.Optional[int] = None,
        archive_only: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[bytes]:
        """
        Downloads query logs for the specified online archive. To use this resource, the requesting Service Account or API Key must have the Project Data Access Read Only or higher role. The API does not support direct calls with the json response schema. You must request a gzip response schema using an accept header of the format: `Accept: application/vnd.atlas.YYYY-MM-DD+gzip`.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster that contains the collection for which you want to return the query logs from one online archive.

        start_date : typing.Optional[int]
            Date and time that specifies the starting point for the range of log messages to return. This resource expresses this value in the number of seconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

        end_date : typing.Optional[int]
            Date and time that specifies the end point for the range of log messages to return. This resource expresses this value in the number of seconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).

        archive_only : typing.Optional[bool]
            Flag that indicates whether to download logs for queries against your online archive only or both your online archive and cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.AsyncIterator[bytes]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.online_archive.download_query_logs()


        asyncio.run(main())
        """
        async with self._raw_client.download_query_logs(
            group_id,
            cluster_name,
            start_date=start_date,
            end_date=end_date,
            archive_only=archive_only,
            request_options=request_options,
        ) as r:
            async for _chunk in r.data:
                yield _chunk
