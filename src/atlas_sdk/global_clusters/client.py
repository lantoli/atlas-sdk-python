# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.geo_sharding20240805 import GeoSharding20240805
from ..types.zone_mapping import ZoneMapping
from .raw_client import AsyncRawGlobalClustersClient, RawGlobalClustersClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class GlobalClustersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawGlobalClustersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawGlobalClustersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawGlobalClustersClient
        """
        return self._raw_client

    def get_cluster_global_writes(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GeoSharding20240805:
        """
        Returns one managed namespace within the specified global cluster. A managed namespace identifies a collection using the database name, the dot separator, and the collection name. To use this resource, the requesting Service Account or API Key must have the Project Read Only role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.global_clusters.get_cluster_global_writes(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.get_cluster_global_writes(group_id, cluster_name, request_options=request_options)
        return _response.data

    def create_custom_zone_mapping(
        self,
        group_id: str,
        cluster_name: str,
        *,
        custom_zone_mappings: typing.Sequence[ZoneMapping],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GeoSharding20240805:
        """
        Creates one custom zone mapping for the specified global cluster. A custom zone mapping matches one ISO 3166-2 location code to a zone in your global cluster. By default, MongoDB Cloud maps each location code to the closest geographical zone. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        custom_zone_mappings : typing.Sequence[ZoneMapping]
            List that contains comma-separated key value pairs to map zones to geographic regions. These pairs map an ISO 3166-1a2 location code, with an ISO 3166-2 subdivision code when possible, to the human-readable label for the desired custom zone. MongoDB Cloud maps the ISO 3166-1a2 code to the nearest geographical zone by default. Include this parameter to override the default mappings.

            This parameter returns an empty object if no custom zones exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient, ZoneMapping

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.global_clusters.create_custom_zone_mapping(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            custom_zone_mappings=[
                ZoneMapping(
                    location="location",
                    zone="zone",
                )
            ],
        )
        """
        _response = self._raw_client.create_custom_zone_mapping(
            group_id, cluster_name, custom_zone_mappings=custom_zone_mappings, request_options=request_options
        )
        return _response.data

    def delete_custom_zone_mapping(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GeoSharding20240805:
        """
        Removes all custom zone mappings for the specified global cluster. A custom zone mapping matches one ISO 3166-2 location code to a zone in your global cluster. Removing the custom zone mappings restores the default mapping. By default, MongoDB Cloud maps each location code to the closest geographical zone. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.global_clusters.delete_custom_zone_mapping(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.delete_custom_zone_mapping(group_id, cluster_name, request_options=request_options)
        return _response.data

    def create_managed_namespace(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection: str,
        custom_shard_key: str,
        db: str,
        is_custom_shard_key_hashed: typing.Optional[bool] = OMIT,
        is_shard_key_unique: typing.Optional[bool] = OMIT,
        num_initial_chunks: typing.Optional[int] = OMIT,
        presplit_hashed_zones: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GeoSharding20240805:
        """
        Creates one managed namespace within the specified global cluster. A managed namespace identifies a collection using the database name, the dot separator, and the collection name. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        collection : str
            Human-readable label of the collection to manage for this Global Cluster.

        custom_shard_key : str
            Database parameter used to divide the *collection* into shards. Global clusters require a compound shard key. This compound shard key combines the location parameter and the user-selected custom key.

        db : str
            Human-readable label of the database to manage for this Global Cluster.

        is_custom_shard_key_hashed : typing.Optional[bool]
            Flag that indicates whether someone hashed the custom shard key for the specified collection. If you set this value to `false`, MongoDB Cloud uses ranged sharding.

        is_shard_key_unique : typing.Optional[bool]
            Flag that indicates whether someone [hashed](https://www.mongodb.com/docs/manual/reference/method/sh.shardCollection/#hashed-shard-keys) the custom shard key. If this parameter returns `false`, this cluster uses [ranged sharding](https://www.mongodb.com/docs/manual/core/ranged-sharding/).

        num_initial_chunks : typing.Optional[int]
            Minimum number of chunks to create initially when sharding an empty collection with a [hashed shard key](https://www.mongodb.com/docs/manual/core/hashed-sharding/).

        presplit_hashed_zones : typing.Optional[bool]
            Flag that indicates whether MongoDB Cloud should create and distribute initial chunks for an empty or non-existing collection. MongoDB Cloud distributes data based on the defined zones and zone ranges for the collection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.global_clusters.create_managed_namespace(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            collection="collection",
            custom_shard_key="customShardKey",
            db="db",
        )
        """
        _response = self._raw_client.create_managed_namespace(
            group_id,
            cluster_name,
            collection=collection,
            custom_shard_key=custom_shard_key,
            db=db,
            is_custom_shard_key_hashed=is_custom_shard_key_hashed,
            is_shard_key_unique=is_shard_key_unique,
            num_initial_chunks=num_initial_chunks,
            presplit_hashed_zones=presplit_hashed_zones,
            request_options=request_options,
        )
        return _response.data

    def delete_managed_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        db: typing.Optional[str] = None,
        collection: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GeoSharding20240805:
        """
        Removes one managed namespace within the specified global cluster. A managed namespace identifies a collection using the database name, the dot separator, and the collection name. Deleting a managed namespace does not remove the associated collection or data. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        db : typing.Optional[str]
            Human-readable label that identifies the database that contains the collection.

        collection : typing.Optional[str]
            Human-readable label that identifies the collection associated with the managed namespace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.global_clusters.delete_managed_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.delete_managed_namespaces(
            group_id, cluster_name, db=db, collection=collection, request_options=request_options
        )
        return _response.data


class AsyncGlobalClustersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawGlobalClustersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawGlobalClustersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawGlobalClustersClient
        """
        return self._raw_client

    async def get_cluster_global_writes(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GeoSharding20240805:
        """
        Returns one managed namespace within the specified global cluster. A managed namespace identifies a collection using the database name, the dot separator, and the collection name. To use this resource, the requesting Service Account or API Key must have the Project Read Only role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.global_clusters.get_cluster_global_writes(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_global_writes(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def create_custom_zone_mapping(
        self,
        group_id: str,
        cluster_name: str,
        *,
        custom_zone_mappings: typing.Sequence[ZoneMapping],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GeoSharding20240805:
        """
        Creates one custom zone mapping for the specified global cluster. A custom zone mapping matches one ISO 3166-2 location code to a zone in your global cluster. By default, MongoDB Cloud maps each location code to the closest geographical zone. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        custom_zone_mappings : typing.Sequence[ZoneMapping]
            List that contains comma-separated key value pairs to map zones to geographic regions. These pairs map an ISO 3166-1a2 location code, with an ISO 3166-2 subdivision code when possible, to the human-readable label for the desired custom zone. MongoDB Cloud maps the ISO 3166-1a2 code to the nearest geographical zone by default. Include this parameter to override the default mappings.

            This parameter returns an empty object if no custom zones exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, ZoneMapping

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.global_clusters.create_custom_zone_mapping(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                custom_zone_mappings=[
                    ZoneMapping(
                        location="location",
                        zone="zone",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_custom_zone_mapping(
            group_id, cluster_name, custom_zone_mappings=custom_zone_mappings, request_options=request_options
        )
        return _response.data

    async def delete_custom_zone_mapping(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GeoSharding20240805:
        """
        Removes all custom zone mappings for the specified global cluster. A custom zone mapping matches one ISO 3166-2 location code to a zone in your global cluster. Removing the custom zone mappings restores the default mapping. By default, MongoDB Cloud maps each location code to the closest geographical zone. To use this resource, the requesting Service Account or API Key must have the Project Owner role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.global_clusters.delete_custom_zone_mapping(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_custom_zone_mapping(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def create_managed_namespace(
        self,
        group_id: str,
        cluster_name: str,
        *,
        collection: str,
        custom_shard_key: str,
        db: str,
        is_custom_shard_key_hashed: typing.Optional[bool] = OMIT,
        is_shard_key_unique: typing.Optional[bool] = OMIT,
        num_initial_chunks: typing.Optional[int] = OMIT,
        presplit_hashed_zones: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GeoSharding20240805:
        """
        Creates one managed namespace within the specified global cluster. A managed namespace identifies a collection using the database name, the dot separator, and the collection name. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        collection : str
            Human-readable label of the collection to manage for this Global Cluster.

        custom_shard_key : str
            Database parameter used to divide the *collection* into shards. Global clusters require a compound shard key. This compound shard key combines the location parameter and the user-selected custom key.

        db : str
            Human-readable label of the database to manage for this Global Cluster.

        is_custom_shard_key_hashed : typing.Optional[bool]
            Flag that indicates whether someone hashed the custom shard key for the specified collection. If you set this value to `false`, MongoDB Cloud uses ranged sharding.

        is_shard_key_unique : typing.Optional[bool]
            Flag that indicates whether someone [hashed](https://www.mongodb.com/docs/manual/reference/method/sh.shardCollection/#hashed-shard-keys) the custom shard key. If this parameter returns `false`, this cluster uses [ranged sharding](https://www.mongodb.com/docs/manual/core/ranged-sharding/).

        num_initial_chunks : typing.Optional[int]
            Minimum number of chunks to create initially when sharding an empty collection with a [hashed shard key](https://www.mongodb.com/docs/manual/core/hashed-sharding/).

        presplit_hashed_zones : typing.Optional[bool]
            Flag that indicates whether MongoDB Cloud should create and distribute initial chunks for an empty or non-existing collection. MongoDB Cloud distributes data based on the defined zones and zone ranges for the collection.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.global_clusters.create_managed_namespace(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                collection="collection",
                custom_shard_key="customShardKey",
                db="db",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_managed_namespace(
            group_id,
            cluster_name,
            collection=collection,
            custom_shard_key=custom_shard_key,
            db=db,
            is_custom_shard_key_hashed=is_custom_shard_key_hashed,
            is_shard_key_unique=is_shard_key_unique,
            num_initial_chunks=num_initial_chunks,
            presplit_hashed_zones=presplit_hashed_zones,
            request_options=request_options,
        )
        return _response.data

    async def delete_managed_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        db: typing.Optional[str] = None,
        collection: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GeoSharding20240805:
        """
        Removes one managed namespace within the specified global cluster. A managed namespace identifies a collection using the database name, the dot separator, and the collection name. Deleting a managed namespace does not remove the associated collection or data. To use this resource, the requesting Service Account or API Key must have the Project Data Access Admin role. Deprecated versions: v2-{2023-02-01}, v2-{2023-01-01}

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies this cluster.

        db : typing.Optional[str]
            Human-readable label that identifies the database that contains the collection.

        collection : typing.Optional[str]
            Human-readable label that identifies the collection associated with the managed namespace.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeoSharding20240805
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.global_clusters.delete_managed_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_managed_namespaces(
            group_id, cluster_name, db=db, collection=collection, request_options=request_options
        )
        return _response.data
