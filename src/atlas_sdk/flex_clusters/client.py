# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.api_atlas_cluster_advanced_configuration import ApiAtlasClusterAdvancedConfiguration
from ..types.bi_connector import BiConnector
from ..types.component_label import ComponentLabel
from ..types.employee_access_grant import EmployeeAccessGrant
from ..types.flex_cluster_description20241113 import FlexClusterDescription20241113
from ..types.flex_provider_settings_create20241113 import FlexProviderSettingsCreate20241113
from ..types.paginated_flex_clusters20241113 import PaginatedFlexClusters20241113
from ..types.replication_spec20240805 import ReplicationSpec20240805
from ..types.resource_tag import ResourceTag
from .raw_client import AsyncRawFlexClustersClient, RawFlexClustersClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class FlexClustersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawFlexClustersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawFlexClustersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawFlexClustersClient
        """
        return self._raw_client

    def list_flex_clusters(
        self,
        group_id: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedFlexClusters20241113:
        """
        Returns details for all flex clusters in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedFlexClusters20241113
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.flex_clusters.list_flex_clusters(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.list_flex_clusters(
            group_id,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    def create_flex_cluster(
        self,
        group_id: str,
        *,
        name: str,
        provider_settings: FlexProviderSettingsCreate20241113,
        tags: typing.Optional[typing.Sequence[ResourceTag]] = OMIT,
        termination_protection_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FlexClusterDescription20241113:
        """
        Creates one flex cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the instance.

        provider_settings : FlexProviderSettingsCreate20241113

        tags : typing.Optional[typing.Sequence[ResourceTag]]
            List that contains key-value pairs between 1 to 255 characters in length for tagging and categorizing the instance.

        termination_protection_enabled : typing.Optional[bool]
            Flag that indicates whether termination protection is enabled on the cluster. If set to `true`, MongoDB Cloud won't delete the cluster. If set to `false`, MongoDB Cloud will delete the cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            Created

        Examples
        --------
        from atlas_sdk import AtlasClient, FlexProviderSettingsCreate20241113

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.flex_clusters.create_flex_cluster(
            group_id="32b6e34b3d91647abb20e7b8",
            name="name",
            provider_settings=FlexProviderSettingsCreate20241113(
                backing_provider_name="backingProviderName",
                region_name="regionName",
            ),
        )
        """
        _response = self._raw_client.create_flex_cluster(
            group_id,
            name=name,
            provider_settings=provider_settings,
            tags=tags,
            termination_protection_enabled=termination_protection_enabled,
            request_options=request_options,
        )
        return _response.data

    def get_flex_cluster(
        self, group_id: str, name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> FlexClusterDescription20241113:
        """
        Returns details for one flex cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the flex cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.flex_clusters.get_flex_cluster(
            group_id="32b6e34b3d91647abb20e7b8",
            name="name",
        )
        """
        _response = self._raw_client.get_flex_cluster(group_id, name, request_options=request_options)
        return _response.data

    def delete_flex_cluster(
        self, group_id: str, name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Removes one flex cluster from the specified project. The flex cluster must have termination protection disabled in order to be deleted. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the flex cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.flex_clusters.delete_flex_cluster(
            group_id="32b6e34b3d91647abb20e7b8",
            name="name",
        )
        """
        _response = self._raw_client.delete_flex_cluster(group_id, name, request_options=request_options)
        return _response.data

    def update_flex_cluster(
        self,
        group_id: str,
        name: str,
        *,
        tags: typing.Optional[typing.Sequence[ResourceTag]] = OMIT,
        termination_protection_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FlexClusterDescription20241113:
        """
        Updates one flex cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the flex cluster.

        tags : typing.Optional[typing.Sequence[ResourceTag]]
            List that contains key-value pairs between 1 to 255 characters in length for tagging and categorizing the instance.

        termination_protection_enabled : typing.Optional[bool]
            Flag that indicates whether termination protection is enabled on the cluster. If set to `true`, MongoDB Cloud won't delete the cluster. If set to `false`, MongoDB Cloud will delete the cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.flex_clusters.update_flex_cluster(
            group_id="32b6e34b3d91647abb20e7b8",
            name="name",
        )
        """
        _response = self._raw_client.update_flex_cluster(
            group_id,
            name,
            tags=tags,
            termination_protection_enabled=termination_protection_enabled,
            request_options=request_options,
        )
        return _response.data

    def tenant_upgrade(
        self,
        group_id: str,
        *,
        name: str,
        accept_data_risks_and_force_replica_set_reconfig: typing.Optional[dt.datetime] = OMIT,
        advanced_configuration: typing.Optional[ApiAtlasClusterAdvancedConfiguration] = OMIT,
        backup_enabled: typing.Optional[bool] = OMIT,
        bi_connector: typing.Optional[BiConnector] = OMIT,
        cluster_type: typing.Optional[str] = OMIT,
        config_server_management_mode: typing.Optional[str] = OMIT,
        disk_warming_mode: typing.Optional[str] = OMIT,
        encryption_at_rest_provider: typing.Optional[str] = OMIT,
        global_cluster_self_managed_sharding: typing.Optional[bool] = OMIT,
        labels: typing.Optional[typing.Sequence[ComponentLabel]] = OMIT,
        mongo_db_employee_access_grant: typing.Optional[EmployeeAccessGrant] = OMIT,
        mongo_db_major_version: typing.Optional[str] = OMIT,
        paused: typing.Optional[bool] = OMIT,
        pit_enabled: typing.Optional[bool] = OMIT,
        redact_client_log_data: typing.Optional[bool] = OMIT,
        replica_set_scaling_strategy: typing.Optional[str] = OMIT,
        replication_specs: typing.Optional[typing.Sequence[ReplicationSpec20240805]] = OMIT,
        retain_backups: typing.Optional[bool] = OMIT,
        root_cert_type: typing.Optional[str] = OMIT,
        tags: typing.Optional[typing.Sequence[ResourceTag]] = OMIT,
        termination_protection_enabled: typing.Optional[bool] = OMIT,
        use_aws_time_based_snapshot_copy_for_fast_initial_sync: typing.Optional[bool] = OMIT,
        version_release_system: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FlexClusterDescription20241113:
        """
        Upgrades a flex cluster to a dedicated cluster (M10+) in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Cluster Manager role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the cluster.

        accept_data_risks_and_force_replica_set_reconfig : typing.Optional[dt.datetime]
            If reconfiguration is necessary to regain a primary due to a regional outage, submit this field alongside your topology reconfiguration to request a new regional outage resistant topology. Forced reconfigurations during an outage of the majority of electable nodes carry a risk of data loss if replicated writes (even majority committed writes) have not been replicated to the new primary node. MongoDB Atlas docs contain more information. To proceed with an operation which carries that risk, set `acceptDataRisksAndForceReplicaSetReconfig` to the current date. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        advanced_configuration : typing.Optional[ApiAtlasClusterAdvancedConfiguration]

        backup_enabled : typing.Optional[bool]
            Flag that indicates whether the cluster can perform backups. If set to `true`, the cluster can perform backups. You must set this value to `true` for NVMe clusters. Backup uses Cloud Backups for dedicated clusters and [Shared Cluster Backups](https://docs.atlas.mongodb.com/backup/shared-tier/overview/) for tenant clusters. If set to `false`, the cluster doesn't use backups.

        bi_connector : typing.Optional[BiConnector]

        cluster_type : typing.Optional[str]
            Configuration of nodes that comprise the cluster.

        config_server_management_mode : typing.Optional[str]
            Config Server Management Mode for creating or updating a sharded cluster. When configured as `ATLAS_MANAGED`, Atlas may automatically switch the cluster's config server type for optimal performance and savings. When configured as `FIXED_TO_DEDICATED`, the cluster will always use a dedicated config server.

        disk_warming_mode : typing.Optional[str]
            Disk warming mode selection.

        encryption_at_rest_provider : typing.Optional[str]
            Cloud service provider that manages your customer keys to provide an additional layer of encryption at rest for the cluster. To enable customer key management for encryption at rest, the cluster `replicationSpecs[n].regionConfigs[m].{type}Specs.instanceSize` setting must be `M10` or higher and `"backupEnabled" : false` or omitted entirely.

        global_cluster_self_managed_sharding : typing.Optional[bool]
            Set this field to configure the Sharding Management Mode when creating a new Global Cluster.

            When set to false, the management mode is set to Atlas-Managed Sharding. This mode fully manages the sharding of your Global Cluster and is built to provide a seamless deployment experience.

            When set to true, the management mode is set to Self-Managed Sharding. This mode leaves the management of shards in your hands and is built to provide an advanced and flexible deployment experience.

            This setting cannot be changed once the cluster is deployed.

        labels : typing.Optional[typing.Sequence[ComponentLabel]]
            Collection of key-value pairs between 1 to 255 characters in length that tag and categorize the cluster. The MongoDB Cloud console doesn't display your labels.

            Cluster labels are deprecated and will be removed in a future release. We strongly recommend that you use Resource Tags instead.

        mongo_db_employee_access_grant : typing.Optional[EmployeeAccessGrant]

        mongo_db_major_version : typing.Optional[str]
            MongoDB major version of the cluster. Set to the binary major version.

            On creation: Choose from the available versions of MongoDB, or leave unspecified for the current recommended default in the MongoDB Cloud platform. The recommended version is a recent Long Term Support version. The default is not guaranteed to be the most recently released version throughout the entire release cycle. For versions available in a specific project, see the linked documentation or use the API endpoint for [project LTS versions endpoint](#tag/Projects/operation/getProjectLtsVersions).

             On update: Increase version only by 1 major version at a time. If the cluster is pinned to a MongoDB feature compatibility version exactly one major version below the current MongoDB version, the MongoDB version can be downgraded to the previous major version.

        paused : typing.Optional[bool]
            Flag that indicates whether the cluster is paused.

        pit_enabled : typing.Optional[bool]
            Flag that indicates whether the cluster uses continuous cloud backups.

        redact_client_log_data : typing.Optional[bool]
            Enable or disable log redaction.

            This setting configures the ``mongod`` or ``mongos`` to redact any document field contents from a message accompanying a given log event before logging. This prevents the program from writing potentially sensitive data stored on the database to the diagnostic log. Metadata such as error or operation codes, line numbers, and source file names are still visible in the logs.

            Use ``redactClientLogData`` in conjunction with Encryption at Rest and TLS/SSL (Transport Encryption) to assist compliance with regulatory requirements.

            *Note*: changing this setting on a cluster will trigger a rolling restart as soon as the cluster is updated.

        replica_set_scaling_strategy : typing.Optional[str]
            Set this field to configure the replica set scaling mode for your cluster.

            By default, Atlas scales under `WORKLOAD_TYPE`. This mode allows Atlas to scale your analytics nodes in parallel to your operational nodes.

            When configured as `SEQUENTIAL`, Atlas scales all nodes sequentially. This mode is intended for steady-state workloads and applications performing latency-sensitive secondary reads.

            When configured as `NODE_TYPE`, Atlas scales your electable nodes in parallel with your read-only and analytics nodes. This mode is intended for large, dynamic workloads requiring frequent and timely cluster tier scaling. This is the fastest scaling strategy, but it might impact latency of workloads when performing extensive secondary reads.

        replication_specs : typing.Optional[typing.Sequence[ReplicationSpec20240805]]
            List of settings that configure your cluster regions. This array has one object per shard representing node configurations in each shard. For replica sets there is only one object representing node configurations.

        retain_backups : typing.Optional[bool]
            Flag that indicates whether the cluster retains backups.

        root_cert_type : typing.Optional[str]
            Root Certificate Authority that MongoDB Atlas cluster uses. MongoDB Cloud supports Internet Security Research Group.

        tags : typing.Optional[typing.Sequence[ResourceTag]]
            List that contains key-value pairs between 1 to 255 characters in length for tagging and categorizing the cluster.

        termination_protection_enabled : typing.Optional[bool]
            Flag that indicates whether termination protection is enabled on the cluster. If set to `true`, MongoDB Cloud won't delete the cluster. If set to `false`, MongoDB Cloud will delete the cluster.

        use_aws_time_based_snapshot_copy_for_fast_initial_sync : typing.Optional[bool]
            Flag that indicates whether AWS time-based snapshot copies will be used instead of slower standard snapshot copies during fast Atlas cross-region initial syncs. This flag is only relevant for clusters containing AWS nodes.

        version_release_system : typing.Optional[str]
            Method by which the cluster maintains the MongoDB versions. If value is `CONTINUOUS`, you must not specify `mongoDBMajorVersion`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.flex_clusters.tenant_upgrade(
            group_id="32b6e34b3d91647abb20e7b8",
            name="name",
        )
        """
        _response = self._raw_client.tenant_upgrade(
            group_id,
            name=name,
            accept_data_risks_and_force_replica_set_reconfig=accept_data_risks_and_force_replica_set_reconfig,
            advanced_configuration=advanced_configuration,
            backup_enabled=backup_enabled,
            bi_connector=bi_connector,
            cluster_type=cluster_type,
            config_server_management_mode=config_server_management_mode,
            disk_warming_mode=disk_warming_mode,
            encryption_at_rest_provider=encryption_at_rest_provider,
            global_cluster_self_managed_sharding=global_cluster_self_managed_sharding,
            labels=labels,
            mongo_db_employee_access_grant=mongo_db_employee_access_grant,
            mongo_db_major_version=mongo_db_major_version,
            paused=paused,
            pit_enabled=pit_enabled,
            redact_client_log_data=redact_client_log_data,
            replica_set_scaling_strategy=replica_set_scaling_strategy,
            replication_specs=replication_specs,
            retain_backups=retain_backups,
            root_cert_type=root_cert_type,
            tags=tags,
            termination_protection_enabled=termination_protection_enabled,
            use_aws_time_based_snapshot_copy_for_fast_initial_sync=use_aws_time_based_snapshot_copy_for_fast_initial_sync,
            version_release_system=version_release_system,
            request_options=request_options,
        )
        return _response.data


class AsyncFlexClustersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawFlexClustersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawFlexClustersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawFlexClustersClient
        """
        return self._raw_client

    async def list_flex_clusters(
        self,
        group_id: str,
        *,
        include_count: typing.Optional[bool] = None,
        items_per_page: typing.Optional[int] = None,
        page_num: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedFlexClusters20241113:
        """
        Returns details for all flex clusters in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        include_count : typing.Optional[bool]
            Flag that indicates whether the response returns the total number of items (`totalCount`) in the response.

        items_per_page : typing.Optional[int]
            Number of items that the response returns per page.

        page_num : typing.Optional[int]
            Number of the page that displays the current set of the total objects that the response returns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedFlexClusters20241113
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.flex_clusters.list_flex_clusters(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_flex_clusters(
            group_id,
            include_count=include_count,
            items_per_page=items_per_page,
            page_num=page_num,
            request_options=request_options,
        )
        return _response.data

    async def create_flex_cluster(
        self,
        group_id: str,
        *,
        name: str,
        provider_settings: FlexProviderSettingsCreate20241113,
        tags: typing.Optional[typing.Sequence[ResourceTag]] = OMIT,
        termination_protection_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FlexClusterDescription20241113:
        """
        Creates one flex cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the instance.

        provider_settings : FlexProviderSettingsCreate20241113

        tags : typing.Optional[typing.Sequence[ResourceTag]]
            List that contains key-value pairs between 1 to 255 characters in length for tagging and categorizing the instance.

        termination_protection_enabled : typing.Optional[bool]
            Flag that indicates whether termination protection is enabled on the cluster. If set to `true`, MongoDB Cloud won't delete the cluster. If set to `false`, MongoDB Cloud will delete the cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            Created

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient, FlexProviderSettingsCreate20241113

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.flex_clusters.create_flex_cluster(
                group_id="32b6e34b3d91647abb20e7b8",
                name="name",
                provider_settings=FlexProviderSettingsCreate20241113(
                    backing_provider_name="backingProviderName",
                    region_name="regionName",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_flex_cluster(
            group_id,
            name=name,
            provider_settings=provider_settings,
            tags=tags,
            termination_protection_enabled=termination_protection_enabled,
            request_options=request_options,
        )
        return _response.data

    async def get_flex_cluster(
        self, group_id: str, name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> FlexClusterDescription20241113:
        """
        Returns details for one flex cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the flex cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.flex_clusters.get_flex_cluster(
                group_id="32b6e34b3d91647abb20e7b8",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_flex_cluster(group_id, name, request_options=request_options)
        return _response.data

    async def delete_flex_cluster(
        self, group_id: str, name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Removes one flex cluster from the specified project. The flex cluster must have termination protection disabled in order to be deleted. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the flex cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.flex_clusters.delete_flex_cluster(
                group_id="32b6e34b3d91647abb20e7b8",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_flex_cluster(group_id, name, request_options=request_options)
        return _response.data

    async def update_flex_cluster(
        self,
        group_id: str,
        name: str,
        *,
        tags: typing.Optional[typing.Sequence[ResourceTag]] = OMIT,
        termination_protection_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FlexClusterDescription20241113:
        """
        Updates one flex cluster in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the flex cluster.

        tags : typing.Optional[typing.Sequence[ResourceTag]]
            List that contains key-value pairs between 1 to 255 characters in length for tagging and categorizing the instance.

        termination_protection_enabled : typing.Optional[bool]
            Flag that indicates whether termination protection is enabled on the cluster. If set to `true`, MongoDB Cloud won't delete the cluster. If set to `false`, MongoDB Cloud will delete the cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.flex_clusters.update_flex_cluster(
                group_id="32b6e34b3d91647abb20e7b8",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_flex_cluster(
            group_id,
            name,
            tags=tags,
            termination_protection_enabled=termination_protection_enabled,
            request_options=request_options,
        )
        return _response.data

    async def tenant_upgrade(
        self,
        group_id: str,
        *,
        name: str,
        accept_data_risks_and_force_replica_set_reconfig: typing.Optional[dt.datetime] = OMIT,
        advanced_configuration: typing.Optional[ApiAtlasClusterAdvancedConfiguration] = OMIT,
        backup_enabled: typing.Optional[bool] = OMIT,
        bi_connector: typing.Optional[BiConnector] = OMIT,
        cluster_type: typing.Optional[str] = OMIT,
        config_server_management_mode: typing.Optional[str] = OMIT,
        disk_warming_mode: typing.Optional[str] = OMIT,
        encryption_at_rest_provider: typing.Optional[str] = OMIT,
        global_cluster_self_managed_sharding: typing.Optional[bool] = OMIT,
        labels: typing.Optional[typing.Sequence[ComponentLabel]] = OMIT,
        mongo_db_employee_access_grant: typing.Optional[EmployeeAccessGrant] = OMIT,
        mongo_db_major_version: typing.Optional[str] = OMIT,
        paused: typing.Optional[bool] = OMIT,
        pit_enabled: typing.Optional[bool] = OMIT,
        redact_client_log_data: typing.Optional[bool] = OMIT,
        replica_set_scaling_strategy: typing.Optional[str] = OMIT,
        replication_specs: typing.Optional[typing.Sequence[ReplicationSpec20240805]] = OMIT,
        retain_backups: typing.Optional[bool] = OMIT,
        root_cert_type: typing.Optional[str] = OMIT,
        tags: typing.Optional[typing.Sequence[ResourceTag]] = OMIT,
        termination_protection_enabled: typing.Optional[bool] = OMIT,
        use_aws_time_based_snapshot_copy_for_fast_initial_sync: typing.Optional[bool] = OMIT,
        version_release_system: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FlexClusterDescription20241113:
        """
        Upgrades a flex cluster to a dedicated cluster (M10+) in the specified project. To use this resource, the requesting Service Account or API Key must have the Project Cluster Manager role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        name : str
            Human-readable label that identifies the cluster.

        accept_data_risks_and_force_replica_set_reconfig : typing.Optional[dt.datetime]
            If reconfiguration is necessary to regain a primary due to a regional outage, submit this field alongside your topology reconfiguration to request a new regional outage resistant topology. Forced reconfigurations during an outage of the majority of electable nodes carry a risk of data loss if replicated writes (even majority committed writes) have not been replicated to the new primary node. MongoDB Atlas docs contain more information. To proceed with an operation which carries that risk, set `acceptDataRisksAndForceReplicaSetReconfig` to the current date. This parameter expresses its value in the ISO 8601 timestamp format in UTC.

        advanced_configuration : typing.Optional[ApiAtlasClusterAdvancedConfiguration]

        backup_enabled : typing.Optional[bool]
            Flag that indicates whether the cluster can perform backups. If set to `true`, the cluster can perform backups. You must set this value to `true` for NVMe clusters. Backup uses Cloud Backups for dedicated clusters and [Shared Cluster Backups](https://docs.atlas.mongodb.com/backup/shared-tier/overview/) for tenant clusters. If set to `false`, the cluster doesn't use backups.

        bi_connector : typing.Optional[BiConnector]

        cluster_type : typing.Optional[str]
            Configuration of nodes that comprise the cluster.

        config_server_management_mode : typing.Optional[str]
            Config Server Management Mode for creating or updating a sharded cluster. When configured as `ATLAS_MANAGED`, Atlas may automatically switch the cluster's config server type for optimal performance and savings. When configured as `FIXED_TO_DEDICATED`, the cluster will always use a dedicated config server.

        disk_warming_mode : typing.Optional[str]
            Disk warming mode selection.

        encryption_at_rest_provider : typing.Optional[str]
            Cloud service provider that manages your customer keys to provide an additional layer of encryption at rest for the cluster. To enable customer key management for encryption at rest, the cluster `replicationSpecs[n].regionConfigs[m].{type}Specs.instanceSize` setting must be `M10` or higher and `"backupEnabled" : false` or omitted entirely.

        global_cluster_self_managed_sharding : typing.Optional[bool]
            Set this field to configure the Sharding Management Mode when creating a new Global Cluster.

            When set to false, the management mode is set to Atlas-Managed Sharding. This mode fully manages the sharding of your Global Cluster and is built to provide a seamless deployment experience.

            When set to true, the management mode is set to Self-Managed Sharding. This mode leaves the management of shards in your hands and is built to provide an advanced and flexible deployment experience.

            This setting cannot be changed once the cluster is deployed.

        labels : typing.Optional[typing.Sequence[ComponentLabel]]
            Collection of key-value pairs between 1 to 255 characters in length that tag and categorize the cluster. The MongoDB Cloud console doesn't display your labels.

            Cluster labels are deprecated and will be removed in a future release. We strongly recommend that you use Resource Tags instead.

        mongo_db_employee_access_grant : typing.Optional[EmployeeAccessGrant]

        mongo_db_major_version : typing.Optional[str]
            MongoDB major version of the cluster. Set to the binary major version.

            On creation: Choose from the available versions of MongoDB, or leave unspecified for the current recommended default in the MongoDB Cloud platform. The recommended version is a recent Long Term Support version. The default is not guaranteed to be the most recently released version throughout the entire release cycle. For versions available in a specific project, see the linked documentation or use the API endpoint for [project LTS versions endpoint](#tag/Projects/operation/getProjectLtsVersions).

             On update: Increase version only by 1 major version at a time. If the cluster is pinned to a MongoDB feature compatibility version exactly one major version below the current MongoDB version, the MongoDB version can be downgraded to the previous major version.

        paused : typing.Optional[bool]
            Flag that indicates whether the cluster is paused.

        pit_enabled : typing.Optional[bool]
            Flag that indicates whether the cluster uses continuous cloud backups.

        redact_client_log_data : typing.Optional[bool]
            Enable or disable log redaction.

            This setting configures the ``mongod`` or ``mongos`` to redact any document field contents from a message accompanying a given log event before logging. This prevents the program from writing potentially sensitive data stored on the database to the diagnostic log. Metadata such as error or operation codes, line numbers, and source file names are still visible in the logs.

            Use ``redactClientLogData`` in conjunction with Encryption at Rest and TLS/SSL (Transport Encryption) to assist compliance with regulatory requirements.

            *Note*: changing this setting on a cluster will trigger a rolling restart as soon as the cluster is updated.

        replica_set_scaling_strategy : typing.Optional[str]
            Set this field to configure the replica set scaling mode for your cluster.

            By default, Atlas scales under `WORKLOAD_TYPE`. This mode allows Atlas to scale your analytics nodes in parallel to your operational nodes.

            When configured as `SEQUENTIAL`, Atlas scales all nodes sequentially. This mode is intended for steady-state workloads and applications performing latency-sensitive secondary reads.

            When configured as `NODE_TYPE`, Atlas scales your electable nodes in parallel with your read-only and analytics nodes. This mode is intended for large, dynamic workloads requiring frequent and timely cluster tier scaling. This is the fastest scaling strategy, but it might impact latency of workloads when performing extensive secondary reads.

        replication_specs : typing.Optional[typing.Sequence[ReplicationSpec20240805]]
            List of settings that configure your cluster regions. This array has one object per shard representing node configurations in each shard. For replica sets there is only one object representing node configurations.

        retain_backups : typing.Optional[bool]
            Flag that indicates whether the cluster retains backups.

        root_cert_type : typing.Optional[str]
            Root Certificate Authority that MongoDB Atlas cluster uses. MongoDB Cloud supports Internet Security Research Group.

        tags : typing.Optional[typing.Sequence[ResourceTag]]
            List that contains key-value pairs between 1 to 255 characters in length for tagging and categorizing the cluster.

        termination_protection_enabled : typing.Optional[bool]
            Flag that indicates whether termination protection is enabled on the cluster. If set to `true`, MongoDB Cloud won't delete the cluster. If set to `false`, MongoDB Cloud will delete the cluster.

        use_aws_time_based_snapshot_copy_for_fast_initial_sync : typing.Optional[bool]
            Flag that indicates whether AWS time-based snapshot copies will be used instead of slower standard snapshot copies during fast Atlas cross-region initial syncs. This flag is only relevant for clusters containing AWS nodes.

        version_release_system : typing.Optional[str]
            Method by which the cluster maintains the MongoDB versions. If value is `CONTINUOUS`, you must not specify `mongoDBMajorVersion`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlexClusterDescription20241113
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.flex_clusters.tenant_upgrade(
                group_id="32b6e34b3d91647abb20e7b8",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.tenant_upgrade(
            group_id,
            name=name,
            accept_data_risks_and_force_replica_set_reconfig=accept_data_risks_and_force_replica_set_reconfig,
            advanced_configuration=advanced_configuration,
            backup_enabled=backup_enabled,
            bi_connector=bi_connector,
            cluster_type=cluster_type,
            config_server_management_mode=config_server_management_mode,
            disk_warming_mode=disk_warming_mode,
            encryption_at_rest_provider=encryption_at_rest_provider,
            global_cluster_self_managed_sharding=global_cluster_self_managed_sharding,
            labels=labels,
            mongo_db_employee_access_grant=mongo_db_employee_access_grant,
            mongo_db_major_version=mongo_db_major_version,
            paused=paused,
            pit_enabled=pit_enabled,
            redact_client_log_data=redact_client_log_data,
            replica_set_scaling_strategy=replica_set_scaling_strategy,
            replication_specs=replication_specs,
            retain_backups=retain_backups,
            root_cert_type=root_cert_type,
            tags=tags,
            termination_protection_enabled=termination_protection_enabled,
            use_aws_time_based_snapshot_copy_for_fast_initial_sync=use_aws_time_based_snapshot_copy_for_fast_initial_sync,
            version_release_system=version_release_system,
            request_options=request_options,
        )
        return _response.data
