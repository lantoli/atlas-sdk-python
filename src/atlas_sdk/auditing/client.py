# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.audit_log import AuditLog
from .raw_client import AsyncRawAuditingClient, RawAuditingClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AuditingClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAuditingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAuditingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAuditingClient
        """
        return self._raw_client

    def get_group_audit_log(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AuditLog:
        """
        Returns the auditing configuration for the specified project. The auditing configuration defines the events that MongoDB Cloud records in the audit log. To use this resource, the requesting Service Account or API Key must have the Project Owner role. This feature isn't available for `M0`, `M2`, `M5`, or serverless clusters.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AuditLog
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.auditing.get_group_audit_log(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.get_group_audit_log(group_id, request_options=request_options)
        return _response.data

    def update_audit_log(
        self,
        group_id: str,
        *,
        audit_authorization_success: typing.Optional[bool] = OMIT,
        audit_filter: typing.Optional[str] = OMIT,
        configuration_type: typing.Optional[str] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AuditLog:
        """
        Updates the auditing configuration for the specified project. The auditing configuration defines the events that MongoDB Cloud records in the audit log. To use this resource, the requesting Service Account or API Key must have the Project Owner role. This feature isn't available for `M0`, `M2`, `M5`, or serverless clusters.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        audit_authorization_success : typing.Optional[bool]
            Flag that indicates whether someone set auditing to track successful authentications. This only applies to the `"atype" : "authCheck"` audit filter. Setting this parameter to `true` degrades cluster performance.

        audit_filter : typing.Optional[str]
            JSON document that specifies which events to record. Escape any characters that may prevent parsing, such as single or double quotes, using a backslash (`\\`).

        configuration_type : typing.Optional[str]
            Human-readable label that displays how to configure the audit filter.

        enabled : typing.Optional[bool]
            Flag that indicates whether someone enabled database auditing for the specified project.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AuditLog
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.auditing.update_audit_log(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.update_audit_log(
            group_id,
            audit_authorization_success=audit_authorization_success,
            audit_filter=audit_filter,
            configuration_type=configuration_type,
            enabled=enabled,
            request_options=request_options,
        )
        return _response.data


class AsyncAuditingClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAuditingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAuditingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAuditingClient
        """
        return self._raw_client

    async def get_group_audit_log(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AuditLog:
        """
        Returns the auditing configuration for the specified project. The auditing configuration defines the events that MongoDB Cloud records in the audit log. To use this resource, the requesting Service Account or API Key must have the Project Owner role. This feature isn't available for `M0`, `M2`, `M5`, or serverless clusters.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AuditLog
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.auditing.get_group_audit_log(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_group_audit_log(group_id, request_options=request_options)
        return _response.data

    async def update_audit_log(
        self,
        group_id: str,
        *,
        audit_authorization_success: typing.Optional[bool] = OMIT,
        audit_filter: typing.Optional[str] = OMIT,
        configuration_type: typing.Optional[str] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AuditLog:
        """
        Updates the auditing configuration for the specified project. The auditing configuration defines the events that MongoDB Cloud records in the audit log. To use this resource, the requesting Service Account or API Key must have the Project Owner role. This feature isn't available for `M0`, `M2`, `M5`, or serverless clusters.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        audit_authorization_success : typing.Optional[bool]
            Flag that indicates whether someone set auditing to track successful authentications. This only applies to the `"atype" : "authCheck"` audit filter. Setting this parameter to `true` degrades cluster performance.

        audit_filter : typing.Optional[str]
            JSON document that specifies which events to record. Escape any characters that may prevent parsing, such as single or double quotes, using a backslash (`\\`).

        configuration_type : typing.Optional[str]
            Human-readable label that displays how to configure the audit filter.

        enabled : typing.Optional[bool]
            Flag that indicates whether someone enabled database auditing for the specified project.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AuditLog
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.auditing.update_audit_log(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_audit_log(
            group_id,
            audit_authorization_success=audit_authorization_success,
            audit_filter=audit_filter,
            configuration_type=configuration_type,
            enabled=enabled,
            request_options=request_options,
        )
        return _response.data
