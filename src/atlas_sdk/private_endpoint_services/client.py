# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.create_gcp_forwarding_rule_request import CreateGcpForwardingRuleRequest
from ..types.endpoint_service import EndpointService
from ..types.private_link_endpoint import PrivateLinkEndpoint
from ..types.project_setting_item import ProjectSettingItem
from .raw_client import AsyncRawPrivateEndpointServicesClient, RawPrivateEndpointServicesClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PrivateEndpointServicesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPrivateEndpointServicesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPrivateEndpointServicesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPrivateEndpointServicesClient
        """
        return self._raw_client

    def list_private_endpoint_service(
        self, group_id: str, cloud_provider: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[EndpointService]:
        """
        Returns the name, interfaces, and state of all private endpoint services for the specified cloud service provider. This cloud service provider manages the private endpoint service for the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint service.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[EndpointService]
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.list_private_endpoint_service(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
        )
        """
        _response = self._raw_client.list_private_endpoint_service(
            group_id, cloud_provider, request_options=request_options
        )
        return _response.data

    def get_private_endpoint_service(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EndpointService:
        """
        Returns the name, interfaces, and state of the specified private endpoint service from one project. The cloud service provider hosted this private endpoint service that belongs to the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint service.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service that you want to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EndpointService
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.get_private_endpoint_service(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
            endpoint_service_id="endpointServiceId",
        )
        """
        _response = self._raw_client.get_private_endpoint_service(
            group_id, cloud_provider, endpoint_service_id, request_options=request_options
        )
        return _response.data

    def delete_private_endpoint_service(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one private endpoint service from the specified project. This cloud service provider manages the private endpoint service that belongs to the project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint service.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service that you want to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.delete_private_endpoint_service(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
            endpoint_service_id="endpointServiceId",
        )
        """
        _response = self._raw_client.delete_private_endpoint_service(
            group_id, cloud_provider, endpoint_service_id, request_options=request_options
        )
        return _response.data

    def create_private_endpoint(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        private_endpoint_ip_address: typing.Optional[str] = OMIT,
        endpoint_group_name: typing.Optional[str] = OMIT,
        endpoints: typing.Optional[typing.Sequence[CreateGcpForwardingRuleRequest]] = OMIT,
        gcp_project_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PrivateLinkEndpoint:
        """
        Creates one private endpoint for the specified cloud service provider. This cloud service provider manages the private endpoint service, which in turn manages the private endpoints for the project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. To learn more about considerations, limitations, and prerequisites, see the MongoDB documentation for setting up a private endpoint.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service for which you want to create a private endpoint.

        id : typing.Optional[str]
            Unique string that identifies the private endpoint's network interface that someone added to this private endpoint service.

        private_endpoint_ip_address : typing.Optional[str]
            IPv4 address of the private endpoint in your Azure VNet that someone added to this private endpoint service.

        endpoint_group_name : typing.Optional[str]
            Human-readable label that identifies a set of endpoints. If this private endpoint belongs to a port-mapped endpoint service, this field is still required but ignored.

        endpoints : typing.Optional[typing.Sequence[CreateGcpForwardingRuleRequest]]
            List of individual private endpoints that comprise this endpoint group. If this endpoint belongs to a port-mapped endpoint service, this field will only take in a list of one private endpoint.

        gcp_project_id : typing.Optional[str]
            Unique string that identifies the Google Cloud project in which you created the endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PrivateLinkEndpoint
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.create_private_endpoint(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
            endpoint_service_id="endpointServiceId",
        )
        """
        _response = self._raw_client.create_private_endpoint(
            group_id,
            cloud_provider,
            endpoint_service_id,
            id=id,
            private_endpoint_ip_address=private_endpoint_ip_address,
            endpoint_group_name=endpoint_group_name,
            endpoints=endpoints,
            gcp_project_id=gcp_project_id,
            request_options=request_options,
        )
        return _response.data

    def get_private_endpoint(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        endpoint_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PrivateLinkEndpoint:
        """
        Returns the connection state of the specified private endpoint. The private endpoint service manages this private endpoint which belongs to one project hosted from one cloud service provider. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service for which you want to return a private endpoint.

        endpoint_id : str
            Unique string that identifies the private endpoint you want to return. The format of the `endpointId` parameter differs for AWS and Azure. You must URL encode the `endpointId` for Azure private endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PrivateLinkEndpoint
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.get_private_endpoint(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
            endpoint_service_id="endpointServiceId",
            endpoint_id="endpointId",
        )
        """
        _response = self._raw_client.get_private_endpoint(
            group_id, cloud_provider, endpoint_service_id, endpoint_id, request_options=request_options
        )
        return _response.data

    def delete_private_endpoint(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        endpoint_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one private endpoint from the specified project and private endpoint service, as managed by the specified cloud service provider. When the last private endpoint is removed from a given private endpoint service, that private endpoint service is also removed. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service from which you want to delete a private endpoint.

        endpoint_id : str
            Unique string that identifies the private endpoint you want to delete. The format of the `endpointId` parameter differs for AWS and Azure. You must URL encode the `endpointId` for Azure private endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.delete_private_endpoint(
            group_id="32b6e34b3d91647abb20e7b8",
            cloud_provider="cloudProvider",
            endpoint_service_id="endpointServiceId",
            endpoint_id="endpointId",
        )
        """
        _response = self._raw_client.delete_private_endpoint(
            group_id, cloud_provider, endpoint_service_id, endpoint_id, request_options=request_options
        )
        return _response.data

    def create_private_endpoint_service(
        self,
        group_id: str,
        *,
        provider_name: str,
        region: str,
        port_mapping_enabled: typing.Optional[bool] = OMIT,
        supported_remote_regions: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EndpointService:
        """
        Creates one private endpoint service for the specified cloud service provider. This cloud service provider manages the private endpoint service for the project. When you create a private endpoint service, MongoDB Cloud creates a network container in the project for the cloud provider for which you create the private endpoint service if one doesn't already exist. To learn more about private endpoint terminology in MongoDB Cloud, see Private Endpoint Concepts. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        provider_name : str
            Human-readable label that identifies the cloud service provider for which you want to create the private endpoint service.

        region : str
            Cloud provider region in which you want to create the private endpoint service. Regions accepted as values differ for [Amazon Web Services](https://docs.atlas.mongodb.com/reference/amazon-aws/), [Google Cloud Platform](https://docs.atlas.mongodb.com/reference/google-gcp/), and [Microsoft Azure](https://docs.atlas.mongodb.com/reference/microsoft-azure/).

        port_mapping_enabled : typing.Optional[bool]
            Flag that indicates whether this endpoint service uses PSC port-mapping. This is only applicable for GCP Private Endpoint Services.

        supported_remote_regions : typing.Optional[typing.Sequence[str]]
            List of regions that the endpoint service supports. Native cross region support is implemented for AWS only.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EndpointService
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.create_private_endpoint_service(
            group_id="32b6e34b3d91647abb20e7b8",
            provider_name="providerName",
            region="region",
        )
        """
        _response = self._raw_client.create_private_endpoint_service(
            group_id,
            provider_name=provider_name,
            region=region,
            port_mapping_enabled=port_mapping_enabled,
            supported_remote_regions=supported_remote_regions,
            request_options=request_options,
        )
        return _response.data

    def update_private_endpoint_service(
        self,
        group_id: str,
        endpoint_service_id: str,
        *,
        cloud_provider: str,
        supported_remote_regions: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EndpointService:
        """
        Updates the specified private endpoint service for the project. The cloud service provider manages the private endpoint service that belongs to the project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service that you want to update.

        cloud_provider : str
            Human-readable label that identifies the cloud service provider for the private endpoint service which you want to update.

        supported_remote_regions : typing.Optional[typing.Sequence[str]]
            List of regions that the endpoint service supports. Native cross region support is implemented for AWS only.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EndpointService
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.update_private_endpoint_service(
            group_id="32b6e34b3d91647abb20e7b8",
            endpoint_service_id="endpointServiceId",
            cloud_provider="cloudProvider",
        )
        """
        _response = self._raw_client.update_private_endpoint_service(
            group_id,
            endpoint_service_id,
            cloud_provider=cloud_provider,
            supported_remote_regions=supported_remote_regions,
            request_options=request_options,
        )
        return _response.data

    def get_regional_endpoint_mode(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ProjectSettingItem:
        """
        Checks whether each region in the specified cloud service provider can create multiple private endpoints per region. The cloud service provider manages the private endpoint for the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ProjectSettingItem
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.get_regional_endpoint_mode(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.get_regional_endpoint_mode(group_id, request_options=request_options)
        return _response.data

    def toggle_regional_endpoint_mode(
        self, group_id: str, *, enabled: bool, request_options: typing.Optional[RequestOptions] = None
    ) -> ProjectSettingItem:
        """
        Enables or disables the ability to create multiple private endpoints per region in all cloud service providers in one project. The cloud service provider manages the private endpoints for the project. Connection strings to existing multi-region and global sharded clusters change when you enable this setting. You must update your applications to use the new connection strings. This might cause downtime. To use this resource, the requesting Service Account or API Key must have the Project Owner role and all clusters in the deployment must be sharded clusters. Once enabled, you cannot create replica sets.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        enabled : bool
            Flag that indicates whether someone enabled the regionalized private endpoint setting for the specified project.

            - Set this value to `true` to enable regionalized private endpoints. This allows you to create more than one private endpoint in a cloud provider region. You need to enable this setting to connect to multi-region and global MongoDB Cloud sharded clusters. Enabling regionalized private endpoints introduces the following limitations:
              - Your applications must use the new connection strings for existing multi-region and global sharded clusters. This might cause downtime.
              - Your MongoDB Cloud project can't contain replica sets nor can you create new replica sets in this project.

              - You can't disable this setting if you have:
                - more than one private endpoint in more than one region
                - more than one private endpoint in one region and one private endpoint in one or more regions.

            - Set this value to `false` to disable regionalized private endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ProjectSettingItem
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.private_endpoint_services.toggle_regional_endpoint_mode(
            group_id="32b6e34b3d91647abb20e7b8",
            enabled=True,
        )
        """
        _response = self._raw_client.toggle_regional_endpoint_mode(
            group_id, enabled=enabled, request_options=request_options
        )
        return _response.data


class AsyncPrivateEndpointServicesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPrivateEndpointServicesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPrivateEndpointServicesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPrivateEndpointServicesClient
        """
        return self._raw_client

    async def list_private_endpoint_service(
        self, group_id: str, cloud_provider: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[EndpointService]:
        """
        Returns the name, interfaces, and state of all private endpoint services for the specified cloud service provider. This cloud service provider manages the private endpoint service for the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint service.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[EndpointService]
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.list_private_endpoint_service(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_private_endpoint_service(
            group_id, cloud_provider, request_options=request_options
        )
        return _response.data

    async def get_private_endpoint_service(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EndpointService:
        """
        Returns the name, interfaces, and state of the specified private endpoint service from one project. The cloud service provider hosted this private endpoint service that belongs to the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint service.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service that you want to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EndpointService
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.get_private_endpoint_service(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
                endpoint_service_id="endpointServiceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_private_endpoint_service(
            group_id, cloud_provider, endpoint_service_id, request_options=request_options
        )
        return _response.data

    async def delete_private_endpoint_service(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one private endpoint service from the specified project. This cloud service provider manages the private endpoint service that belongs to the project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint service.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service that you want to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.delete_private_endpoint_service(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
                endpoint_service_id="endpointServiceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_private_endpoint_service(
            group_id, cloud_provider, endpoint_service_id, request_options=request_options
        )
        return _response.data

    async def create_private_endpoint(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        private_endpoint_ip_address: typing.Optional[str] = OMIT,
        endpoint_group_name: typing.Optional[str] = OMIT,
        endpoints: typing.Optional[typing.Sequence[CreateGcpForwardingRuleRequest]] = OMIT,
        gcp_project_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PrivateLinkEndpoint:
        """
        Creates one private endpoint for the specified cloud service provider. This cloud service provider manages the private endpoint service, which in turn manages the private endpoints for the project. To use this resource, the requesting Service Account or API Key must have the Project Owner role. To learn more about considerations, limitations, and prerequisites, see the MongoDB documentation for setting up a private endpoint.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service for which you want to create a private endpoint.

        id : typing.Optional[str]
            Unique string that identifies the private endpoint's network interface that someone added to this private endpoint service.

        private_endpoint_ip_address : typing.Optional[str]
            IPv4 address of the private endpoint in your Azure VNet that someone added to this private endpoint service.

        endpoint_group_name : typing.Optional[str]
            Human-readable label that identifies a set of endpoints. If this private endpoint belongs to a port-mapped endpoint service, this field is still required but ignored.

        endpoints : typing.Optional[typing.Sequence[CreateGcpForwardingRuleRequest]]
            List of individual private endpoints that comprise this endpoint group. If this endpoint belongs to a port-mapped endpoint service, this field will only take in a list of one private endpoint.

        gcp_project_id : typing.Optional[str]
            Unique string that identifies the Google Cloud project in which you created the endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PrivateLinkEndpoint
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.create_private_endpoint(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
                endpoint_service_id="endpointServiceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_private_endpoint(
            group_id,
            cloud_provider,
            endpoint_service_id,
            id=id,
            private_endpoint_ip_address=private_endpoint_ip_address,
            endpoint_group_name=endpoint_group_name,
            endpoints=endpoints,
            gcp_project_id=gcp_project_id,
            request_options=request_options,
        )
        return _response.data

    async def get_private_endpoint(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        endpoint_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PrivateLinkEndpoint:
        """
        Returns the connection state of the specified private endpoint. The private endpoint service manages this private endpoint which belongs to one project hosted from one cloud service provider. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service for which you want to return a private endpoint.

        endpoint_id : str
            Unique string that identifies the private endpoint you want to return. The format of the `endpointId` parameter differs for AWS and Azure. You must URL encode the `endpointId` for Azure private endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PrivateLinkEndpoint
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.get_private_endpoint(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
                endpoint_service_id="endpointServiceId",
                endpoint_id="endpointId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_private_endpoint(
            group_id, cloud_provider, endpoint_service_id, endpoint_id, request_options=request_options
        )
        return _response.data

    async def delete_private_endpoint(
        self,
        group_id: str,
        cloud_provider: str,
        endpoint_service_id: str,
        endpoint_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Any:
        """
        Removes one private endpoint from the specified project and private endpoint service, as managed by the specified cloud service provider. When the last private endpoint is removed from a given private endpoint service, that private endpoint service is also removed. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cloud_provider : str
            Cloud service provider that manages this private endpoint.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service from which you want to delete a private endpoint.

        endpoint_id : str
            Unique string that identifies the private endpoint you want to delete. The format of the `endpointId` parameter differs for AWS and Azure. You must URL encode the `endpointId` for Azure private endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            This endpoint does not return a response body.

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.delete_private_endpoint(
                group_id="32b6e34b3d91647abb20e7b8",
                cloud_provider="cloudProvider",
                endpoint_service_id="endpointServiceId",
                endpoint_id="endpointId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_private_endpoint(
            group_id, cloud_provider, endpoint_service_id, endpoint_id, request_options=request_options
        )
        return _response.data

    async def create_private_endpoint_service(
        self,
        group_id: str,
        *,
        provider_name: str,
        region: str,
        port_mapping_enabled: typing.Optional[bool] = OMIT,
        supported_remote_regions: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EndpointService:
        """
        Creates one private endpoint service for the specified cloud service provider. This cloud service provider manages the private endpoint service for the project. When you create a private endpoint service, MongoDB Cloud creates a network container in the project for the cloud provider for which you create the private endpoint service if one doesn't already exist. To learn more about private endpoint terminology in MongoDB Cloud, see Private Endpoint Concepts. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        provider_name : str
            Human-readable label that identifies the cloud service provider for which you want to create the private endpoint service.

        region : str
            Cloud provider region in which you want to create the private endpoint service. Regions accepted as values differ for [Amazon Web Services](https://docs.atlas.mongodb.com/reference/amazon-aws/), [Google Cloud Platform](https://docs.atlas.mongodb.com/reference/google-gcp/), and [Microsoft Azure](https://docs.atlas.mongodb.com/reference/microsoft-azure/).

        port_mapping_enabled : typing.Optional[bool]
            Flag that indicates whether this endpoint service uses PSC port-mapping. This is only applicable for GCP Private Endpoint Services.

        supported_remote_regions : typing.Optional[typing.Sequence[str]]
            List of regions that the endpoint service supports. Native cross region support is implemented for AWS only.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EndpointService
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.create_private_endpoint_service(
                group_id="32b6e34b3d91647abb20e7b8",
                provider_name="providerName",
                region="region",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_private_endpoint_service(
            group_id,
            provider_name=provider_name,
            region=region,
            port_mapping_enabled=port_mapping_enabled,
            supported_remote_regions=supported_remote_regions,
            request_options=request_options,
        )
        return _response.data

    async def update_private_endpoint_service(
        self,
        group_id: str,
        endpoint_service_id: str,
        *,
        cloud_provider: str,
        supported_remote_regions: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EndpointService:
        """
        Updates the specified private endpoint service for the project. The cloud service provider manages the private endpoint service that belongs to the project. To use this resource, the requesting Service Account or API Key must have the Project Owner role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        endpoint_service_id : str
            Unique 24-hexadecimal digit string that identifies the private endpoint service that you want to update.

        cloud_provider : str
            Human-readable label that identifies the cloud service provider for the private endpoint service which you want to update.

        supported_remote_regions : typing.Optional[typing.Sequence[str]]
            List of regions that the endpoint service supports. Native cross region support is implemented for AWS only.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EndpointService
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.update_private_endpoint_service(
                group_id="32b6e34b3d91647abb20e7b8",
                endpoint_service_id="endpointServiceId",
                cloud_provider="cloudProvider",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_private_endpoint_service(
            group_id,
            endpoint_service_id,
            cloud_provider=cloud_provider,
            supported_remote_regions=supported_remote_regions,
            request_options=request_options,
        )
        return _response.data

    async def get_regional_endpoint_mode(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ProjectSettingItem:
        """
        Checks whether each region in the specified cloud service provider can create multiple private endpoints per region. The cloud service provider manages the private endpoint for the project. To use this resource, the requesting Service Account or API Key must have the Project Read Only role.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ProjectSettingItem
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.get_regional_endpoint_mode(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_regional_endpoint_mode(group_id, request_options=request_options)
        return _response.data

    async def toggle_regional_endpoint_mode(
        self, group_id: str, *, enabled: bool, request_options: typing.Optional[RequestOptions] = None
    ) -> ProjectSettingItem:
        """
        Enables or disables the ability to create multiple private endpoints per region in all cloud service providers in one project. The cloud service provider manages the private endpoints for the project. Connection strings to existing multi-region and global sharded clusters change when you enable this setting. You must update your applications to use the new connection strings. This might cause downtime. To use this resource, the requesting Service Account or API Key must have the Project Owner role and all clusters in the deployment must be sharded clusters. Once enabled, you cannot create replica sets.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        enabled : bool
            Flag that indicates whether someone enabled the regionalized private endpoint setting for the specified project.

            - Set this value to `true` to enable regionalized private endpoints. This allows you to create more than one private endpoint in a cloud provider region. You need to enable this setting to connect to multi-region and global MongoDB Cloud sharded clusters. Enabling regionalized private endpoints introduces the following limitations:
              - Your applications must use the new connection strings for existing multi-region and global sharded clusters. This might cause downtime.
              - Your MongoDB Cloud project can't contain replica sets nor can you create new replica sets in this project.

              - You can't disable this setting if you have:
                - more than one private endpoint in more than one region
                - more than one private endpoint in one region and one private endpoint in one or more regions.

            - Set this value to `false` to disable regionalized private endpoints.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ProjectSettingItem
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.private_endpoint_services.toggle_regional_endpoint_mode(
                group_id="32b6e34b3d91647abb20e7b8",
                enabled=True,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.toggle_regional_endpoint_mode(
            group_id, enabled=enabled, request_options=request_options
        )
        return _response.data
