# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.coll_stats_ranked_namespaces import CollStatsRankedNamespaces
from ..types.measurements_coll_stats_latency_cluster import MeasurementsCollStatsLatencyCluster
from ..types.measurements_coll_stats_latency_host import MeasurementsCollStatsLatencyHost
from ..types.pinned_namespaces import PinnedNamespaces
from .raw_client import AsyncRawCollectionLevelMetricsClient, RawCollectionLevelMetricsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CollectionLevelMetricsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCollectionLevelMetricsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCollectionLevelMetricsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCollectionLevelMetricsClient
        """
        return self._raw_client

    def list_coll_stat_measurements(
        self,
        group_id: str,
        cluster_name: str,
        cluster_view: str,
        database_name: str,
        collection_name: str,
        *,
        metrics: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeasurementsCollStatsLatencyCluster:
        """
        Get a list of the Coll Stats Latency cluster-level measurements for the given namespace.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to retrieve metrics for.

        cluster_view : str
            Human-readable label that identifies the cluster topology to retrieve metrics for.

        database_name : str
            Human-readable label that identifies the database.

        collection_name : str
            Human-readable label that identifies the collection.

        metrics : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            List that contains the metrics that you want to retrieve for the associated data series. If you don't set this parameter, this resource returns data series for all Coll Stats Latency metrics.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeasurementsCollStatsLatencyCluster
            Succeeded

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.list_coll_stat_measurements(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            cluster_view="clusterView",
            database_name="mydb",
            collection_name="mycoll",
            period="PT10H",
        )
        """
        _response = self._raw_client.list_coll_stat_measurements(
            group_id,
            cluster_name,
            cluster_view,
            database_name,
            collection_name,
            metrics=metrics,
            start=start,
            end=end,
            period=period,
            request_options=request_options,
        )
        return _response.data

    def get_cluster_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        cluster_view: str,
        *,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CollStatsRankedNamespaces:
        """
        Return the subset of namespaces from the given cluster sorted by highest total execution time (descending) within the given time window.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to pin namespaces to.

        cluster_view : str
            Human-readable label that identifies the cluster topology to retrieve metrics for.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CollStatsRankedNamespaces
            Succeeded

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.get_cluster_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
            cluster_view="clusterView",
            period="PT10H",
        )
        """
        _response = self._raw_client.get_cluster_namespaces(
            group_id, cluster_name, cluster_view, start=start, end=end, period=period, request_options=request_options
        )
        return _response.data

    def list_pinned_namespaces(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PinnedNamespaces:
        """
        Returns a list of given cluster's pinned namespaces, a set of namespaces manually selected by users to collect query latency metrics on.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to retrieve pinned namespaces for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.list_pinned_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.list_pinned_namespaces(group_id, cluster_name, request_options=request_options)
        return _response.data

    def pin_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        namespaces: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PinnedNamespaces:
        """
        Pin provided list of namespaces for collection-level latency metrics collection for the given Group and Cluster. This initializes a pinned namespaces list or replaces any existing pinned namespaces list for the Group and Cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to pin namespaces to.

        namespaces : typing.Optional[typing.Sequence[str]]
            List of namespace strings (combination of database and collection) on the specified host or cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.pin_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.pin_namespaces(
            group_id, cluster_name, namespaces=namespaces, request_options=request_options
        )
        return _response.data

    def update_pinned_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        namespaces: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PinnedNamespaces:
        """
        Add provided list of namespaces to existing pinned namespaces list for collection-level latency metrics collection for the given Group and Cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to pin namespaces to.

        namespaces : typing.Optional[typing.Sequence[str]]
            List of namespace strings (combination of database and collection) on the specified host or cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.update_pinned_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.update_pinned_namespaces(
            group_id, cluster_name, namespaces=namespaces, request_options=request_options
        )
        return _response.data

    def unpin_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        namespaces: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PinnedNamespaces:
        """
        Unpin provided list of namespaces for collection-level latency metrics collection for the given Group and Cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to unpin namespaces from.

        namespaces : typing.Optional[typing.Sequence[str]]
            List of namespace strings (combination of database and collection) on the specified host or cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.unpin_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            cluster_name="clusterName",
        )
        """
        _response = self._raw_client.unpin_namespaces(
            group_id, cluster_name, namespaces=namespaces, request_options=request_options
        )
        return _response.data

    def list_coll_stat_metrics(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Returns all available Coll Stats Latency metric names and their respective units for the specified project at the time of request.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Succeeded

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.list_coll_stat_metrics(
            group_id="32b6e34b3d91647abb20e7b8",
        )
        """
        _response = self._raw_client.list_coll_stat_metrics(group_id, request_options=request_options)
        return _response.data

    def list_process_measurements(
        self,
        group_id: str,
        process_id: str,
        database_name: str,
        collection_name: str,
        *,
        metrics: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeasurementsCollStatsLatencyHost:
        """
        Get a list of the Coll Stats Latency process-level measurements for the given namespace.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        process_id : str
            Combination of hostname and IANA port that serves the MongoDB process. The host must be the hostname, fully qualified domain name (FQDN), or Internet Protocol address (IPv4 or IPv6) of the host that runs the MongoDB process (mongod or mongos). The port must be the IANA port on which the MongoDB process listens for requests.

        database_name : str
            Human-readable label that identifies the database.

        collection_name : str
            Human-readable label that identifies the collection.

        metrics : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            List that contains the metrics that you want to retrieve for the associated data series. If you don't set this parameter, this resource returns data series for all Coll Stats Latency metrics.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeasurementsCollStatsLatencyHost
            Succeeded

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.list_process_measurements(
            group_id="32b6e34b3d91647abb20e7b8",
            process_id="my.host.name.com:27017",
            database_name="mydb",
            collection_name="mycoll",
            period="PT10H",
        )
        """
        _response = self._raw_client.list_process_measurements(
            group_id,
            process_id,
            database_name,
            collection_name,
            metrics=metrics,
            start=start,
            end=end,
            period=period,
            request_options=request_options,
        )
        return _response.data

    def get_process_namespaces(
        self,
        group_id: str,
        process_id: str,
        *,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CollStatsRankedNamespaces:
        """
        Return the subset of namespaces from the given process ranked by highest total execution time (descending) within the given time window.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        process_id : str
            Combination of hostname and IANA port that serves the MongoDB process. The host must be the hostname, fully qualified domain name (FQDN), or Internet Protocol address (IPv4 or IPv6) of the host that runs the MongoDB process (mongod or mongos). The port must be the IANA port on which the MongoDB process listens for requests.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CollStatsRankedNamespaces
            Succeeded

        Examples
        --------
        from atlas_sdk import AtlasClient

        client = AtlasClient(
            token="YOUR_TOKEN",
        )
        client.collection_level_metrics.get_process_namespaces(
            group_id="32b6e34b3d91647abb20e7b8",
            process_id="my.host.name.com:27017",
            period="PT10H",
        )
        """
        _response = self._raw_client.get_process_namespaces(
            group_id, process_id, start=start, end=end, period=period, request_options=request_options
        )
        return _response.data


class AsyncCollectionLevelMetricsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCollectionLevelMetricsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCollectionLevelMetricsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCollectionLevelMetricsClient
        """
        return self._raw_client

    async def list_coll_stat_measurements(
        self,
        group_id: str,
        cluster_name: str,
        cluster_view: str,
        database_name: str,
        collection_name: str,
        *,
        metrics: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeasurementsCollStatsLatencyCluster:
        """
        Get a list of the Coll Stats Latency cluster-level measurements for the given namespace.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to retrieve metrics for.

        cluster_view : str
            Human-readable label that identifies the cluster topology to retrieve metrics for.

        database_name : str
            Human-readable label that identifies the database.

        collection_name : str
            Human-readable label that identifies the collection.

        metrics : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            List that contains the metrics that you want to retrieve for the associated data series. If you don't set this parameter, this resource returns data series for all Coll Stats Latency metrics.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeasurementsCollStatsLatencyCluster
            Succeeded

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.list_coll_stat_measurements(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                cluster_view="clusterView",
                database_name="mydb",
                collection_name="mycoll",
                period="PT10H",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_coll_stat_measurements(
            group_id,
            cluster_name,
            cluster_view,
            database_name,
            collection_name,
            metrics=metrics,
            start=start,
            end=end,
            period=period,
            request_options=request_options,
        )
        return _response.data

    async def get_cluster_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        cluster_view: str,
        *,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CollStatsRankedNamespaces:
        """
        Return the subset of namespaces from the given cluster sorted by highest total execution time (descending) within the given time window.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to pin namespaces to.

        cluster_view : str
            Human-readable label that identifies the cluster topology to retrieve metrics for.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CollStatsRankedNamespaces
            Succeeded

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.get_cluster_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
                cluster_view="clusterView",
                period="PT10H",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_cluster_namespaces(
            group_id, cluster_name, cluster_view, start=start, end=end, period=period, request_options=request_options
        )
        return _response.data

    async def list_pinned_namespaces(
        self, group_id: str, cluster_name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PinnedNamespaces:
        """
        Returns a list of given cluster's pinned namespaces, a set of namespaces manually selected by users to collect query latency metrics on.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to retrieve pinned namespaces for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.list_pinned_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_pinned_namespaces(
            group_id, cluster_name, request_options=request_options
        )
        return _response.data

    async def pin_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        namespaces: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PinnedNamespaces:
        """
        Pin provided list of namespaces for collection-level latency metrics collection for the given Group and Cluster. This initializes a pinned namespaces list or replaces any existing pinned namespaces list for the Group and Cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to pin namespaces to.

        namespaces : typing.Optional[typing.Sequence[str]]
            List of namespace strings (combination of database and collection) on the specified host or cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.pin_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.pin_namespaces(
            group_id, cluster_name, namespaces=namespaces, request_options=request_options
        )
        return _response.data

    async def update_pinned_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        namespaces: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PinnedNamespaces:
        """
        Add provided list of namespaces to existing pinned namespaces list for collection-level latency metrics collection for the given Group and Cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to pin namespaces to.

        namespaces : typing.Optional[typing.Sequence[str]]
            List of namespace strings (combination of database and collection) on the specified host or cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.update_pinned_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_pinned_namespaces(
            group_id, cluster_name, namespaces=namespaces, request_options=request_options
        )
        return _response.data

    async def unpin_namespaces(
        self,
        group_id: str,
        cluster_name: str,
        *,
        namespaces: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PinnedNamespaces:
        """
        Unpin provided list of namespaces for collection-level latency metrics collection for the given Group and Cluster.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        cluster_name : str
            Human-readable label that identifies the cluster to unpin namespaces from.

        namespaces : typing.Optional[typing.Sequence[str]]
            List of namespace strings (combination of database and collection) on the specified host or cluster.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PinnedNamespaces
            OK

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.unpin_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                cluster_name="clusterName",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unpin_namespaces(
            group_id, cluster_name, namespaces=namespaces, request_options=request_options
        )
        return _response.data

    async def list_coll_stat_metrics(
        self, group_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Any:
        """
        Returns all available Coll Stats Latency metric names and their respective units for the specified project at the time of request.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Any
            Succeeded

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.list_coll_stat_metrics(
                group_id="32b6e34b3d91647abb20e7b8",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_coll_stat_metrics(group_id, request_options=request_options)
        return _response.data

    async def list_process_measurements(
        self,
        group_id: str,
        process_id: str,
        database_name: str,
        collection_name: str,
        *,
        metrics: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeasurementsCollStatsLatencyHost:
        """
        Get a list of the Coll Stats Latency process-level measurements for the given namespace.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        process_id : str
            Combination of hostname and IANA port that serves the MongoDB process. The host must be the hostname, fully qualified domain name (FQDN), or Internet Protocol address (IPv4 or IPv6) of the host that runs the MongoDB process (mongod or mongos). The port must be the IANA port on which the MongoDB process listens for requests.

        database_name : str
            Human-readable label that identifies the database.

        collection_name : str
            Human-readable label that identifies the collection.

        metrics : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            List that contains the metrics that you want to retrieve for the associated data series. If you don't set this parameter, this resource returns data series for all Coll Stats Latency metrics.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeasurementsCollStatsLatencyHost
            Succeeded

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.list_process_measurements(
                group_id="32b6e34b3d91647abb20e7b8",
                process_id="my.host.name.com:27017",
                database_name="mydb",
                collection_name="mycoll",
                period="PT10H",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_process_measurements(
            group_id,
            process_id,
            database_name,
            collection_name,
            metrics=metrics,
            start=start,
            end=end,
            period=period,
            request_options=request_options,
        )
        return _response.data

    async def get_process_namespaces(
        self,
        group_id: str,
        process_id: str,
        *,
        start: typing.Optional[dt.datetime] = None,
        end: typing.Optional[dt.datetime] = None,
        period: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CollStatsRankedNamespaces:
        """
        Return the subset of namespaces from the given process ranked by highest total execution time (descending) within the given time window.

        Parameters
        ----------
        group_id : str
            Unique 24-hexadecimal digit string that identifies your project. Use the [/groups](#tag/Projects/operation/listProjects) endpoint to retrieve all projects to which the authenticated user has access.

            **NOTE**: Groups and projects are synonymous terms. Your group id is the same as your project id. For existing groups, your group/project id remains the same. The resource and corresponding endpoints use the term groups.

        process_id : str
            Combination of hostname and IANA port that serves the MongoDB process. The host must be the hostname, fully qualified domain name (FQDN), or Internet Protocol address (IPv4 or IPv6) of the host that runs the MongoDB process (mongod or mongos). The port must be the IANA port on which the MongoDB process listens for requests.

        start : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        end : typing.Optional[dt.datetime]
            Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**.

        period : typing.Optional[str]
            Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CollStatsRankedNamespaces
            Succeeded

        Examples
        --------
        import asyncio

        from atlas_sdk import AsyncAtlasClient

        client = AsyncAtlasClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.collection_level_metrics.get_process_namespaces(
                group_id="32b6e34b3d91647abb20e7b8",
                process_id="my.host.name.com:27017",
                period="PT10H",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_process_namespaces(
            group_id, process_id, start=start, end=end, period=period, request_options=request_options
        )
        return _response.data
